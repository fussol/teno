# Scope Requests（首相跨域請求登記）

| ID | 檔 | 原因 | 請求首相 | 日期 | 處置 |
|---|---|---|---|---|---|
| C4-SR1 | tools/cli.mjs | cli.mjs:133 `makeSession` 內 `new FSRS()` 未傳 maxIvl（預覽/排程 cap 36500）— 同 C4 缺陷同款；E4-fix-plan.md:100 已登記雙缺（預設權重+cap），併入 E4 處理 | PM1 | 2026-08-27 | ✅ 裁示成立——makeSession 漏網，改走 fsrsCtx；實改由 PM2 E6 commit 帶入（harness `4b3fac2`） |
| C6-SR2 | src/pages/settings.js | settings.js:116/321 渲染 store 版 computeCombinedStats 的 `due`＝第三套待複習平行實作（含 new 額度加總 store.js:464-468），與 dashboard 單源（C6）/badge 可學語意三軌並存 — 跨頁一致性非 PM1 白名單（dashboard.js 內已單源化），登案待改派 | PM1 | 2026-08-27 | ✅ 裁示成立（跨頁三軌並存屬真不一致）——派 PM6（settings.js 領地）統一 due 單源 |
| C7-SR1 | src/pages/study-v4.js, study-mc.js, study-spell.js | C7 完成畫面 undo 的「↩ 復原」按鈕能見性：renderEmpty（study-v4.js:26-48、study-mc.js、study-spell.js）無 undoBtn；renderCard 的 undoBtn 僅 ANSWER 態渲染（study-v4.js:66、study-mc.js:96、study-spell.js:94），手機無鍵盤用戶在 QUESTION/完成態零 undo 路徑。引擎側 Ctrl+Z 已由 C7 修復（PM1 白名單內），按鈕 DOM 屬 PM8 白名單（study-v4.js）→ 登案請改派 | PM1 | 2026-08-27 | ✅ 裁示成立（真 UI 缺口）——派 PM8（study 頁 DOM）改 render 能見性＋綁定判斷 |
| E5-SR1 | tools/cli.mjs | E5 審查期掃出 cmdSimulate（cli.mjs:2295，多日模擬末段 upsert 寫回 cards）自有 FSRS 構造：:2355 `'['+fsrsWeights+']'` 對 bracketed 儲存格式變嵌套陣列（＝E5 修前 cmdAudit 同款崩法）、:2356-2357 steps split-map 無 A4 parseStepsStr 防線、futureCounts 恆 null。fuzz 已是 seeded mulberry32（非 E5 核心 bug）。E5 計畫書 §7 已凍結範圍（僅治 rate/sim/audit 三寫入+replay 路徑），中途擴編違反憲法⑤ → 登案併 C4-SR1（同為 makeSession/自有構造讀路徑統一命題）統由總統裁示另立重構單 | PM2 | 2026-08-27 | ⚪ 裁示部分成立——cmdSelfTest:2238 為測試本體（不動）、:2545 已對齊語意（風格非 bug）→ 登「已涵蓋」；與 C4-SR1 併案，於 `4b3fac2` 裁示定案 |
| G8-SR1 | src/pages/simulator.js | 任務書白名單寫 `src/lib/simulator.js`——該檔**不存在**（ls 實錘）。G8 病灶（`_simResults` 無上限＋每筆 entry `params:{...req}` 全詞表快照 → 記憶體線性成長）實際在 `src/pages/simulator.js`:11/:330-335/:351（行號 2026-08-28 覆核）。白名單意圖待裁示：若 `src/lib/simulator.js` 係 `src/pages/simulator.js` 筆誤 → 請裁示改派或放行；PM1 依鐵律⑦不逕改 | PM1 | 2026-08-28 | ✅ 放行（2026-08-28 總統再派 PM1「從佇列第一顆逐一完成」＝唯一殘題即此二顆；筆誤實證三件：任務書註自明「G26 若需動 **simulator.js 以外檔** → scope-requests」即 simulator.js 本體屬 PM1 領地＋病灶徵狀全庫唯 pages 一份（勘誤：`src/core/simulator.js` 實在但無徵狀，R1#2 校正）＋佇列行號唯 pages 吻合） |
| G26-SR1 | src/pages/simulator.js | 同 G8-SR1 白名單障礙：G26（手機 viewport 切換模式舊 render ref 失效）病灶在 `src/pages/simulator.js` onMount:586 綁定段/`renderInPlace` 呼叫鏈×9 點，非白名單內檔（白名單路徑不存在）。請連同 G8-SR1 一併裁示 | PM1 | 2026-08-28 | ✅ 放行（裁示同 G8-SR1，2026-08-28 再派 PM1 即放行本檔） |
| D5-SR1 | src/lib/api.js | D5（backup_db fs::copy 無 WAL checkpoint→備份缺最近複習）正解修法在白名單外：Rust 端執行 PRAGMA 需經 tauri-plugin-sql 連接池，但 plugin 2.4.0 未 re-export sqlx（lib.rs 僅 `pub use Error/DbPool`，DbPool 內部 accessor 整段註解掉）、Tauri v2 無 server-side invoke、無 rusqlite dep（Cargo.toml 為 PM3 禁區「要加 dep→scope-requests」）——純 lib.rs 零依賴無法執行 PRAGMA，三路實錘封閉。前端已有全部素材：`checkpoint()` 已存在 src/lib/db.js:122-126（PRAGMA wal_checkpoint(TRUNCATE)＋catch），settings.js:548-551 import 路徑已有「checkpoint→backupDb」正確順序先例；缺的是 api.js:72 backupDb 呼叫點未接（4 呼叫點 backup-scheduler.js:33 / settings.js:484/551/928 唯 :551 有 checkpoint）。建議修法（單點封頂）：api.js `import { checkpoint } from './db.js'`＋`export const backupDb = async () => { await checkpoint(); return invoke('backup_db') }`——唯一 choke point 涵蓋全 4 呼叫點，Scheduler 定期備份（最大受害路徑：夜間定時備份永遠缺最後一段 WAL 內複習）自此完整 | PM3 | 2026-08-28 | 待裁示 |
| F10-SR1 | src-tauri/gen/android/app/src/main/java/com/teno/app/TtsPlugin.kt | F10（import_piper_model_dialog Android 必敗）Rust 側死路（FilePath::Url→as_path()=None，file_path.rs:42-47 源碼釘）鏡同檔 import_db_dialog 模式可修，**但委派鏈下游 Kotlin copyUriToCache :272 落地檔名硬編碼 `"import.db"`**——piper 鏈需要真實 .onnx 檔名（collect_piper_voices 以檔名建語音目錄），落地即改名 import.db 則 piper 匯入仍 100% 失敗（僅錯誤訊息換臉），R1 委員鑿出實證（TtsPlugin.kt:272 原文 `java.io.File(activity.cacheDir, "import.db")`，首相 grep 覆核屬實）。正修需兩檔：① Kotlin copyUriToCache 改 ContentResolver.query(OpenableColumns.DISPLAY_NAME) 取真實檔名（失敗兜底＋sanitize），import_db 鏈零影響（.db 檔 DISPLAY_NAME 照樣 .db）② lib.rs import_piper_model_dialog 改裝（补丁已封存 `_dev/notes/F10-pending.patch`，配套驗證 `_dev/notes/F10-pending-verify.mjs` 曾 23/23 綠，Kotlin 修妥即可併入復跑）。gen/android 非 PM3 白名單 → 鐵律⑦登案跳過；**附帶地雷**：import.db 固定名與 db 匯入共用 cacheDir，交錯匯入互相覆寫（序貫無恙，併此單治） | PM3 | 2026-08-28 | 待裁示 |
| D20-SR1 | src/lib/db.js | B4（e53a3ce）已把 exam_history.word 欄語意統一為 word_id，但欄内雙世代混存（B4 前 legacy=單字文字）。GUI 兩清理函式各只蓋一族：deleteWordsByDeck:370 `word IN (SELECT id ...)` 只刪 id 族（legacy 文字孤兒留）、deleteWord:221-226（D14 修法）只刪 text 族（**B4 後新資料 id 族孤兒留＝刪字主路徑現在就會漏**）。CLI 域修法已兩族皆刪（tools/cli.mjs cmdDeleteDeck，verify-d20 T1d/T8c 釘）；db.js 非 PM2 白名單 → 登案請裁示修法（兩條 DELETE 各蓋一族 or JOIN 語意統一），D14 verify 測資只測 text 族故未逮，屬測資世代滯後 | PM2 | 2026-08-28 | 待裁示 |
| E16-SR1 | src/lib/deprecated/sim-behavior.js | E16 刪三死檔後連帶孤兒：sim-behavior.js（7.6KB，analyzeBehavior/getDiffDist/blend 等）唯一 import 者=已刪的 deprecated/sim-engine.js:13，自此全庫零引用。檔案不在 PM2 白名單（任務書僅列 sim-engine/themes/svg 三檔）→依鐵律⑦不逕刪登案請裁示（建議准刪：E8 波次已政策隔離整條舊 JS 模擬鏈，官方 fsrs-rs simulate_fsrs 為唯一模擬路徑，this 檔屬同鏈殘臟；刪後 deprecated/ 目錄空可一併消失） | PM2 | 2026-08-28 | 待裁示 |
| F11-SR1 | src-tauri/build.rs | F11 改 option_env!("TENO_DRIVE_CLIENT_ID/SECRET") 編譯期注入後，發佈者「已編譯過再改 env 值」時 cargo 不感知 env 變動、option_env! 不重新求值（除非 clean 或源檔 mtime 變）。正解＝build.rs 加兩行 `println!("cargo:rerun-if-env-changed=TENO_DRIVE_CLIENT_ID"); println!("cargo:rerun-if-env-changed=TENO_DRIVE_CLIENT_SECRET");`。build.rs 非 PM4 白名單（僅 drive_sync.rs＋_dev/notes）→鐵律⑦登案不逕改；PM4 主修法與驗證不依賴此檔（驗證以 touch 強制重編證 option_env 嵌入鏈），临时解影響面僅發佈者重注入憑證場景 | PM4 | 2026-08-28 | 待裁示 |
- F13: 任務書路徑 src-tauri/src/cambridge/url.rs 實為 src-tauri/cambridge_scraper/src/url.rs（行號/函式吻合，intent 映射登記，PM3 照白名單意圖執行）
| F13-SR2 | src-tauri/cambridge_scraper/src/english.rs（.entry-body 選擇器域） | R1 委員實錘第二根因：Cambridge 新版 tw-/superentry 模板條目頁（get-rid-of/take-a-shower）零 `.entry-body`（但有 def-block×5），scrape_cambridge_html 回 WordNotFound——F13 修好 URL 層（hyphen 直達條目頁）後片語端到端仍「解析失敗」；舊 hello 模板路徑仍綠＝非全局壞。屬 scraper 解析域（非 PM3 白名單 url.rs）→ 登記待派。F13 閉環禁把此徵狀記其帳上 | PM4/他相 | 2026-08-28 | 待裁示 |
| F14-SR1 | tools/cli.mjs | F14 鏡像檔從 /tmp/teno-monitor.log 搬家至 app_log_dir()（symlink 攻擊面根源消除）後，cli.mjs:1476 與 _dev/cli/cli.mjs:1292 的 `teno logs` 預設路徑失效（可傳參覆蓋非完全斷路）。正解一行：預設改 ~/.local/state/com.teno.app/logs/teno-monitor.log。cli.mjs 為他軌在製髒檔（SR-C4）禁夾帶→鐵律⑦登案 | PM2 | 2026-08-28 | 待裁示 |
| D10-SR1 | tools/cli.mjs | D10 修法（find_db_file orderBy+modifiedTime 取最新）在 CLI 鏡像點 :3283-3290 同款 bug（fields=files(id)＋files?.[0]?.id 盲取首筆）；tools/ 非 PM4 白名單（僅 drive_sync.rs＋_dev/notes）→ 鐵律⑦登案不逕改，待總統改派（PM1/PM3 域） | PM4 | 2026-08-28 | 待裁示 |
| D10-SR2 | _dev/cli/cli.mjs | 同 D10-SR1 的第二鏡像（R1#2 委員穷举補登）：_dev/cli/cli.mjs:3046-3051 同款 fields=files(id)＋files?.[0]?.id 盲取；_dev/cli/ 白名單外。D10-SR1 改派時建議同檔同修法一併處理 | PM4 | 2026-08-28 | 待裁示 |
| G9-SR1 | package.json＋src-tauri/tauri.conf.json | G9 fix commit 依憲法版本規範第1條應升版（5.1.19→5.1.20），但兩檔非 PM5 白名單＋多首相並發升版必然衝突（aec329a 後本波 D10/OCR-IMP/F17/G10 fix commit 均未升版＝波尾集中慣例）→鐵律7登案不逕改，留總統波尾裁示 | PM5 | 2026-08-28 | 待裁示 |
| F6-SR1 | src-tauri/gen/android/app/src/main/res/xml/data_extraction_rules.xml（新檔）＋AndroidManifest.xml dataExtractionRules 屬性 | F6 白名單外：allowBackup=false 官方實查不保證 targetSdk31+ D2D 傳輸停止（Samsung Smart Switch 真實暴露面，官方原文「doesn't disable device-to-device transfers」）；屬性引用必須與 res/xml 檔原子同 commit（AAPT link 約束），manifest 註解已指認。recipes（空 section vs exclude 根域）需承接者一手查證後定 | PM5 | 2026-08-28 | 待裁示 |
| F14-SR2 | package.json＋src-tauri/tauri.conf.json | F14 fix commit 依憲法版本規範第1條應升版（patch），兩檔非 PM3 白名單＋多首相並發升版衝突，比照 G9-SR1 併波尾集中升版；F14 commit 帶此登記入庫 | PM3 | 2026-08-28 | ✅ 已結——版本規則改制（b09a3a1）後 F14 commit 19edaf9 直接帶指紋 5.2.6 入庫 |
| F15-SR1 | tools/cli.mjs＋_dev/cli/cli.mjs | report/compare HTML 模板 CDN 無 SRI 共 4 實例：tools/cli.mjs:2031（cmdCompare）、:2821（cmdReport）＋_dev/cli/cli.mjs:1804、:2590（鏡像）。修法要點（R1#3 精確化）：①chart.js 釘版本＋integrity 或改本地資產；②模板系生成器，生成期可算內嵌 script 塊 sha256 寫 `<meta>` CSP（CSP3 准 hash 禁 nonce）＝真解（事後注入式才unsafe-inline 假牙）；③現行嵌入面零字串型使用者資料（逐變數溯源實錘），但 JSON.stringify 不逃 `<`，新增字串欄位必 `\u003c` 轉義；④兩鏡像同步。F15 刪 open_report 死窗後，本 SR＝使用者以瀏覽器開 report.html 之剩余供應鏈面。詳見 F15-fix-plan.md §6 | PM3 | 2026-08-29 | 待裁示 |
| F16-SR1 | src/lib/api.js＋src/pages/export.js | F16 已殲 Rust 死命令 export_csv_data（fn＋註冊），JS 側殘鏈白名單外：api.js:107-108 死 wrapper `exportCsvData`＋export.js:8 死 import（全檔零呼叫，實證見 F16-fix-plan §2 grep 冊）。正解：兩處刪除（可同單清 verify-g24-export-render.mjs:19 mock stub）；**雙向鎖**：落地日須同步修訂 tools/verify-f16-csv-data.mjs T3b「wrapper 在冊」釘（該釘現為 fail-closed 存續期鎖，不修必紅） | PM3 | 2026-08-29 | 待裁示 |
| O3 | src-tauri/gen/android/.../TtsPlugin.kt（PM5 白名單內） | R1#1 挖出：saveExportFile 用 MediaStore$Downloads（API29+ 類），API24-28 裝置匯出先 NoClassDefFoundError 必斷（非 F6 範圍，manifest 層修不了，需版本分支或 ContentResolver 泛化）。F6 佇列外，待總統入站後承接 | PM5 | 2026-08-28 | 待裁示 |
| E16-SR2 | tools/verify-e16-orphan-deletion.mjs | E16-SR1 刪除 sim-behavior.js 後，verify-e16 T5a 臨時釘「sim-behavior 本波必須仍在」注定翻紅（post 態 28/1 實錘）。該工具檔不在 SR1 白名單（tools/ 僅 d5/d20/f16/g24）→鐵律⑦不逕改。請求裁示：將 T5 段更新為「已刪」態或除役 | SR1 | 2026-08-30 | 待裁示 |
