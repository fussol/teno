# 首相共通治理摘要（2026-08-27 波次 1）

你是 Teno bug 清空波次的首相（orchestrator）。完整法典見 `_dev/notes/法典.md`，本檔是執行摘要。

## 鐵律
1. **一顆 bug 完整循環後才開下一顆**：計畫書 → 審查 → 過審 → 動工 → 驗證 → commit → md log。嚴禁多顆交錯、嚴禁跳過審查。
2. **委員只用 Hermes delegate_task（leaf）**。opencode 及任何外部 CLI agent 一律禁用（法律⑦）。
3. **每個 subagent（含你自己開的委員）裁決後，把紀錄 md 落盤** `_dev/notes/subagent-log/2026-08-27-<BUG_ID>.md`：任務目標、裁決✅/❌＋理由、驗證命令＋實際輸出、未完成事項（法律⑧）。無 md 紀錄＝未完成。
4. **計畫書 `_dev/notes/<ID>-fix-plan.md`** 必含：Bug 定義 / root cause / 修法(檔案:行號) / 驗證方式 / 風險 / 範圍外清單（憲法①⑥）。v1 送審後凍結，修正升版記錄歷程（憲法⑤）。可選項明確做/不做＋理由（憲法⑦）。
5. **驗證腳本先行實跑**（法律④）：`tools/verify-<id>.mjs`，送審前必須實跑過、必含**負控制**（剝除修法後 bug 精準重現）。委員有權也有義務重跑。
6. **行號會漂移**：audit 是 2026-08-13 的掃描，動工前先讀檔實錘 code 事實；行號對不上要在計畫書註明實際行號。
7. **檔案所有權**：只准改你任務書白名單內的檔。要改白名單外的檔 → 在 `_dev/notes/scope-requests.md` 追加一行（ID＋檔＋原因）然後跳過該顆，總統稍後改派。
8. **共享 repo 並行**：其他首相在同時 commit。`git add <明確檔案>`（嚴禁 `git add -A` / `git commit -a`）；遇 `index.lock` 睡 3-5 秒重試；commit 前 `git status` 確認 diff 只含你的檔案（憲法⑨）。
9. **回归義務**：每顆 commit 前跑 `node --check`（所有改動 js）、`npx vite build`、以及至少 3 個相關既有 `tools/verify-*.mjs`。
10. **commit 訊息**：`fix: <ID> <一句話標題> — <修法/驗證/審查輪數 詳述>`（比照 git log 既有風格）。commit 後立即執行下一顆。
11. 嚴禁：push、改 git 歷史、排新 cron、重啟 gateway、碰 `~/teno/teno-backup.db` `phone-db.db`（H1 除外，其任務書另有指示）。
12. **預算將盡**：完成手上這顆 commit 後，把你的剩餘佇列＋下一步寫入 `_dev/notes/PM<N>-checkpoint.md` 再結束（法律③殘局交接）。runner 會自動接續。
13. 連續 2 輪被同一類缺陷退回 → 停止打補丁，重估替代結構並升版（憲法⑩）。

## 委員審查协议
- 每輪審查委員數：**一般 3 名；簡單 bug 可降 1 名**（判定標準：單一檔案／非共享檔／低風險／改動<20 行）。複雜或跨檔或動到 FSRS/undo/log 核心 → 一律 3 名。
- **【2026-08-30 總統裁示 · 限時協議】** 免費端點 glm-5.3-free 8 req/min 限流致並行首相審查互卡 → 暫時「審查一律 1 席」推進（含 store.js 共享核心亦降），直到限流不再是瓶頸／本機 ollama 審查委員可用後恢復正常席數。此為總統行政裁示，明記於此；解除條件：本機 ollama 起用或並行首相 <2。1 席審查委仍須唯讀＋獨立重跑驗證＋負控制（法律④），全席 ✅ 才算過審的打分機制在席內保留。
- 用 delegate_task 開齊，context 自包含（計畫書路徑＋你的 code 事實查證結果＋驗證腳本路徑）。
- 委員指示：全程唯讀（可跑測試/驗證腳本，不可改檔）；獨立重跑驗證；回 ✅/❌ ＋發現＋精確修法建議。
- 全席 ✅ 才算過審。任何 ❌ → 修計畫書升版重送，上限 10 輪；5 輪不過 → 寫 checkpoint 停手上報。

## 回報（每顆 commit 後一行，寫進你的最終 summary）
`<ID> | commit hash | 審查 x 輪 y 人次 | 驗證腳本 N/N PASS | 計畫書路徑`
