# Subagent log — F15（2026-08-29, PM3）

## 任務目標
F15：open_report 以 `WebviewUrl::External("file://…/report.html")` 開獨立視窗，
不受 config CSP 管轄＋生產端模板（cli.mjs×2 鏡像）CDN chart.js 無 SRI。
修法＝刪除死命令全家（lib.rs fn 77-91＋generate_handler token，43→42），
生產端 CSP/SRI 登 F15-SR1。計畫書 `_dev/notes/F15-fix-plan.md` v1.0 凍結→v1.1。

## 審查（R1 三席 sequential，全 ✅）
| 席 | 主攻 | 裁決 | 要點 |
|---|---|---|---|
| #1 消費者穷舉 | RC3 死面 | ✅ | 動態形態（模板字串/變數/拼接）全掃皆字面 invoke；Kotlin 零通道；src/teno-5.1.0 散件實為 gitignore 未入庫（比宣稱更乾淨）；Android 不可達三層論據分層誠實；提醒 F15→F16 landing 順序（同行相鄰） |
| #2 驗證鑑識 | 腳本假綠 | ✅ | /tmp 變異實跑五向量（註解留屍/改名復活/本體殲半/幽靈注入/負控制腐化）全封堵 fail-closed；建議①T3 目錄存在性釘（當輪吸收）②NDK glob③拆行註解剝除（後兩項非阻塞登記） |
| #3 治理裁決 | 刪除決策 | ✅ | 一手源碼（tauri-2.11.3/wry-0.55.1）實證白名單內無真解漏網：builder 無 header API、on_web_resource_request 自明不覆蓋 External、uri scheme 僅 Builder 期；M-1 升版行＋M-2 SR 精確化（4 CDN 實例＋生成期 sha256 CSP 真解＋`\u003c` 轉義加固）當輪吸收 v1.1；F16 非空殼認證（export_csv_data 鏈死但獨立） |

無 ❌ 輪次；R1 即閉合（3 人次）。

## 驗證命令＋實際輸出
- 動工前態 `node tools/verify-f15-report-surface.mjs` → 9 PASS/8 FAIL
  （T1×7＋T2d 紅＝徵狀在位設計語意；T2a-c 負控制有牙；T3/T4 綠）
- 動工後同腳本 → **18 PASS / 0 FAIL**（T3-0 新增後 19 條腿全綠）
- `cd src-tauri && cargo test --lib` 總數 42（≥下限釘）、3 預存 sim fixture 紅（非本帳）
- cargo check host＋aarch64-linux-android 雙綠（NDK env 四件套，F12 同款）
- 回歸：verify-f14 38/38、verify-d16 32/32、verify-d17 32/32、npm build 803ms
- 環境假紅案例×2 誠實登記：PATH 缺 cargo（android target）＋NDK env 缺席
  （ring build script 炸）——先復測再歸因（F14 判例沿用）

## 未完成事項
- F15-SR1（4 CDN 模板實例 CSP-hash/SRI/本地化）登 scope-requests.md 待裁示；
  共享檔髒不夾帶（F12/F14 先例）。
- F16 落地時需同步 verify-f15 EXPECTED_CMDS 42→41（S-2 註已寫入計畫書 §6）。
- src/teno-5.1.0{.tar.gz,} 散件清出＝呈總統（gitignore 內，非庫污染）。
