# Subagent 執行紀錄 — E12（2026-08-28，首相2／PM2）

## 任務目標
修 cmdMature sim_runs.total_reviews：超時分支讀 mature.db review_log（＝
app 備份快照冒充模擬 effort）、成功分支不傳（恆 null）。simple 單席制。

## 動工前實錘
- simulate 全鏈只 upsert cards 零寫 review_log（INSERT INTO review_log 唯
  cmdRate:1343＋import:3533）。
- fsrs.js:305 `newReps=(reps||0)+1` 每評 +1 → reps 基線差＝本次 run effort。

## 修法
cmdMature：sumReps 小工具＋repsBase 基線捕捉（建檔/from-zero 後、日循環前）
＋simReviews() 兩出口（成功分支補欄＋超時分支刪錯表查詢）。null 誠實不謊報。
零改 simulate 寫入面（E5 波次政策：不落庫，落庫＝mature.db 假刷題史污染）。

## 委員清單
| 輪 | 席次 | 模型 | 裁決 | 重點 |
|---|---|---|---|---|
| R1 | 1（simple 單席） | HF leaf | ✅ 通過（2 觀察） | reps 語意钉死＋消費者三處零炸＋基線前移變異三連紅 |

## 意外收獲（誠實登記）
初跑紅×2＋地雷×1：
1. 測資 review_log difficulty=NULL ≥30 組 → 踩 cmdSimulate **既存潛在地雷**
   `_diffBins` NaN bin → getDiffDist 崩。R1 親驗收窄：唯「全 NaN 單 bin」才
   崩（NaN 比較 false 自然排除其他態）；真庫 NULL＝0 筆不可達 → 登 §6 另案
   （修法=Number.isFinite 濾 bin，非 ?.dist 靜默降級）。測資改帶 difficulty。
2. T3 初版查從未跑過的 DB（設計失誤）→ 改查 T1 實跑产物。
3. T2 預種卡撞 setup run 已創的卡 → 改 upsert。

## 驗證命令＋實際輸出
- `node tools/verify-e12-mature-totalreviews.mjs` → `verify-e12: 11 PASS / 0 FAIL — ALL PASS`（×2）
  - T1b/c reps增量15==[store.rate]日誌15==total_reviews 15（雙獨立源釘死）
  - T5 二次 run==S2−S1 基線差非總和；T4 負控制快照 40 謊數＋成功分支 null 重現
- 回歸（commit 前，SR-C4 反剝後）：e12 11/11 e11 12/12 e10 9/9 e9 19/19
  e4 24/24 e5 18/18 d19 32/32 d20 31/31 d7 18/18 c3 ALL PASS c5 96/96 vite OK

## 裁決與產物
R1 ✅ 過審。計畫書 v1.1（§6 地雷＋§7 R1）；驗證 tools/verify-e12-mature-totalreviews.mjs
（新建）；改動 tools/cli.mjs cmdMature 段。

## 未完成事項
無。地雷（_diffBins NaN）＋tampering 負數＋spawn 超時低估＋UI ??0 混消 登 §6 另案。
