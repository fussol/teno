# Subagent Log — C6（2026-08-27 波次, PM1）

## 任務目標
審查 C6 修法：dashboard 同頁「待複習」三處（hero/statTile/字本 grid）互相矛盾 → 單源化（getDueCards 權威集剔 new 成 reviewDueSet 三處共用）。R1 三委員＋R2 複審一委員。

## 裁決
### R1（3 委員）
- #1 修法正確性：✅ PASS（附 M1 tzM `x??x` 死式註解誤導 latent、L1 state-0 容器卡 LOW、P1 計畫書「stats.due 零消費者」宣稱有誤＝settings.js:116/321 有 store 版消費者）
- #2 消費者完整性：✅ PASS（dueCount 全庫消費者窮舉：main.js badge×3/study.js/store producer 全未動；notify 一律在 refreshDerived 後、無陳舊窗口；LOW：state-0 容器卡同 #1 L1）
- #3 測試設計＋Anki 語意：❌ — 剔件 `!modeCards.get(id)` 漏 state-0 容器卡（store.js:194/323 跨 mode 承載卡真實存在，scheduler 歸 newQueue 但 cards 內 truthy → 誤計待複習；實測 hero 2→3）＋精確修法。Anki new≠due 剔 new 判定正確；四邊角（純 new/全 buried/cpd=0/simParams undefined）全過；雙向四象限有牙。

### R2（#3 席位複審 v1.2）
✅ PASS 放行：剔件修法與精確修法逐字一致、fixture a0 牙驗證（剝牙→hero 3→3 斷言撂倒）、7/7＋6/6 實跑、邊角 22/22、tzM 鏡像 store:495/501 同構、stats.due 宣稱已糾正＋C6-SR2 登 scope-requests。

## 修法（v1.2 最終）
dashboard.js 單檔：import getDueCards；render() 內 per-mode 引數鏡像 store.refreshDerived（含 capM/ratedM/tzM fallback 鏈）→ getDueCards 一次 → reviewDueSet（剔無卡 new ＋ state-0 容器卡）→ hero/statTile/renderDeckGrid(dueSet) 共用；廢除 isDue 平行實作。

## 驗證命令＋實際輸出
```
$ node --experimental-test-module-mocks tools/verify-c6-dashboard-due.mjs
C6 verify: 7/7 PASS ALL PASS                        (exit 0)

$ /tmp HEAD 副本 --expect-legacy
C6 verify [legacy 負控制]: 6/6 PASS ALL PASS        (hero=99 sentinel ≠ Σgrid=6)

牙驗證（R2）：剔件退回 if(!c) → 4/7 3 FAIL（hero 3）
回歸：c5 96/96 / a6 19/19 / vite build 722ms / node --check OK
```

## 範圍外/跟進
- C6-SR2（settings.js 第三套 due 平行實作）登 scope-requests.md
- O1：hero「新詞」tile＝全量庫存非今日額度（Anki 語意差異，範圍外）
- cardsPerDay `?? 80` vs store 無 fallback：不可達路徑 INFO

## 未完成事項
無。
