# Subagent log — F16（2026-08-29, PM3）

## 任務目標
F16：dead commands open_report / export_csv_data。open_report 半已由 F15
（1695638）承接；本單殲 export_csv_data（fn＋註冊 token，命令 42→41）＋
S-2 同步釘演化 verify-f15（EXPECTED_CMDS 42→41、T1f 計數、T2d 等式→單調性）。
JS wrapper（api.js:107-108）＋死 import（export.js:8）＝白名單外→F16-SR1。
計畫書 `_dev/notes/F16-fix-plan.md` v1.0。

## 審查（R1 單席——簡單 bug 降席：單一檔案／PM3 獨佔非共享／改動 7 行／非 FSRS 核心）
| 席 | 主攻 | 裁決 | 要點 |
|---|---|---|---|
| #1 驗證鑑識 | 獨立重跑＋攻擊 | ✅ | f16 16/16＋演化後 f15 18/18 實跑屬實；自有 node 掃描器復核 lib.rs（零 token×2、41 命令、近似名 dialog 8 token 未誤傷）；grep 冊忠實（動態形態全掃唯字面一處、Kotlin 零、build 產物殘影非源碼）；T3b SR 存續期釘 fail-closed 雙向鎖成立；必須項①F16-SR1 補登 scope-requests（commit 前工作區落實）②顯式路徑 add 嚴禁 -A |

## 驗證命令＋實際輸出
- 動工前 `node tools/verify-f16-csv-data.mjs` → 11P/5F（T1×4 徵狀紅＋T2c 雙態紅；
  T2a/b 負控制有牙；T3 全綠）
- 動工後同腳本 → **16 PASS / 0 FAIL**
- 演化後 `node tools/verify-f15-report-surface.mjs` → **18 PASS / 0 FAIL**（委員復跑確認）
- 回歸 verify-f14 38/38、verify-d17 32/32、npm build 722ms
- cargo check host＋aarch64-linux-android 雙綠；test 總數 42（f≤3 預存 sim fixture）

## 憲法②grep 冊摘要（本單核心義務）
export_csv_data 全部出處：lib.rs fn＋註冊（殲滅目標）／api.js:107-108 wrapper
（死，SR）／export.js:8 死 import（零呼叫，SR）／verify-g24 mock（測試鏡像非消費者）
／Kotlin 零／CLI 零／動態 invoke 形態零。審計「註冊但無呼叫」對本命令成立。

## 未完成事項
- F16-SR1（JS wrapper＋死 import 刪除，可比照 G24 mock 同單清理）登 scope-requests
  待裁示；落地日需同步修訂 verify-f16 T3b（雙向鎖成文）。
- 本 commit 帶指紋 5.2.10（版本新法機械閘，F14/F15/F6/D6 先例）。
