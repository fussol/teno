# Subagent log — F13（2026-08-28, PM3）

## 任務目標
F13：cambridge build_english_url/build_chinese_url 零編碼直插 → 片語（space
→ureq 壓 %20→Cambridge ?q= 搜尋 fallback 頁→scrape「解析失敗」）、`#`
（fragment 截斷）、`?`、`%` 查詢失敗。

## 白名單映射
任務書路徑 `src-tauri/src/cambridge/url.rs` 不存在 → 實為
`src-tauri/cambridge_scraper/src/url.rs`（行號 2-10/函式名吻合，登記
scope-requests 後照 intent 執行）。

## 修法（url.rs，核心 ~20 行）
`encode_slug`：split_ascii_whitespace→join("-")→trim_matches('-')→白名單
[A-Za-z0-9-._~] passthrough、其餘 UTF-8 pct（大寫 hex）。前提實測：
hyphen slug 直達條目頁（無 ?q=），%20 落 fallback。零新依賴（Cargo.toml
零觸碰）。既有 test mod 保留＋新增 4 測（6 測全綠）。

## 委員（R1，簡單 bug 降 1 席，✅ 有條件放行）
- 重跑驗證 30/30＋cargo 17/17＋負控制 pin 真性覆核。
- 自建攻擊向量全過：路徑穿越殲滅、%2520 防注入背書、CRLF/emoji/NUL/30k
  不炸、o'clock live 新舊位元組級相同（單字零回歸）。
- **[F-4] 重大採納**：第二根因實錘——Cambridge 新 tw-/superentry 模板條目頁
  零 `.entry-body`→scraper WordNotFound（hello 舊模板綠）＝F13 修 URL 層≠
  片語端到端全閉。處置：F13-SR2 登 scope-requests 待派（scraper 域）＋
  T2-3 端到端條目頁釘＋WARN。
- [F-6] §3.3 lowercase 論據勘誤（live Hello 落 ?q=）採納。
- [F-5] cli.mjs 髒檔屬 C4-SR1 他軌，commit 禁捲入。
- nit 不採納：全點 guard/256B cap（服务端 414 自有兜底，不動凍結範圍）。

## 裁決與處置
- R1 ✅（放行條件 F-7 已全數閉合：SR2 登案＋T2-3 釘＋§5/§3.3 勘誤 v1.1，
  代碼零變動）
- 驗證 v1.1：**31/31 ALL PASS**（T0 計數釘・T1 真碼向量機×15・T2 線上×3
  （含端到端釘，WARN 模板漂移如設計響起）・T3 負控制 pin 258458d×6・
  T4 結構釘×6）
- 回归：cargo 17/17、d16/d19/d7 綠、vite build 綠
- commit：url.rs＋verify 腳本＋計畫書＋本 log（scope-requests.md 共享檔含
  他軌四條未提交行，依precedent 不夾帶，登記以落盤為準）

## 範圍外（§6＋F13-SR2）
- 新模板 scraper 選擇器漂移（F13-SR2，端到端片語徵狀真正尾因，待派）
- 卷曲引號/全形標點/大小寫正規化（查詢品質域）
- tatoeba.rs（已用 url crate，本無此 bug）

## 教訓
- 「修了根因 A」不等於「徵狀閉環」：F13 修 URL 層時端到端另有模板漂移
  第二根因——閉環驗證要打到使用者可見層，URL 對≠ scrape 對（F-4 課）。
- 驗證腳本自己也有 bug：本 session 腳本連環三錯（workspace cargo cwd、
  舊碼提取重複定義、println! 手滑、負控制 regex 永假紅）——「腳本綠」
  之前先看腳本紅得有沒有道理，负控制紅要先分辨是 bug 重現還是腳本壞。
