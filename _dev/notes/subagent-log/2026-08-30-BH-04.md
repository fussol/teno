# subagent-log — 2026-08-30 BH-04

## 任務
tools.js 自實作 custom-select 對 document 累積 click listener 無 guard（BUGHUNT-TODO.md#BH-04）

## 實錘
- tools.js `_initCustomSelects()`（原 :349-364）每次 onMount 對 `document` 加一條常駐 click listener（:350），無去重 flag；renderPage 每次導航重跑 onMount → N 次累積 N 條，O(N×DOM) + 多次 toggle 競態。
- lib/custom-select.js:12-16 已有 G5 module 級 `_globalDocBound` 去重模式可仿。
- **首輪審查抓出作用域陷阱**：tools.js 幾乎整檔在 `export function onMount()`（:254）閉包內；初版把 flag 放閉包內 → 每次導航重生、guard 無效（假綠）。修正為 module 級 col0（:253、onMount 之外），guard 留 _initCustomSelects 內（閉包引用 module 變數）。

## 修法（最終）
- tools.js :253 前加 module 級 col0 `let _toolsCsBound = false;`（onMount 外）。
- `_initCustomSelects` 首行 `if (_toolsCsBound) return; _toolsCsBound = true;`。
- 首次 addEventListener 行為不變。

## 驗證
- tools.js 源碼釘：8 PASS / 0 FAIL（S1-S5 source，含 S5 col0 作用域 + P1-P3 jsdom 跨導航場景：修後 1 條 vs 未修 5 條）
- 負控制 stash：未修態 S1-S5 轉紅（有牙，捕作用域盲區）
- 回歸 verify-bh01 11/11、verify-bh02 19/19、vite build 937ms

## 審查
1 席 ✅（tools.js 頁面專屬低風險，GOV-BRIEF 降席規則）
- 首輪 ❌ 抓 flag 放錯作用域（閉包內每次導航重生→無效），源碼釘未驗作用域=假綠
- 修正後複審 ✅：flag 已 module 級 col0、guard 有效、首次行為不變、harness 8/8 + 負控制有牙

## 版本
v5.9.1（四檔一致；並行 OCR2-E′ 進場 major 5.9.0 後緊跟，維持只升不降）

## Commit
52abd18 (v5.9.1)