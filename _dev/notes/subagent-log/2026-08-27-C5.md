# Subagent Log — C5（2026-08-27 波次, PM1）

## 任務目標
審查 C5 修法：dashboard 待複習計數（scheduler.getDueCards）把 maxReviewsPerDay 打在 review+new 總和，session buildQueue 只 cap reviewQueue → 數字互相矛盾。修法：cap 移到 reviewQueue（對齊 session 權威行為）。3 委員唯讀審查。

## 裁決
- 委員 #1（修法正確性）：✅ PASS
  - 語意三點逐行對齊（learning 不 cap／review sort 後截最早 N／new 額度 = max(0, newPerDay−ratedNewToday) 兩端同式）
  - 獨立負控制：`git show HEAD` 還原舊碼 /tmp 副本 → 15/15 exit 0，T1 sch=9 ≠ ses=12 實錘
- 委員 #2（消費者完整性）：✅ PASS
  - getDueCards 消費者窮舉：store.js:486/493/499（→dueCount×3→dashboard/study/main badge）唯讀計數，無隱藏依賴舊總和 cap；cli.mjs 非消費者；simulator.js 舊語意（僅 review 計 cap）反而與新碼收斂
  - 順帶修掉舊碼 newCount 虛報缺陷（cap 切掉 new 但仍計入）
  - Anki 官方 docs 實查：計畫書「Anki 含 learning 計入」陳述準確、偏差誠實登記範圍外
- 委員 #3（測試設計）：✅ PASS
  - fixture 區分力反向實測（HEAD 舊碼跑正向模式恰好 T1/T3 兩條紅）→ 斷言有牙
  - new 只比數量的妥協：git log -L 實錘 A7 shuffle 自始存在，非本 fix 引入
  - 四邊角親跑一致（cap<learning／ratedNew 超額／cap==review 邊界／畸形 due）
  - 發現既有 LOW：混偏移 due 排序器不一致（生產 canonical Z 不可達）、Invalid Date 死碼 — 範圍外登記

## 驗證命令＋實際輸出
```
$ node tools/verify-c5-review-cap.mjs
C5 verify: 96/96 PASS ALL PASS                       (exit 0)

$ node /tmp副本/tools/verify-c5-review-cap.mjs --expect-legacy   # HEAD 舊碼還原
C5 verify [legacy 負控制]: 23/23 PASS ALL PASS        (exit 0)

回歸：a6-sort 19 pass 0 fail / a10-due-anchor ALL PASS / c4 ALL PASS /
node --check scheduler.js OK / vite build 735ms
```

## 審查後補強
T7（cap<learning 含 new，兩端 8）、T8（cap==review 數邊界不截，兩端 17）、T9（ratedNew 超額 new=0 不為負，兩端 9）— 修法/負控制雙模式全過。

## 未完成事項
無。範圍外觀察（混偏移排序器、Invalid Date 死碼、Anki learning-in-review-limit 語意）已登計畫書審查紀錄節。
