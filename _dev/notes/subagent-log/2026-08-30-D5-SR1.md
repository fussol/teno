# D5-SR1 審查紀錄 — 2026-08-30

## 任務目標
backupDb 未先 WAL checkpoint → 備份缺最近複習（資料完整性最高優先）。

## 裁決
✅ 通過（1 輪 1 人次委員，符合簡單 bug 降 1 名規則）。

## 修法
`src/lib/api.js:2` 新增 `import { checkpoint } from './db.js'`；`backupDb`(:已改)
改 async 且 `invoke('backup_db')` 前 `await checkpoint()`。單點封頂。

## 委員發現（採納，升計畫書 v1.1）
1. 受害敘述勘誤：HEAD 樹上 4 呼叫點實皆有意義相鄰 checkpoint，真正缺陷=API 契約缺口＋backup-scheduler TOCTOU 殘窗。
2. 版本欄位撞號：樹被並行首相搶到 5.8.1 → D5 結案 5.8.2。
3. tools/cli.mjs:180 鏡像 backupDb 無顯式 checkpoint（CLI 走 DatabaseSync 連線 close 自動 checkpoint，暴露面低）→ 登記觀望，非本任務範圍。

## 驗證命令＋實際輸出
- 委員實跑：`node --experimental-test-module-mocks tools/verify-d5-checkpoint.mjs` → ALL PASS（正向）；/tmp 鏡像負控制 bug 態 4 FAIL。
- 首相複跑：ALL PASS。回歸：D17 32/32、D6 17/17、g30-reentrant 6/6、g24 ALL PASS、verify-version-sync PASS。

## 未完成事項
- cli.mjs:180 backupDb 鏡像無 checkpoint：CLI 用 Node DatabaseSync，close 時自動 checkpoint，暴露面低；比照 F14-SR1 模式可登記 scope-requests（非本任務白名單，不逕改）。

## Commit
`1655186` (v5.8.2)

## 產出文件
- `_dev/notes/D5-SR1-fix-plan.md` (v1.1)
- `tools/verify-d5-checkpoint.mjs`