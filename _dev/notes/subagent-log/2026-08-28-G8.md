# 2026-08-28 G8 — 首相1 波次 subagent 紀錄

## 任務目標
G8：`src/pages/simulator.js` `_simResults` 無上限＋每筆 entry `params:{...req}`
全詞表快照 → 記憶體線性成長。完整循環：計畫書→審查→動工→驗證→commit→log。

## 前置：白名單障礙解除
- G8-SR1/G26-SR1 原「待裁示」；本輪總統再派 PM1「從佇列第一顆逐一完成」＝唯一殘題，
  依任務書註（「G26 若需動 simulator.js 以外檔 → scope-requests」自明本體屬 PM1）
  ＋佇列行號唯 pages 吻合，判筆誤放行，scope-requests.md 處置欄更新。
- R1#2 校正：v1.0「全庫 simulator.js 唯 pages 一份」有誤——`src/core/simulator.js`
  實在（store.js:528 import）但無病灶徵狀；措辭已改「病灶徵狀唯 pages 一份」。

## 審查紀錄
### R1（3 委員，全 ❌ 有條件——骨幹無反對，攔點全在腳本/登記面）
- #1 修法正確性 ❌：算術周遊（基準實作全綠）、零消費者覆核屬實、T3/T4 三變體牙精確；
  C1 T2e 不可滿足（pushSimEntry 本身必須 push）／C2 mock 指紋計失敗輪→T6 帳本必紅／
  C3 快照釘可被 `params:req` 別名規避／C4 **:598 切模式不重置選態 REPRO 實錘**
  （HEAD 與 v1.0 修法皆中，新納入修法）／C5 #N 飽和登記。
- #2 消費者窮舉 ❌：零消費者成立且更廣（模組私有無 export、CLI/bot 零碰、addSimRun
  吃活變數）；發現 2 #N 永停 #20、發現 3 標籤滑動重排、發現 6 T2e 自相矛盾、
  發現 7「唯 pages 一份」事實錯誤——四必須項全採納。
- #3 變異牙檢 ❌：V1-V6 全斃；**D1 鐵證 v1.0「修法後 ALL PASS」從未實跑**（誠實登記）；
  盲區 V7（:333 漂移）/V9（守衛無牙）/V10（別名快照）→ FIX-4/5/3；V8（meta 零依賴）
  如實登記不補牙。
### R2（2 委員，全 ✅ 放行）
- #1（修復點覆核）✅：負控制 7/7；HEAD 20 FAIL 全對位（T2g 釘緊 ≥4 多一紅屬預期）；
  基準實裝 **30/30 ALL PASS**（無不可滿足釘）；登記逐條行號對位；T2f 脆弱性單向
  fail-closed 可接受（選配括號計數加固未採——處方已釘 function 簽名）。
- #2（變異矩陣重殺）✅：V7/V9/V10 三盲區閉合（T9b/T10a+b/T2b 各一斃）；V1/V2/V3/V4/
  V6 重確認仍斃；新探針 V11a/b（死碼重置/const 遮蔽）斃於 T8 動態腿（T2g 遮蔽規避
  由行為腿兜死——雙層結構登記）；V12 off-by-one 斃於 T10；V14 回魂快照斃於 T2a/b；
  V13（先騰位後 push）判定語意等價變體，登記免追殺。基準連跑 3/3 零非確定性。

## 實施（修法五點，凍結 v1.1 §3 逐字）
1. entry 砍 `params:{...req}`，留 totalWords＋新增 meta 標量（{mode,days,seed}）。
2. `SIM_HISTORY_MAX = 20`＋`pushSimEntry(entry)`（push→while shift→選態三分支）。
3. 成功/錯誤條目雙軌同走 pushSimEntry。
4. 切模式段補 `_simSelectedIndex = null`（R1#1 C4 同族缺陷，REPRO 實錘納入）。
5. 歷史鈕 click 邊界守衛 `Number.isInteger(i) && 0<=i<len`。

## 驗證（實測輸出）
- `tools/verify-g8-simhistory-cap.mjs` v1.1 30 釘：修法後 **ALL PASS ×3 連跑**；
  HEAD `/tmp/g8nc` 負控制 **ALL PASS**（7 釘，含 T8L 切模式殘留選態辨證腿）。
- 修前（未實施）非 legacy 態：恰 20 FAIL 紅集=辨證全集（T2 靜態群/T1b/T1c/T5b/
  T3×3/T4×3/T10×2/T8×2/T2g）。
- 變異矩陣：13 非等價變體全斃＋1 等價登記（R2#2 實跑）。
- 回歸：c7/c8/c9/c10/g4b EXIT=0；`npm run build` 812ms；`node --check` 綠。

## 語意變化誠實登記（計畫書 §5）
- 第 21 筆起最舊筆無痕消失；#N 計數飽和恆 #20（不加計數器）；歷史鈕標籤滑動重排；
  選中被擠出→跳顯示最新；切模式重置選態（UI 不可見，僅消 stale 窗口）。

## 範圍外（§6）
G26（下一顆）／btn-primary 恒亮最新鈕怪癖（G26/UX 域掛名）／`src/core/simulator.js`
（同名無病灶不碰）／歷史持久化、per-mode 保留（新特性無授權）／reviewLog 全量記憶體（store 域）。

## 未完成事項
無——本顆閉合。
