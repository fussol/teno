# 2026-08-30 SR3-F6-SR1 修復紀錄

## 任務目標
堵 Android D2D(device-to-device) 傳輸（teno.db SQLite + Drive token 換機外洩）。allowBackup=false 官方明示 targetSdk31+ 不停 D2D。

## 病灶 / 一手查證
- allowBackup=false 只停 cloud，不停 D2D（官方 autobackup＋behavior-changes-12 明示）。
- 關鍵決定 recipes：官方「no rules for a mode = fully enabled」→ **空/缺 section 堵不住**，採顯式 exclude 根域。
- 一手複驗 domain 清單：官方 schema 9 個（file/database/sharedpref/external/root＋device_file/device_database/device_sharedpref/device_root），非「deviceProtectedStorage」（委員記憶有誤，已用官方正確域）。

## 修法
- 新增 res/xml/data_extraction_rules.xml（cloud-backup＋device-transfer 兩 section 各 9 domain exclude path="."）。
- AndroidManifest.xml `<application>` 加 android:dataExtractionRules="@xml/data_extraction_rules"（allowBackup=false 旁）。
- verify-f6-manifest.mjs：既有 8 test 不動，追加 T0SR1（基準寫死 504049e 三段）＋T6（屬性錨 application＋9-domain 集等）＋T7（compileSdk≥31）＋NC3（刪 section）＋NC4（空 section）。

## 審查
3 委員全 ✅：
- 委員1（安全）✅ 方向零變更，4 精確修法（落點陳述/compileSdk釘/NC空section/minSdk句）。
- 委員2（harness）需修正後過審：T0 寫死非浮動 HEAD、NC 補空 section mutant、正向錨 application、domain 集合相等、strip 註解防「dataExtractionRules」註解字樣誤判。
- 委員3（整合）✅ 過審（不擋 merge），建議 9-domain 補齊。

## 驗證（實際輸出）
- verify-f6-manifest.mjs：**13/13 PASS**（POST 注入，含 9-domain T6、T0SR1 寫死、NC3/NC4 兩回歸形態）。
- processArmDebugMainManifest＋compileArmDebugKotlin：**BUILD SUCCESSFUL**（AAPT link 9-domain 合法）。
- vite build ✓ 1.29s。
- verify-f10-piper-import.mjs：30/30（不互撞回歸）。

## Commit
- `73e3a7f` fix: F6-SR1 …（5.8.7）

## 未完成事項 / backlog
- Android ≤11（minSdk=24）與 OEM 私有通道非官方保證（根治需遷移 no_backup，屬後續 hardening）。
- 既有碼 copyUriToCache input stream 洩漏（F10 發現，見 F10 log backlog）。