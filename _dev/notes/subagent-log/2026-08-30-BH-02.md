# subagent-log — 2026-08-30 BH-02

## 任務
deleteWord memory 端清理不全（reviewLog/examHistory/六 Set/三 buriedAt/examples 殘留）→ 保留率溢算（BUGHUNT-TODO.md#BH-02，動保留率，特嚴謹）

## 實錘
- store.js deleteWord():1697-1705 只清 words/cards/cardsMc/cardsSpell。
- computeRetention（scheduler.js:419）：30 天窗內 state.reviewLog 的 total/correct，rating>=3 算 correct → 已刪字作答被算入保留率。
- DB 端 db.deleteWord():217-234 已清 review_log/exam_history → memory/DB 分歧。
- reviewLog push 用 wordId(:839)；examHistory push 用 word=wordId(:2004)；六 Set 初始 new Set();(:114-119)；三 buriedAt 初始 {}(:120-122)；examples 是 Map(:123)。

## 修法
- 新增 module 級 helper `deleteWordFromMemory(id)`（:574-580，createStore 內、actions 外）：清 reviewLog/examHistory filter、六 Set state[k].delete(id)、三 buriedAt if(state[k]) delete state[k][id]、examples.delete(id)。
- deleteWord() cardsSpell.delete 後呼叫 deleteWordFromMemory(id)（:1712），接 refreshDerived()（保留率立即重算）。

## 驗證
- node --check OK
- tools/verify-bh02.mjs：19 PASS / 0 FAIL（源碼釘+保留率實測+語意重放+負控制）
- 保留率層：buggy(含已刪字) rate=0.75(3/4) vs fixed(剔除) rate=1.0(2/2)，兩態數字不同 → harness 真抓到溢算
- 負控制 stash：未修態源碼釘 S1-S7 轉紅（有牙）；回歸 verify-bh01 11/11、verify-d20 10/10、vite build 960ms

## 審查
3 委員 ✅✅✅（串行，免費端點 8req/min 429 打爆 3 並行 burst 後改串行+收斂指令成功）
- 委員1（完整源碼）：helper 清理齊全欄位形狀對、呼叫位置正確、undo C1 無 resurrect、負控制咬得住 ✅
- 委員2（保留率視角）：刪字鏈 deleteWord→helper 剔 reviewLog→refreshDerived 重算 閉環、real computeRetention 兩態數字不同 ✅
- 委員3（負面 edge）：Set 恆 new Set 無 TypeError、examHistory 純 word_id 世代無漏清、db 失敗屬既有樂觀更新 pattern、與 BH-01 無執行衝突 ✅

## 技術債追蹤（委員 E1/E2，定案不擴張）
六 Set/buriedAt/examples 的 DB settings 在 deleteWord 端無 setSetting 持久化 → 刪字後重載六 Set 孤兒 id 復活。保留率/stats 不受影響（reviewLog/examHistory 走專屬 table 跨重載乾淨；孤兒 id 不匹配活卡無害）。deleteWord 誕生即無寫 settings，屬既有缺口非本修法回歸。任務範圍封閉（memory 端清理+保留率已達），BH-02 定案不做；建議 BH-03 或日後 loadAll 孤兒過濾一次解。

## 版本
v5.8.14（三指紋 + Cargo.lock）

## Commit
7a87ff0 (v5.8.14)