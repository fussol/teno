# F5 R2 複核報告（單席 c1）— 2026-08-28

## 任務與紀律
- 角色：R2 單席複核委員（F17/F4 判例：R1#2 未明示逕判 ✅ → 單席複核）。
- 範圍（僅兩項）：(a) R1 處方落實檢查；(b) 雙態驗證重跑＋攻擊變體重現。
- 紀律：repo 唯讀，未改任何倉庫檔；實驗突變體全寫 /tmp/f5-r2/。基線 HEAD=ace9306，
  工作區 IconPlugin.kt 為未 commit 的 F5 修法改動（代碼已凍結不動）。

## 裁決：✅

---

## (a) R1 處方落實——逐項核對 v1.1 版本紀錄聲稱 vs 實際 code/腳本

| # | 聲稱 | 實核 | 判定 |
|---|---|---|---|
| 必須① | T3 guard 窗口 200＋真 guard 形釘 | 腳本 `verify-f5-icon-cleanup.mjs:82` disable 匹配窗 `{0,200}`；`:86-88` guard 窗 `slice(at-200, at)` ＋ `assert.match(guard, /st\s*==\s*PackageManager\.COMPONENT_ENABLED_STATE_ENABLED/)`。keep-else 分支的 ENABLED 比較在 200 窗外，無法再造假 guard | ✅ 落實 |
| 必須② | T2 load keep running 分支釘＋三元定義釘 | `:73` `runningIsAliasActive\s*->\s*runningComp` 釘＋`:75` 定義三元釘（`runningComp != null … isComponentActive(runningComp`，250 窗內）；對應代碼 IconPlugin.kt:156 | ✅ 落實 |
| 必須③ | T2 堵否定式閘變體 | `:64` `if (\s*!\s*resolved(?:IsAlias|Active)\s*\)`、`:65` `.not()` 變體、`:67-70` runningComp 定義→清理迴圈間 `\breturn\b` 結構釘（含順序錨 `iLoop > iRun`） | ✅ 落實 |
| 必須④ | SPEC S3 keep=Icon1＋decide() live 模型 | `:122-123` S3『Icon1 被殺自癒』keep: 'Icon1'；`:132` 自癒 mutate（`st.Icon1 = E`）先於 keep 判定（`:133-136`），註解 `:120-121` 誠實標註表碼矛盾修正理由 | ✅ 落實 |
| 代碼採納 | runningComp 支路 active 查＋keep resolvedActive active 查 | getCurrentIcon fallback 首支 `name == runningComp && isComponentActive(...)`（IconPlugin.kt:200-201）；load `resolvedActive = resolvedIsAlias && resolvedName != null && isComponentActive(...)`（:153-154）→ `resolvedActive -> resolvedName`（:157） | ✅ 落實 |
| NC1/NC2 真突變 | 注入 d/c 攻擊原樣→斷言釘必命中 | NC1（腳本:148-158）對 LOAD 真注入 `if (!resolvedIsAlias) { super.load(webView); return }` 後跑 T2 偵測正則；NC2（:160-173）對整檔真刪 `runningIsAliasActive -> runningComp` 分支＋突變體結構完整性斷言（清理迴圈仍在＝非亂碼）。**且本席以獨立端到端重現佐證非自證遊戲**：注入同文本的 /tmp 變體真讓 T2 紅（見 (b)2），即 NC 的偵測面與真實紅域一致。原恆真 NC（early-skip＋近永真式）確已滅 | ✅ 落實 |

其他對照：`DEFAULT_ALIAS` 常量（:71）＋註解釘 manifest 對照責任；`isComponentActive`（:74-76）＝
`ENABLED ∨ (DEFAULT ∧ DEFAULT_ALIAS)`；enabledList 消費（:122）；label 條件化
`default(=active)/default(=inactive)`（:118）——與 §3 修法一致。

## (b) 驗證重跑＋攻擊重現

### 1. 雙態實跑
```
PRE ：cd ~/teno && node tools/verify-f5-icon-cleanup.mjs
  → T0✔ T1✘ T2✘ T3✔ T4✘ T5✘ T6✔ NC1✔ NC2✔，exit 1
  （預期「T0/T3/T6/NC 綠、T1/T2/T4/T5 紅、exit 1」——符合）
POST：SRC=src-tauri/.../IconPlugin.kt node tools/verify-f5-icon-cleanup.mjs
  → 9/9 全綠，exit 0（符合）
```

### 2. 四攻擊重現（突變體由 POST 源生成於 /tmp/f5-r2/attack-{d,c,b,a}.kt）
| 攻擊 | 注入 | 預期 | 實測紅域 | 觸發斷言 | 收斂 |
|---|---|---|---|---|---|
| d | runningComp 定義前插 `if (!resolvedIsAlias) { super.load(webView); return }` | T2 紅 | **僅 T2 紅**（8 綠） | `腳本:64 「否定式閘變體復活」`——正是設計命中點 | ✅ 單點 |
| c | 刪 `runningIsAliasActive -> runningComp` 分支 | T2 紅 | **僅 T2 紅** | `腳本:73 「load keep 缺 running 分支」`——正是設計命中點 | ✅ 單點 |
| b | 清理 disable 前置 guard 改 `st != DISABLED` | T3 紅 | **僅 T3 紅** | guard 窗無 `st == ENABLED` | ✅ 單點 |
| a | enabledList 改回 `state != DISABLED` 一刀切 | T5 紅 | **僅 T5 紅** | `enabledList 未改用 isComponentActive` | ✅ 單點 |

（各變體 exit=1；d/c 錯誤訊息经 grep 驗證落在預期 assert，非順帶誤傷。）

### 3. 編譯閘
```
cd ~/teno/src-tauri/gen/android && JAVA_HOME=$HOME/jdk21 ANDROID_HOME=$HOME/Android/Sdk \
  ./gradlew :app:compileArmDebugKotlin --offline --rerun-tasks
→ BUILD SUCCESSFUL，66/66 tasks executed（真編譯，非 up-to-date 虛綠）
```

### 4. 本席自發新攻擊（無意回歸/偷工威脅模型）
| 攻擊 | 注入 | 實測 | 評估 |
|---|---|---|---|
| e | keep-else 分支 `component(n) == ENABLED` 偷翻成 `!= DISABLED`（PRE 慣用法回潮路徑） | **逃網（9/9 綠）** | 語意弱化：keep 可落 DEFAULT-inactive alias（如 Icon2 DEFAULT）→ 誤 disable 真正 runtime-ENABLED 的 alias，使用者選的圖示無聲還原。屬「keep=首個 runtime ENABLED」（計畫 §3.3）之未釘定義面；非 R1 四必須項範圍 |
| g | getCurrentIcon runningComp 支路偷簡 `&& isComponentActive(...)`（只剩 `name == runningComp`） | **逃網（9/9 綠）** | 恰為 v1.1【建議3·代碼採納】之強化可被無聲拆除而驗證不響；T4:100 末段釘是 `isComponentActive|"original"` 或式，"original" 常駐使其近恆真 |

**新攻擊定級（不擋裁決）**：計畫書/R1 四必須項均未聲稱釘住此二處；e 的最壞後果與已登記的
§5『受限自癒邊界』同域（圖示非資料、可恢復），g 是 v1.1 後加強化的回歸面。按 F4 判例
（R2 僅驗處方落實＋列名攻擊收斂）屬登記項非缺口。建議 v1.2 順手補（見下）。

## 裁決理由
1. (a) 六項全部落實（腳本行號＋代碼行號逐點對上，見表）。
2. (b) PRE/POST 雙態與預期逐字相符；四列名攻擊全部收斂至預期**單點紅域**且觸發斷言與設計意圖一致；
   `--rerun-tasks` 66/66 真編譯綠。
3. 新攻擊 e/g 逃網定級為非阻斷登記項（威脅模型內、但超出本輪聲稱的釘陣範圍），附修法建議。
→ **✅**

## 建議（v1.2 順手修，非阻斷）
- 堵 e：T2 加 `assert.match(LOAD, /firstOrNull \{ \(_, n\) ->\s*pm\.getComponentEnabledSetting\(component\(n\)\)\s*==\s*PackageManager\.COMPONENT_ENABLED_STATE_ENABLED/)`（keep-else 定義形釘，禁 `!= DISABLED` 變體）。
- 堵 g：T4 把 :100 或式釘拆細——對 runningComp 支路加 `assert.match(GCI, /name == runningComp &&[\s\S]{0,120}isComponentActive\(/)`。
- NC 正則與 T2 正則為複製品（漂移風險）：可提成共用 const（如 `NEG_GATE_RE`）供 T2 與 NC1 同讀。

## 未完成/範圍外事項
- e/g 兩針未補（屬建議，代碼凍結紀律下本席未動）。
- R1 已登記不修項照舊：受限自癒邊界、~40 次 binder 讀快照化、launcher 第三方快取延遲、
  settings.js:823（JS 域另單）。
- 真機 e2e 仍不可行（Kotlin/Android），以靜態釘＋規格決策表＋真編譯閘代替（計畫 §4 既定立場）。

## 命令紀錄摘要
- `git status`/`git log -3`：HEAD=ace9306，IconPlugin.kt 為髒改動（預設 F5 工作態）。
- `git diff HEAD -- …/IconPlugin.kt`：改動＝DEFAULT_ALIAS＋isComponentActive＋dump 條件化
  ＋清理段掃描重構（keep 三元）＋getCurrentIcon fallback 四段式，與計畫 §3 一致，未見範圍外改動。
- 雙態跑／四攻擊跑／兩新攻擊跑／gradle --rerun-tasks：輸出見上文各節。
- 產出物：僅本報告；突變體滯留 /tmp/f5-r2/（attack.mjs＋attack-{a,b,c,d,e,g}.kt），不進倉庫。
