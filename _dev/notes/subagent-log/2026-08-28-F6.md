# F6 subagent 紀錄（整合）— 2026-08-28

## 任務目標
F6 三部分：①MANAGE_EXTERNAL_STORAGE 零消費者上架風險 ②「release cleartext 擋 localhost LLM」
③無 backup 排除（DB 被雲端還原）。檔案：AndroidManifest.xml＋build.gradle.kts（後者判定零改動）。
基線 ee93ffa（動工時 HEAD 已並行推進至 19edaf9，manifest 無他軌漂移，預後結構對拍逐字一致）。

## 審查歷程
- R1（3 席）：#1 代碼事實 ✅（附 E1 必改：MediaStore$Downloads 為 API29+ 類，①b 原論據不實
  →v1.1 論據改寫＋O3 另單登記；獨立穷举含 tauri-plugin-dialog 2.7.1 源碼實查零誤傷）
  ／#2 官方語意 ✅（D2D 殘洞引文兩頁逐字存在→F6-SR1 恰當；Play AFA「不需要就必刪」；
  cleartext 管轄清單不含 native socket；Tauri PathPlugin.kt getConfigDir→dataDir 一手佐證）
  ／#3 驗證鑑識 ❌（S1 守門 grep 自爆釘：腳本含字面符號＋pathspec 含 tools/→合併起 T1 恒紅，
  clone 模擬 rc=0 實證；D1 錯置雙盲：allowBackup 錯置 activity 靜態釘+AAPT2 雙綠，merged 產物實證）。
- v1.1 當輪修畢（僅動腳本＋文字，manifest/gradle 未動）：S1 拆串＋:(exclude) 雙保險；
  D1 錨定 `<application\b[^>]*`；E1 ①b/T2 論據改寫。
- R2（1 席 c1，F4/F5 判例複核）：**✅ 明示無需 R3**。處方 9/9 落檔；攻擊矩陣 11/11 收斂
  （含 c1 錯置 activity→T3 單紅治癒、S1 clone 合併態 8/8 綠自爆除死、自發三新攻擊全 fail-closed）；
  AAPT2 processArmDebugMainManifest rc=0。
共 2 輪 4 人次。

## 驗證命令＋實際輸出（動工後）
```
$ SRC_MF=<repo manifest> node tools/verify-f6-manifest.mjs → 8/8 綠 exit 0
$ node tools/verify-f6-manifest.mjs（HEAD 釘基線）→ 6/2（T1/T3 紅＝PRE bug 事實）
$ gradle :app:processArmDebugMainManifest :app:compileArmDebugKotlin --offline → EXIT=0
$ 回歸 f4/f5(PRE+POST)/tts-contract PASS＋build 719ms
```

## 未完成/範圍外
- F6-SR1：dataExtractionRules 堵 D2D（res/xml 白名單外，scope-requests 待改派）。
- O3：API24-28 匯出必斷（TtsPlugin NoClassDefFoundError，TTS 域另單，PM5 可承接）。
- fetch_llm 無 localhost 白名單（lib.rs 域，R1#2 發現登記）。
- usesCleartextTraffic targetSdk38 deprecation 預警（波尾集中處理）。
- 版本指紋未帶：package.json/tauri.conf.json 白名單外（G9-SR1/F14-SR2 波尾集中先例）。
- 真機驗證不可行：manifest 層以靜態釘＋AAPT2 merge 產物＋官方一手文檔代替。
