# 2026-08-30 SR3-F10-SR1 修復紀錄

## 任務目標
修 Android 端 import_piper_model_dialog 匯入 Piper 模型必敗（雙層病灶）。

## 病灶
1. Rust：FilePath::Url（SAF content://）as_path()=None → Err("無效路徑")。
2. Kotlin：copyUriToCache 硬編碼落地 import.db，piper 鏈以檔名建語音目錄 → 仍 100% 敗。
3. 附帶地雷：import.db 固定名與 db 匯入共用 cacheDir 互相覆寫。

## 修法
- lib.rs：套用封存 F10-pending.patch（git apply --3way）→ cfg(android) match + copy_uri_to_cache 委派 + .onnx 守門 + as_path 殲滅 + import_piper_path_tests。
- TtsPlugin.kt copyUriToCache：ContentResolver.query(DISPLAY_NAME) 取真實檔名 + sanitize + 兜底 import.db。
- 新增 tools/verify-f10-piper-import.mjs（含 Rust T0-T4 + Kotlin T5-T6 負控制）。

## 審查
3 委員（Rust / Kotlin / harness 設計）全 ✅，1 輪。
- 委員1 ✅（Rust 改裝正確、獨立復跑 29/29、lib.rs 恰 2 hunk、負控制基準寫死恰當）。
- 委員2 ✅（DISPLAY_NAME/sanitize/cacheDir 交錯治理全正確），附註：既有碼 input stream 洩漏（非本次引入）→ backlog。
- 委員3 條件過審（必修 T6 反換閉環）→ 已修（T6 反換演示 + replace 回呼消 $ 地雷 + 註解補正），復跑 30/30。

## 驗證（實際輸出）
- tools/verify-f10-piper-import.mjs：**30 PASS / 0 FAIL**（cargo test piper 全綠含 3 機制釘+13 回歸、host+aarch64-linux-android 交叉 cargo check 綠、T2/T6 負控制反換閉環）。
- compileArmDebugKotlin：**BUILD SUCCESSFUL**（僅 2 既有 warning）。
- npx vite build：✓ 945ms。
- 回歸：verify-f9-piper-url 18 PASS、verify-g9-tts-fallback pass 13/fail 0、verify-tts-contract-loader EXIT 0。

## Commit
- `7a05183` fix: F10-SR1 …（v5.7.0 基線已在波次中被並行首相推進至 5.8.3，本顆升 5.8.4）

## 未完成事項 / backlog
- 既有碼 copyUriToCache input stream 洩漏（非本次引入）：另開小 commit 以 input.use{} 收口（委員2 建議）。
- json 兄弟檔在 Android cacheDir 必不存在，json 複製段 Android 形同跳過（既有 pick_file 單選限制，backlog）。
- Android 運行時真機驗證需用戶真機（全波次共有）。