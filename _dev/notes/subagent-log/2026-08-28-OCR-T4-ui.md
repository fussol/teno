# PM-OCR2 · T4 `feat(ocr): ui` — tools.js OCR 區塊＋引擎選單（2026-08-28）

## 任務目標
§4/§6.3：tools.js OCR 區塊 HTML（Generate Forms 與 Cambridge 之間）＋
`#ocrEngineSelect`（form-input，選項由 registry 動態生成，change→
setSetting('ocr_engine')，原生 .value 讀值）＋ camera 圖標（§6.1）＋
上傳/辨識/候選勾選/全選/入庫確認事件綁定。

## 交付
- `src/lib/svg.js`：lucide camera import ＋ icons.camera 映射。
- `src/pages/tools.js`：
  - import listEngines（registry 靜態可見，選項零寫死；標籤表屬 UI 層）
  - OCR 區塊 HTML（計畫 §4 骨架原樣，select 選項改動態生成）
  - `initOcrBlock()`：getSetting('ocr_engine') 還原（選項存在才套選值）、
    change→setSetting 持久化＋`[ocr] engine set:` 記錄、
    上傳 change→runOcr（getActiveEngine 現讀→recognize→§5 正則過濾→去重保序
    →勾選清單預全選）、busy 態 capBtn.disabled＋防重入、全選鈕、
    confirm→s.actions.importOcrText（T5 前優雅 catch toast）。
- `tools/verify-ocr-ui.mjs`：靜態斷言 20 項＋NC1/NC2 雙負控制＋反換釘 3 項。

## 四層內測門
1. **靜態**：node --check tools.js/svg.js/verify ✅；`npm run build` 779ms 綠
2. **驗證腳本** `tools/verify-ocr-ui.mjs` **26/26 ALL PASS**
   （U1 registry 動態生成＋寫死偵測／U2 form-input＋原生 .value＋零 .cs-t／
   U3 持久化雙向／U4 camera 圖標三釘／U5 區塊位置／U6 零 emoji／
   U7 token 正則逐字元＝§5／U8 九元素＋busy 防重入／U9 registry 標籤覆蓋；
   NC1 改寫死 option→U1 紅＋反換釘 U2 綠；NC2 剝 setSetting→U3b 紅＋U3a/c 綠）
   回歸 verify-ocr-engine 12/12＋verify-ocr-assets 16/16 全綠
3. **Browser 內測（dev:5199 實跑，非 mock）**：
   - OCR 區塊 render：camera svg 標題、九元素齊備、custom-select 轉換
     （原生 select display:none＋cs-wrap），選項 tesseract/paddle 動態生成
   - **引擎切換**：cs trigger→選 PaddleOCR→原生 `sel.value==='paddle'`＋
     trigger label 更新（change re-dispatch 鏈路實測通）
   - **真辨識**：DataTransfer 注入 canvas 字卡圖（journey/example/apple）→
     候補三詞全勾選 render、btn busy -disable 恢復 ✅
   - **busy 同步探針**：dispatch 後即時 capBtn.disabled=true＋「辨識中...」
   - **負控制 A**（§7）：全黑圖 → 「未偵測到有效單字」提示、零候選、不拋
   - **全選鈕**：取消 1 勾→點全選→回 2/2 ✅
   - **confirm 接線（T4 態 PRE 紅）**：toast「入庫失敗：s.actions.importOcrText
     is not a function」= 優雅 catch 邊界正確，T5 store 落地後轉綠
   - console：js_errors 0（全程含重載；getSetting 無 DB 環境靜默回退與
     T3 前例一致，DB 持久化驗證歸 T5 真 DB 路徑＋T6）
4. **證據**：本檔＋verify-ocr-ui.mjs

## 事故登記（誠實）
- 本 session 工具傳輸層數度損毀含 `{...}` 字面量的 tool-call 參數
  （`{1,30}`→`...`），一度寫壞 tools.js 第 9 行正則與驗證腳本 U7 斷言。
  U7 斷言如實抓到源碼損毀（雙態驗證價值實錘）；od 位元組級複查確認
  最終落盤正確。教訓：脆弱字面量斷言用分段 join 構造＋位元組級驗證。
- 驗證腳本 U2c 初版被自身註解內 `.cs-t` 字面打紅——斷言錨點避諱自註解。

## 範圍外／登記
- 引擎選單持久化「重載後保持」需真 DB（Tauri），dev browser 無 DB 僅測
  回退態；T6 全流程內測以 node 真 DB 腳本補硬（settings 表 ocr_engine 往返）。
- 候選清單不顯示 block 信心分數/相似度警告（§9 P3 範圍外，僅整體訊息）。

## commit
`feat(ocr): ui` — src/lib/svg.js、src/pages/tools.js、
tools/verify-ocr-ui.mjs、本 log
