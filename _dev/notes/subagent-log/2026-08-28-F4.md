# Subagent 紀錄 — F4（2026-08-28，PM5 軌）

## 任務目標
F4（audit 2026-08-13:107）：IconPlugin.kt ①restart alarm requestCode 0 無 cancel
②「finish 在 try 外」③停用 running alias 的 disabled-package race。
修法：統一 PI 構造器＋全 alias 掃蕩 cancel＋ELAPSED_REALTIME_WAKEUP 單調鐘＋
catch 回滾＋出口誠實化（reject）。

## 委員配置與輪次
R1 三席（多部件重構）→ ✅❌✅；#2 四必須項（全腳本級）修畢且其明示「修畢升版可逕改判✅」
→ R2 單席複核（F17/G9 判例）→ ✅。合計 2 輪 4 人次。全程唯讀、實驗限 /tmp。

## 裁決摘要

### R1 #1（代碼事實＋Android API 語義）：✅ 零必須項
- PI 匹配語義論證與官方文檔逐字對齊（filterEquals 不比 flags；component 不同＝不同 PI
  →「連環切累積 alarm」實錘前提成立）。
- 掃蕩 cancel 完備性：目標空間閉合（排程只發生於 aliases 集內，掃得回來）；
  NO_CREATE-null（已觸發）場合無害；PI 注冊於 system_server 跨進程存活。
- ELAPSED_REALTIME_WAKEUP×setAndAllowWhileIdle 組合合法（文檔僅收 RTC_WAKEUP/
  ELAPSED_REALTIME_WAKEUP）；non-exact 家族不受 SCHEDULE_EXACT_ALARM 約束。
- 回滾狀態機逐 catch 時點推演無更糟中間態；@Command 主線程考證（Tauri mobile.rs→
  wry MainPipe→tao looper 實證鏈）→ var restartScheduled 零線程問題。
- 建議·高採納：探測 flags 補 FLAG_IMMUTABLE；建議2 採納：回滾出口改 reject
  （實錘 settings.js:827 resolve 即 toast「重新啟動中…」，ok:true 會說謊）。

### R1 #2（驗證腳本鑑識）：❌→（R2 核銷轉✅）
8 攻擊向量，3 決定性假綠/假紅在威脅模型內：
- e2 迴圈探錯變數（target.second）＝掃蕩壞死原 bug 復活，10/10 假綠 → 必須①backreference
- c2 死變數偽裝（實參牆鐘＋log 留 ELAPSED 字樣）全綠 → 必須②實參位址一體釘＋RTC\b
- f2 doc 註解提及 finish → T7 假紅 → 必須③strip 域
- 必須④NC1 恆真死斷言（`|| true` 裝飾品）刪除＋guard 讀 KT
修後 e2→僅 T1 紅、c2→僅 T2 紅（PM＋R2 雙獨立自證收斂單點）。

### R1 #3（治理）：✅
白名單零越權；計畫七要件＋行號九點全對拍；b4cc444 零漂移宣稱實跑驗證（diff 空）；
「finish 在 try 外」不實宣稱「登記不修＋T7 反向釘」判定恰當（誠實歸責）；
必須項＝commit 紀律兩條（他軌髒檔實測 6 個含 scheduler.js/db.js/store.js——嚴禁 -A；
subagent-log 與 fix 原子入庫）。

### R2 複核席：✅（逕改判）
四必須項逐條實碼核銷＋三攻擊變體重現精准收斂；gradle --rerun-tasks 真編譯綠（66/66 重跑）；
reject 出口連動檢查無破損。

## 驗證命令＋實際輸出
- POST：`SRC=<工作區 IconPlugin.kt> node --test tools/verify-f4-icon-restart.mjs`
  → **tests 11 / pass 11 / fail 0**（T0 基線正宗釘 b4cc444 三缺陷＋T1-T8＋NC1/NC2）
- PRE 態（git show HEAD）：4 pass（T0 綠＋T7＋NC 跳過）＝T1-T6 紅基線如設計
- e2/c2/f2 攻擊變體（/tmp）：精準單點紅×2＋f2 不紅＝修復全部如實
- `./gradlew :app:compileArmDebugKotlin --offline --rerun-tasks`：BUILD SUCCESSFUL
- 回歸：npm run build ✓ 745-841ms；tts-contract fail 0；g9 fail 0；f11 29/29

## 未完成事項
代碼側無。範圍外登記（含 R2 殘留）：settings.js:823 投機寫入 setSetting 先於 await
（reject 後 DB 與 launcher 短暫不一致，JS 域另單）；getCurrentIcon DEFAULT 誤判＋
ResolverActivity 清理（F5 下一顆）；JS 消費 restart 欄位；PendingIntent.cancel()
deprecated（API36 仍可用）；非 alias running 1.2s 窗（PRE 同態）。
