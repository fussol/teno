# Subagent 紀錄 — E16（2026-08-28，PM2 首相2）

## 任務目標
刪除三個死檔：src/lib/deprecated/sim-engine.js（舊 JS 模擬引擎，E8 波次
政策隔離）、src/lib/themes.js（死調色盤，現行主題系統=lib/theme.js 單數）、
src/pages/svg.js（icon 死分裝層，全庫 import 指向活本體 lib/svg.js）。
連帶孤兒 sim-behavior.js 白名單外→E16-SR1 登案不逕刪。

## 委員
R1 ×1（simple 單席，計畫階段審查）：✅ 准行（0 阻斷/3 次要/2 nit）。
全發現吸收升版 v1.1：錯誤碼雙態判別子（pages/svg.js Node ESM 不可 import，
?raw 擴充錯誤＝存在性副證）、SVG 独有符號改釘零導入者、vite build 升必測
＋glob 盲點釘（main.js:70 模板字串 import）。委員獨立穷举（Rust/py/sh/
new URL/cron）零活引用、verify-e8 親跑 18/18＋刪後推理恆綠、_dev/cli 豁免
覆核理由補強（指向原路徑早已死）。

## 驗證
tools/verify-e16-orphan-deletion.mjs 雙態自適應：
- pre 態 23/23 ALL PASS（零引用前提＋import 判別子方向性＋歷史真實＋邊界釘）
- post 態 29/29 ALL PASS×2（殲滅＋ERR_MODULE_NOT_FOUND 點名本檔＋負控制 NC
  樹死檔復生重現 6 腿）
- vite build 0（glob 收斂實證：dist import map 無 "./pages/svg.js"、死 chunk
  svg-*.js 消失）
- 回归 e8/e15/e14/e13/e4/e9/d7/d19/d20 全 EXIT=0

## 裁決與產出
刪除 3 檔（git rm）；E16-SR1 落盤 scope-requests（sim-behavior.js 建議准刪，
刪後 deprecated/ 可整目錄消失）。

## 未完成事項
- sim-behavior.js（7.6KB 連帶孤兒）待總統裁示 E16-SR1。
- 本波 PM2 佇列全數完成。
