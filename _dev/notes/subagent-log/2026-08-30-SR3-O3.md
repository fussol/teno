# 2026-08-30 SR3-O3 修復紀錄

## 任務目標
修 TtsPlugin.saveExportFile 用 API29+ 的 MediaStore.Downloads 類 → minSdk=24 裝置（Android 7-9）NoClassDefFoundError 崩潰。

## 病灶 / 一手查證
- MediaStore.Downloads 為 API29+ 巢狀靜態欄位類（官方 MediaStore.Downloads reference 證實）；minSdk=24 裝置解析 EXTERNAL_CONTENT_URI 靜態欄位即 NCDFE。
- Art 類引用為懶解析：SDK_INT>=29 分支讓 <29 裝置永不執行 MediaStore.Downloads sget → 類不載入 → NCDFE 消除。

## 修法（TtsPlugin.kt saveExportFile）
- Build.VERSION.SDK_INT>=29：保留現行 MediaStore.Downloads 現行邏輯（零變更）。
- else（API24-28）：legacy 舊路徑（Environment.getExternalStoragePublicDirectory DOWNLOADS/Teno + FileOutputStream），全限定名（此檔無 java.io import）。
- catch 保留 Log.e（診斷能力）。

## 審查
1 名委員（簡單 bug：單檔、非共享、低風險、<20 行 — GOV-BRIEF 降級條件）：
- 修正 1 處後過審：§3 legacy 分支 File/FileOutputStream 未限定/未 import 會編譯敗 → 改全限定名。
- 權限界線確認：targetSdk≥30「WRITE 無效」規則僅 Android11+ 平台碼，API24-28 可 runtime 授予，權限流另顆宣告成立。

## 驗證（實際輸出）
- verify-o3-api29.mjs：**5/5 PASS**（T0 PRE 基準 73e3a7f 無分支、T1 分支保留 MediaStore、T2 legacy 零 MediaStore 引用、T3 負控制真舊碼對 T1 必紅、T4 compileSdk36）。
- compileArmDebugKotlin：**BUILD SUCCESSFUL**。
- vite build ✓ 956ms。
- 回歸：verify-f6 13/13、verify-f10 30/30。

## Commit
- `17fefeb` fix: O3 …（5.8.8）

## 未完成事項 / backlog
- **WRITE_EXTERNAL_STORAGE runtime 權限 request 流**（API23+ dangerous 權限）：現 app 無權限機制，API24-28 legacy 匯出若未授權會 SecurityException→reject（優雅，不再崩）。功能補全屬另顆（scope-requests 登記）。
- F10 發現之 copyUriToCache input stream 洩漏（見 F10 log）。