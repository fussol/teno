# D20-SR1 審查紀錄 — 2026-08-30

## 任務目標
GUI 刪字/刪牌組路徑補 `exam_history` 雙世代孤兒清理（deleteWordsByDeck / deleteWord 各漏一族）。

## 裁決
✅ 通過（1 輪 3 人次委員）。

## 修法
`src/lib/db.js`：
- `deleteWordsByDeck`(:370)：併入 legacy 文字世代 DELETE（`word IN (SELECT word FROM words WHERE deck=$1)`），對齊 CLI cmdDeleteDeck。
- `deleteWord`(:226)：補 B4 後 id 世代 DELETE（`word = String(id)`）。
- 順序：兩條 exam DELETE 皆在 DELETE words 前（子查詢依賴）。

## 委員發現（採納，升計畫書 v1.1）
- F1：verify-d14 需 `--experimental-test-module-mocks` flag（node:test mock.module），裸跑會炸；回歸無退化。
- F2：§8 checklist 補勾。
- F3：§3「兩條 DELETE vs JOIN 統一」碰撞風險等價，真正優勢=簡潔＋先例對齊。

## 驗證命令＋實際輸出
- `node tools/verify-d20-dual-generations.mjs` → 10/10 ALL PASS（語意重放 tmp SQLite + source 契約釘 + 負控制剝除單一族孤兒留）。
- 回歸：`node --experimental-test-module-mocks tools/verify-d14-delete-orphans.mjs` 7/7 PASS；g24/g30/d6 全綠。
- `verify-d20-delete-deck` 29/2：T8b/T8e 為 cli.mjs 既有失敗（stash + 結構證明 grep db.js=0 隔離，非本次引入）。

## 未完成事項
- none。

## Commit
`d04986a` (v5.8.3)

## 產出文件
- `_dev/notes/D20-SR1-fix-plan.md` (v1.1)
- `tools/verify-d20-dual-generations.mjs`

## 工具教訓（記入 memory/skill）
- `git commit -F /dev/stdin` + heredoc 組合不可靠（hook 吞無輸出）；改用暫存檔 `-F /tmp/xx.txt` 穩定。