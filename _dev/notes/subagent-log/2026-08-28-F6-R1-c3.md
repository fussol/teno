# F6 R1 驗證鑑識席（c3）— verify-f6-manifest.mjs

日期：2026-08-28 ｜ 基線：HEAD=ee93ffa ｜ 計畫：F6-fix-plan.md v1（凍結）
威脅模型（腳本頭註定文）：防無意回歸＋偷工，不抵禦蓄意偽裝對抗。
紀律：repo 唯讀（編譯閘之 gradle 產物落 .gitignore'd build/ 屬任務自帶必然後果）；實驗全在 /tmp（/tmp/f6-atk 攻擊樣本、/tmp/f6-clone 語意實測克隆）。

## 0. 任務目標
雙態重跑 → 7+ 攻擊變體（紅域收斂判定）→ 正則品質／repoGrepEmpty 樹引用語意逐行查 → 可選編譯閘 → 裁決。

## 1. 雙態重跑（與計畫宣稱一致）
| 態 | 命令 | 結果 |
|---|---|---|
| PRE | `node tools/verify-f6-manifest.mjs` | T0✓ T1✘ T2✓ T3✘ T4✓ T5✓ NC1⊘ NC2⊘，**exit 1**（T1=MANAGE 在位、T3=allowBackup 缺失，正宗 bug 事實釘）|
| POST | `SRC_MF=/tmp/f6-post/AndroidManifest.xml node ...` | **8/8 綠，exit 0** |

註：PRE 態 NC1/NC2 為 `assert.ok(true)` 文檔化跳過＝真空通過，PRE 有效釘實為 6（D6 登記）。
工作區 manifest/gradle 未改（`git diff` 空）＝PRE 態；POST 樣本與 HEAD diff＝MANAGE 刪＋allowBackup＋註解，與計畫 §3 逐字一致。

## 2. 攻擊矩陣（POST 樣本注入，SRC_MF/SRC_GR 指向）
| # | 攻擊 | 預期 | 實際紅域 | 收斂判定 |
|---|---|---|---|---|
| a | 半成品：忘加 allowBackup | T3 | **T3＋NC2** | ✓ 收斂備份域（NC2 錨點缺失→mutant===MF→fail-loud 樣本形態漂移，屬加分行為）|
| b | 忘刪 MANAGE | T1 | **T1** | ✓ 單紅 |
| c | allowBackup="true" 形對值錯 | T3 | **T3＋NC2** | ✓ 收斂 |
| d | 一刀切連 WRITE 也刪 | T2 | **T2＋NC1** | ✓ 收斂權限對域（NC1 同款 fail-loud）|
| e | manifest 佔位寫死 "false" | T5 | **T5** | ✓ 單紅 |
| f | gradle release 注入 cleartext=true | T4 | **T4** | ✓ 單紅（雙保險：on.count=2∨doesNotMatch 皆命中）|
| g1 | MANAGE 重排多行標籤逃 MANAGE_RE | T1 | **T1** | ✓ L49 子串兜底釘生效（MANAGE_RE 本就先紅——`[^>]*` 原就跨行）|
| g2 | allowBackup 錯置 `<activity>`（application 移除）| 至少 T3 | **8/8 綠＝漏網** | ✘ **缺陷 D1**；且 AAPT2 編譯閘亦不覆蓋（§4 實測編譯綠、merged 中 application 無此屬性＝bug 存活）|
| g3 | 重複標籤（第二處 allowBackup="false"）| T3 計數紅 | **T3＋NC2** | ✓ 計數釘恰一處生效（hits=2）|
| h | 註解內藏 allowBackup="false"（誘誤紅）| 綠（去註解防線）| **綠** | ✓ stripXmlComments 不虚紅 |
| tts | SRC_TTS_KT 換掉 MediaStore.Downloads URI | T2 | **T2** | ✓ 消費者成對釘負向真有效 |

f 首轮曾現「全紅」＝本席 harness 誤（未同指 SRC_MF→manifest 回落 HEAD=PRE 態），修正後單紅收斂；**非腳本缺陷**，誠實記錄。

## 3. 正則品質／逐行缺陷清單（檔案＝tools/verify-f6-manifest.mjs）
### 必須項（v1.1）
- **S1 自爆釘（严重，合併即死）**：:29-32 `repoGrepEmpty` 路徑白名單含 `tools`，而本腳本自身 :48-52 含字面 `isExternalStorageManager`。今日綠僅因腳本**未入庫**（HEAD 無此檔）；F6 合併後 `git grep ... HEAD -- tools` 必自 hit → T1 第三斷言恒紅 → **回歸閘與修復同步死亡**。實測：/tmp/f6-clone 內提交本腳本後 `git grep HEAD -- …tools` 命中 `HEAD:tools/verify-f6-manifest.mjs`（rc=0）；ee93ffa 對照乾淨（rc=1）。
  **修法**（:32 pathspecs 追加）：`':(exclude)tools/verify-f6-manifest.mjs'`（推荐，訊息字面可保留）；或 needle＋:52 訊息全部串接混淆。
- **D1 錯置雙盲（中）**：:65-68 T3 僅釘字面計數，無元素錨——allowBackup 貼到 `<activity>` 靜態釘全綠＋**AAPT2 亦綠**（§4 clone 實測：BUILD SUCCESSFUL，merged manifest 該屬性滯留 activity、application 缺省＝備份仍開）。錯置屬「偷工形態學」，在威脅模型內。
  **修法**：T3 增 `assert.match(MF, /<application[^>]*android:allowBackup="false"/)`（`[^>]*` 跨行，已實測兼容 POST 多行 application 標籤＝true），計數釘保留原樣。
### 建議項
- **D2（中）**：:22 GR 未去註解——gradle 註解若含 `manifestPlaceholders["usesCleartextTraffic"] = "true"` 會令 T4 誤紅（安全向誤報）。修法：GR 匹配前過 `//`／`/* */` 註解。
- **D3（低）**：:31 `${PIN.replace('HEAD','HEAD')}` ＝ no-op 死代碼，掩蓋原意（疑為 worktree→HEAD 正規化殘留），刪或註意圖。
- **D4（低）**：:32 白名單缺頂層 `scripts/`、`public/`、`index.html`（JS 消費者理論可出處）；補路徑或登記限制。
- **D5（低）**：:56 T2 釘死 `WRITE…maxSdk` 屬性順序——語義等價重排（maxSdk 在 name 前）誤紅（安全向）；可改同標籤雙 match。
- **D7（資訊）**：T4 `off.length===1` 把「release 顯式再標 false」這類語義安全寫法也釘紅——過嚴但安全向。
### 正面鑑定
- **repoGrepEmpty 樹引用語意＝正確**（逐行＋實測）：帶 `HEAD` 樹引用讀**提交態**——/tmp/f6-clone 弄髒已追蹤檔後，`git grep … HEAD` 零命中（rc=1）對照裸 `git grep`（讀追蹤檔工作區態）命中（rc=0）。**PRE 基線釘不會被髒工作區污染**；T0 另釘死 ee93ffa 更免疫；MF/GR 預設源 `git show HEAD:` 與守門同讀提交態＝語意自洽。非 0/1 退出（128 等）→ 回 false → 紅＝fail-loud 正確。
- **NC1/NC2＝真突變非恆真**：NC1＝notEqual（錨點漂移 fail-loud）∧ MANAGE_RE 命中突變體，合 T1 綠（對乾淨 POST 偵測面必 false）構成辨別力；NC2＝notEqual ∧ 命中歸零（防偵測面虛胖）。a/c/d/g 變體中 NC 自紅＝偵測到「樣本偏離預期形態」的加分 fail-loud，紅域全部收斂對應域，**無全紅亂響**。
- 註解防禦（F5 教訓移植）有效：h 變體不誤紅；T1 子串兜底（:49）封死非自閉合/多行 MANAGE 形態（g1）。

## 4. 編譯閘（可選項，已實跑）
- 主倉 PRE 工作區：`:app:processArmDebugMainManifest --offline --rerun-tasks` → **BUILD SUCCESSFUL（16/16 真執行）**，AAPT2 鏈可用記錄之；merged debug 產物 `usesCleartextTraffic="true"` ＝ **gradle→manifest 佔位注入鏈 AAPT2 終端實測通**。
- /tmp/f6-clone＋g2 錯置 manifest：BUILD SUCCESSFUL ＝ **AAPT2 對錯置屬性無感**，故 D1 只能靠靜態釘補錨（已給修法）。
- clone 補件記錄：tauri.settings.gradle/tauri.build.gradle.kts/tauri.properties/proguard-tauri.pro 為 gitignore 產物需複製；keystore.properties 以假值 stub（不讀真鑰）。

## 5. 未完成事項
- verify-f4／verify-f5／tts-contract／npm run build 回歸（計畫 §4 有列，本席任務域外未跑）。
- POST 樣本尚未经 AAPT2 實跑（本次只驗 PRE 態與 g2；POST 未落工作區，紀律禁 repo 寫入）。
- 真機備份行為（allowBackup=false 雲備份關閉之 e2e）＝靜態釘＋官方文檔代替，F5/F17 同款登記。

## 6. 裁決：❌（v1.1 複審制）
雙態、8/11 攻擊完美收斂、NC 真突變、樹引用語意正確、AAPT2 鏈實通——骨架高品質。但 **S1 自爆釘**（回歸閘在修復合併的同一個 commit 死亡，實測非推測）屬驗證器存在性缺陷：**今日全綠的代價是明日必全紅**；加上 **D1 錯置雙盲**（靜態＋編譯閘皆不覆蓋、bug 存活）於威脅模型射程內。兩者皆為一行級修法（S1＝pathspec 排除；D1＝application 錨，已預驗兼容 POST），修後單席複審即可轉 ✅。
