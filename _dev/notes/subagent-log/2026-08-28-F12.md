# Subagent Log — F12 (2026-08-28, PM3 接續首相)

## 任務目標
curl 殲滅（Android 無 curl 二進制：scrape_quizlet＋install_piper_model 下載閉包
必敗）＋ 404 中毒鏈根治（curl 無 -f，HF 錯誤頁存成假 .onnx＋安裝成功假象）。
修法＝ureq 化三呼叫點＋pub fn download_url_to_file（先 call 後建檔）＋
parse_piper_url 四項強化（?/# 去尾、大小寫不敏感前綴鏈、非 ASCII 起頭守門、
panic 安全 .get()）。計畫書 `_dev/notes/F12-fix-plan.md` v1.0→v1.1。

## 接手現狀覆核（誠實條款）
- 前首相 session（429 熔斷，pm3.runner.log 實錘）已寫碼＋驗證腳本＋整合測試，
  但 R1 **未閉環**：無本 md、無計畫書 §7、R1#3 返修只做一半——
  `multibyte_prefix_no_panic` 測試釘在、拒斥守門碼缺席（接手初跑 cargo piper
  20 passed/1 FAILED 實錘；verify-f12 19 PASS/2 FAIL）。
- v1.0 測試第 4 斷言（U+3000 is_err）係錯誤斷言：首相 rustc 實錘 str::trim 依
  Unicode White_Space **會**剝 U+3000（前版註解「不剥」不實）→ v1.1 勘誤為
  is_ok＋值釘（三委員獨立 rustc 複核裁決勘誤正確、非粉飾）。
- 本首相補全：非 ASCII 起頭守門（lib.rs:469-476）→ cargo piper 21/21 重建綠。

## 委派（R1 重組全量，3 委員 leaf，delegate_task，全程唯讀）
### #1 負控制與驗證完整性 → ✅（0 阻斷 2 次要 4 nit）
- 獨立跑 cargo piper 21/21＋rustc 實錘 U+3000 trim＋T3 徵狀親重現
  （curl 舊旗標 exit 0＋"Entry not found" 落盤）。
- EVIL 變體注入模擬×5：EVIL1（建檔先於 call）→T2 次序釘紅；EVIL1'（抄防殘）
  →T2 中斷釘紅；EVIL2（吞狀態碼）→T1 真網紅；EVIL3（刪錯時機）→結構釘被騙但
  T1 成功腿紅；**EVIL4（scrape .ok()? 吞錯）唯一半開通道**→採納補 T2 錯誤傳播釘。
- 次要#2：下載中斷刪半成品僅結構釘無行為腿→採納 f12_download.rs
  truncated_body_removes_partial_file（本地迴環 TcpListener，免 TENO_NET 閘）。
- T3a 正交性獨立驗證：基準 pin 靜態 hash 6cd51c9^（git show 實測含 3 處 curl），
  T4d 反轉釘讀現行樹——一釘過去一釘現在，零矛盾。
- nit 採納：T0 計數下限釘（防子孫刪測試 vacuous 綠）、§5「10 跳」勘誤。
### #2 消費者穷举＋行為等價性 → ✅（0 阻斷 2 次要 2 nit）
- curl 全庫穷举歸零（tools/ 命中僅驗證腳本負控制腿＝預期；speak_piper 播放器鏈
  paplay/pw-play/aplay 非 HTTP 屬 §3.5 範圍外）。
- 消費者唯一鏈：scrape_quizlet←api.js:22←import.js:517；install_piper_model←
  api.js:66←settings.js:754；兩鏈 catch 皆直出 toast，零錯誤文案字串分支。
- 自建 24 向量探針（真碼提取）：tree/blob/resolve/裸 host/大小寫/query 尾/
  尾斜杠/tab/換行/U+3000 全正確，零誤殺；F9 世代 13 釘全綠。
- 混合粘貼「请帮我安装 <url>」「🎤 <url>」→守門 Err 清晰訊息，UX 裁決合理。
- node --check cli.mjs OK＋vite build 759ms 綠。
- 次要#1 守門不對稱（ASCII 起頭空格粘貼繞過）→§6 另案；次要#2 into_string
  10MB 上限→§6 登記。
### #3 安全面＋純函數＋commit 純度 → ✅（0 阻斷 2 次要 5 nit）
- 路徑穿越不可達實錘：name 由 split('/') 段切＋'-' 拼接**永不含 `/`**；
  dir 可含 ../ 但只進 URL，url crate（WHATWG）正規化困於 host 內終究 404。
- SSRF 零新增面：install_piper_model 硬編 https://huggingface.co base 重組 URL，
  用戶只控 path 段；舊 curl 同樣無白名單，404 中毒鏈封閉＝淨降。
- 邊界實測登記：BOM U+FEFF/U+200B trim 不剝→守門 Err（正確）；NBSP trim 剝→
  正常解析；全形冒號 lenient→段位移 404（登記）。
- U+3000 勘誤獨立裁決正確；T4d 演化釘與 F9 T3a 正交不腐化。
- commit 純度清單：lib.rs/verify-f9/新檔屬 F12；**cli.mjs SR-C4 hunk（PM1 域）
  與 scope-requests.md（共享檔）勿夾帶**。
- 次要#1 §5「不跟 https→http 降級」宣稱不實（agent.rs:260 https_only:false＋
  httpbingo 實測照跟）→採納勘誤；次要#2 `..` 段守門→§6 另案。

## 吸收（v1.1）
EVIL4 錯誤傳播釘、truncated 行為腿（4/4）、T0 計數下限、§5 雙勘誤（5 跳＋
降級敘事撤回）、T1 計數 3→4、註解 20→21、檔案 mode 644。

## 驗證（吸收後全量復跑，實際輸出）
- tools/verify-f12-curl-to-ureq.mjs：**22 PASS / 0 FAIL**
  （T0 21/21＋計數下限／T1 真網 4/4 含中斷腿／T2 結構釘×11／T3 負控制徵狀面／
  T4 host＋aarch64-linux-android cargo check 雙綠）
- 回歸：verify-f9 18/18（T4d 演化釘落地後首跑）· verify-e8 18/18 · verify-d7 18/18
- cargo test --lib piper 21/21；cargo test --test f12_download 4/4

## 未完成/範圍外
§6 登案（守門不對稱另案・.. 段守門另案・https_only(true) 另案・sha256/續傳呈總統・
Android 真機端到端需用戶終端）。

## 委派事故登記
首輪三席並行中 #1/#2 遭免費 HF 端點 429 熔斷（半途中止無有效裁決），
改單席 sequential 重派成功—— Governance 實測：三 leaf 並行＋本首相 session
≥4 併發即觸頂（4 req/IP 上限），本波次後續審查改 2+1 或 sequential。
