# 2026-08-28 C10 委派紀錄（首相1）

## 任務目標
Bug C10：三同構檔 rateCard `_ratingLock` 無 try/finally——同步尾段（increment 鏈、快照、
rate/requeue/next、easter-egg checks、generateOptions、state 指派）任一同步拋錯 → 鎖永停
true → 評分靜默吞＋undo 頭部同款檢查連坐＝雙路徑癱瘓（模組級變數，唯重進頁面可解）。
修法：鏡像 C8 undoRating 結構——鎖取得後全包 try＋finally 釋鎖＋renderFn 出 try。

## 委派清單（全部 Hermes delegate_task leaf，法律⑦）

### R1 三委員（v1.0 送審）
- **#1 修法正確性**：✅。root cause 實讀屬實（行號顯微漂移：flip try/catch 實 :99-105、
  mc generateOptions 呼叫實 :149——v1.1 已登 §2）；四路徑等價逐條查證（JS finally-on-return
  語意實測＋/tmp/c10a 獨立覆現修法 22/22 綠）；`_ratingLock` 24 matches 恰三檔無第四鎖；
  C8/C9 互斥不破壞（check→acquire 原子對）；附帶改善登記（原 catch 內 toast 若拋反鎖死，
  修法後 finally 兜底嚴格更好）。發現：try 前窗口補證→§5 已錄。
- **#2 消費者穷举**：✅。鎖無匯出、rateCard 呼叫面恰 6 點全在 mount 內（click await 無
  catch＋keydown fire-and-forget）——rejection 僅 console 紅字，監聽註冊鏈路零影響；
  easter-eggs 拋源實錘（checkAchievement :47/:49 裸 JSON.parse，`_achievements` 無防呆寫端）；
  undo 誤放跨幀不可能論證成立。登記：click 路徑「拋後評分鈕殘 disabled」既存紋裂（LEGACY/
  FIXED 同款，§6 域，非本單引入）。
- **#3 測試品質（變異牙檢）**：✅ 有條件。變體 C 吞錯棄案 12 紅雙重公正否定、D 漏修檔 4 紅、
  E 挪快照唯 T8 逮、F noop-finally 10 紅（不淪 marker-grep）、注入偽綠通道排查完整。
  盲區三形全綠：A1（renderFn 進 try＋finally 後置）、A2b（renderFn 提前＝stale render）、
  B（只包 await 段＋finally＝結構違約）。必補三釘。

### v1.1 響應
- §2 行號修正＋§5 try 前必不拋論證＋§8 R1 紀錄。
- 驗證腳本補 T9（渲染時序＋重入鎖探針，封 A1/A2b）＋T10（結構忠實靜態釘，封 B）。
- 誠實登記：T9 初版以 undo spy 作同步鎖探針失敗——undo spy 位於 undoRating 內
  `await _goalPending` 微任務之後不可同步觀測；實測後改 rateCard 重入→rateCalls spy 同步
  push 探針（`await f()` 之 f() 掛起前同步執行）。
- v1.1 雙向實跑：--expect-legacy 26/26 ALL PASS；未修碼正常模式恰 15 FAIL（12 行為辨證＋
  T10×3，T9 恆綠釘零誤報）。

### R2 複審（#3，v1.1）
- ✅ 放行動工。手套正確修法全綠基準；四變體實跑：A1→T9 鎖探針紅＋T10 紅（盲區閉合）、
  A2b→T9 三釘紅（盲區閉合）、B→行為釘如實綠（等價登記）＋結構釘紅（T10 生效）、
  G（catch-all 吞錯）→T1/T3/T4/T5 上拋釘紅（腳本不奖励吞錯）。T9 探針改法與腳本實裝一致
  （誠實）。勘誤兩條（非阻斷）：斷言總數 27/28 非 26；§2 mc 起點 :107。

## 動工後驗證（實際執行輸出）
- node --check ×3 CHECK_OK；verify-c10 正常模式 **ALL PASS ×3 連跑**（28 斷言）。
- 負控制（/tmp/xn HEAD 三檔還原）--expect-legacy **ALL PASS**。
- 回歸：verify-c3/c7/c8/c1/c9/c5/c6 **全 EXIT=0**；vite build 736ms。

## 未完成事項
無。佇列下一顆＝G4（easter-eggs `_totalRated` 無人寫入→成就永不觸發，src/lib/easter-eggs.js
屬 PM1 白名單；與本單 §6「easter-eggs 健護」延伸相接）。
