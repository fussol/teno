# Subagent 紀錄 — E6（2026-08-27/28 波次，PM2 佇列第 3 顆）

## 任務目標
修 `tools/cli.mjs` cmdStudy 全程不寫 review_log → cmdOptimize（fsrs-optimize.py
per-mode）/cmdAudit 看不到 CLI 學習；連帶 FSRS 構造統一 fsrsCtx(mode)、elapsed
dayCutoff-aware＋夾零、futureCounts 補上、逐題實測 duration（cap 60s）。
計畫書 `_dev/notes/E6-fix-plan.md`（v1.0→v1.1→v1.2）。

## 委員（delegate_task leaf，全程唯讀指示）與裁決
| 委員 | 審查面 | 裁決 | 重點 |
|---|---|---|---|
| #1 | FSRS 計算同構性 | ✅ | §3a 等價屬實（修前即讀 mode 化 key）；F1=SR-C4 hunk 混入（範圍外，隔離處理見下）；F2=malformed blob 回退微調（不修） |
| #2 | 寫入路徑＋消費者窮舉 | ✅ | 11 欄逐欄全符；消費者 8 處無崩潰；採納 ensureSchema() 建議；交易包框/undo 交錯列範圍外簽署 |
| #3 | 驗證腳本變異牙檢 | ✅ | A/C 變異紅（核心防線實錘）；B/D/E 三項斷言強度缺陷如實登記；strict 斷言建議經實測 14/20 同 ms 碰撞駁回 |

全席 ✅ → 過審（R1 一輪）。

## 驗證命令＋實際輸出（本機重跑）
- `node tools/verify-e6-study-log.mjs` → `E6 驗證: 22/22 PASS ALL PASS`（×7 連跑 0 失敗，
  含隔離 SR-C4 後重跑）
- 負控制：內建 T7a（剝 INSERT→log 0 筆 cards 卻更新）✅紅、T7b（預設權重構造→audit
  1 有差異）✅紅
- 委員 #3 外部變異：A 剝 push→紅／C elapsed 退化→紅；B（scheduled round）/D3（雙欄
  時戳坍縮）/E（cap 上界）三處斷言失明→已登記 §8，不假裝有防
- 牆鐘探測：`/tmp/e6-gap-probe.mjs` → `zero-or-negative gaps: 14/20, min gap ms: 0`
  （駁回 strict 遞增斷言的依據）
- 回歸：e4 24/24、e5 18/18、a10/a9/c3（node --experimental-test-module-mocks）ALL PASS、
  c5 96/96、a3 7/7、`node --check`、`npx vite build` ✓ 705ms

## 未完成/範圍外事項
- E7（study mc/spell base 卡污染）＝下一佇列；log 結構已預留（card_state 來源改
  mode 卡零改動）。
- study due 錨定 A10、queue 模型、交易包框、undo 交錯：§7 登記。
- SR-C4 makeSession hunk：非 E6，commit 前反向隔離、commit 後已原樣還原工作區，
  待 E5 範圍外案或 C 系列认领。

## commit
`fix: E6 …`（tools/cli.mjs＋tools/verify-e6-study-log.mjs＋計畫書＋本紀錄）
