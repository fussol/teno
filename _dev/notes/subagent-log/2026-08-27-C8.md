# 2026-08-27 C8 — rateCard/undoRating in-flight 互斥（PM1・首相1）

## 任務目標
修 C8：rateCard `await store.actions.rateCard` in-flight 期間按 Ctrl+Z／點 undoBtn，
undo 照跑（undoRating 完全不查 `_ratingLock`）→ 與 in-flight 評分 DB 寫入＋engine 續航段
互插，DB/engine 狀態雙重不一致。反向（undo 在途直呼 rateCard）同缺陷類。三同構檔。

## 流程（法③一顆完整循環）
- 計畫書 `_dev/notes/C8-fix-plan.md` v1.0（bug/root cause 實錸行號/修法/可選項定案/消費者清單/驗證/風險/範圍外）
- 驗證腳本 `tools/verify-c8-undo-race.mjs`（deferred 閘門把 in-flight 窗口拉成確定態，非時間擡測）先行實跑
- R1 送審 3 委員（Hermes leaf，全程唯讀）→ 全 ✅ → 動工 → 驗證 → commit

## 修法（三檔同構，rateCard 零改動）
`session-utils.js`／`session-mc-utils.js`／`session-spell-utils.js` 的 undoRating：
頭加 `if (_ratingLock) return; _ratingLock = true; try {`，body 原樣（含 `_undoSnapshot = null;`
移入 try 尾），尾 `} finally { _ratingLock = false; }`，renderFn 移 try 外。
雙向互斥：undoRating 取得 rateCard 既有鎖 → 評分 in-flight 擋 undo、undo in-flight 擋評分。

## 審查裁決（全 ✅，1 輪 3 人次）
- **#1 修法正確性 ✅**：目標域（reviewLog/cards/_undoSnapshot/session/results/current）唯一兩條
  非同步寫鏈即 rateCard↔undoRating，無第三條繞鎖；死鎖/漏釋逐路徑安全（body 無早退、finally
  必達、renderFn 在 try 外）。**發現 C7 pre-existing 競態**（完成畫面 completion-undo 在途時
  notify→ensureQueue 命中 reset 清 results）→ 登 C8-SR0 後續單。
- **#2 消費者/治理相容 ✅**：undoRating 全庫恰 6 呼叫端窮舉；/tmp/c8v 實跑 verify-c3（26）＋
  verify-c7（32）＋c1/undo-cycle/next-after-undo 全綠＝零逆轉。瑕疵：計數 21→24（v1.1 修）。
- **#3 驗證方法學 ✅**：三態獨立重跑復現；變異牙檢 M-a（剝 check）紅集={T1,T3,T4,T1log}、
  M-b（剝 set）恰{T2}、M-c（剝 finally）**存活**→ F1 補 T7（已補並驗牙）；閘門確定性通過。

## 驗證命令＋實際輸出（動工後）
- verify-c8 修法後：**17/17 ALL PASS exit 0**
- verify-c8 `--expect-legacy`（對 /tmp/c8leg HEAD 未修三檔）：**負控制 ALL PASS exit 0**
- T7 驗牙（M-c 重演 finally 換順序釋鎖）：恰 1 紅＝T7（got=0 鎖卡死）
- T8 驗牙（刪 `_undoSnapshot=null` 行）：恰 1 紅＝T8（二次 Ctrl+Z got=1）
- 回歸組合拳：c3=0 c7=0 c1=0 undo-cycle=0 next-after-undo=0 c5=0 c6=0；vite build ✓ 734ms

## 實裝事故（誠實歸責）
首次 python 三段 patch 重建 body 時**整行 `_undoSnapshot = null;` 遭遺漏**（try 內外皆無）
→ 快照未清 → 第二次 Ctrl+Z 重複 pop results。verify-c8 本尊未抓（T1b 只驗 undo 觸發），
**是回歸組合拳 verify-c7 T3（results 5 vs 6）＋T4 逮到**。非修法設計錯，是實裝手誤＋腳本盲區。
處置：補回该行入 try 尾＋verify-c8 加 T8 快照必清釘（刪行變異驗牙）。
**教訓**：重構函式體單跑本尊 bug 腳本不足；try/finally 包裹類修法必含「狀態清理完成」正向釘。

## 未完成事項
無（C8 完整循環完成，已 commit）。
- C8-SR0（發現的 pre-existing 競態）列後續 bug 單呈總統裁示。
- C10（rateCard 無 try/finally）、C9（incrementGoal fire-and-forget）佇列下一/再下顆。
