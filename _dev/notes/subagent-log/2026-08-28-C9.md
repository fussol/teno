# C9 委派紀錄 — 2026-08-28（首相1）

## 任務目標
修復 C9：`incrementGoal` fire-and-forget（三同構檔 rateCard 內）→ 評分後立即 undo 時，
increment 的 `goal_streak` DB 寫入（W1）與 undo 的還原寫入（W2）無 happens-before，
W1 後落 → 覆寫 undo 還原 → 幽靈「今日已達」（記憶體對、DB 錯、重啟回靈）。

## 治理軌跡
- 計畫書 `_dev/notes/C9-fix-plan.md` v1.0（變體 B：呼叫點立即執行＋鏈式追縱完成＋undo 鎖內 drain）
  → 送審凍結 → R1 意見採納升 v1.1（產品碼零變動，腳本/文件升版）。
- 驗證先行：`tools/verify-c9-goal-undo-race.mjs` 送審前雙向實跑留證：
  - 未修碼 `--expect-legacy`：29/29 ALL PASS（bug 精準重現：T1/T3/T4 in-flight 直進＋DB 幽靈=1）
  - 未修碼正常模式：11 FAIL＝紅集恰為辨證斷言全集，恆綠釘零誤報
  - 實裝前迭代两處（誠實登記）：①殘閘未放導致幽靈誤綠 ②wrapper 傳引用至放閘才序列化
    （未按呼叫時刻定值 payload）→ 負控制轉紅暴露建模不忠實 → 改呼叫時刻深拷貝快照。

## 委員裁決
### R1（3 委員，delegate leaf，全程唯讀＋/tmp 副本自驗）
- **#1 修法正確性 ✅**：root cause 逐行複核吻合；(a)鏈式 vs 單變數論證成立（單變數變體實測
  T6 精準轉紅）(b)行為保真成立（T1 同步釘實證）(c)鎖安全（drain 在 check-acquire 原子區後、
  finally 兜底）(d)鏈永不 reject（歸納封閉）(e)棄案變體 A 論證成立（呼叫延鏈→快照窗口錯位）。
  /tmp 套修法 29/29＋HEAD 覆蓋負控制 29/29。F1（低）：跨 mode payload 串道幽靈措辭補強→採納。
- **#2 消費者窮舉 ✅**：incrementGoal 產品消費者恰三檔零漏網；goalStreak DB 寫者窮舉＝
  increment/undo/settings 第三寫者；undo 域四腳本（c3/c7/c8/c1）HEAD 與修後雙態 exit 0；
  四象限（HEAD×legacy、HEAD×normal、修×normal、修×legacy）無假綠方向。F1/F2（advisory）
  ＝範圍外逐名登記→採納 §6。
- **#3 測試品質 ❌**：變異 A（單變數）恰紅✅、變異 B（刪 drain）恰紅✅、**變異 C（drain 放鎖外）
  存活**——自建探針實錘 TOCTOU：check-acquire 原子性破壞→第二評竄入覆寫 `_undoSnapshot`→
  undo 錯靶（用戶撤 A 實撤 B）。處方：補 T8（drain 期間第二評必被鎖丟棄）。另 T2 mode 參數
  脆弱、T1 偽綠註明→採納。
### v1.1 修訂
產品碼不變（drain 本在鎖內，#3 確認決策正確、僅腳本未執約）；腳本補 T8＋rateCalls spy＋T2 修正。
升版後未修碼雙向重跑：legacy 33/33 ALL PASS、normal 恰 13 FAIL（紅集含 T8×2）。
### R2（#3 席位複審）✅ 放行
變體 C 重試→**T8 恰 2 斷言轉紅餘 31 綠**（缺口閉合）；變體 B 恰 10 紅可與缺標記分辨；
基底修法 33/33；§4/§6/§8 無宣稱超載；編輯小疵兩處（§4 閘層描述、T7 目次）→補。

## 驗證命令＋實際輸出（實裝後）
```
$ node --check src/engine/session-{utils,mc-utils,spell-utils}.js   → CHECK_OK
$ node --experimental-test-module-mocks tools/verify-c9-goal-undo-race.mjs   → ALL PASS ×3 連跑（33/33）
$ /tmp HEAD 負控制（git show 三檔覆蓋）--expect-legacy   → 負控制 ALL PASS
$ verify-c3-rating-lock → EXIT=0 ALL PASS
$ verify-c7-undo-gate   → EXIT=0 ALL PASS
$ verify-c8-undo-race   → EXIT=0 ALL PASS
$ verify-c1-undo-modes  → EXIT=0（pass 21/fail 0）
$ verify-c5-review-cap  → 96/96 ALL PASS
$ vite build            → ✓ built in 726ms
```

## 未完成事項／範圍外（憲法⑥）
- `_totalRated`（G4 生涯計數）undo 不回滾 → G4 域延伸，呈總統備註。
- 跨 mode payload 串道幽靈＋settings 第三寫者 → store 層 DB 序列化另立單（§6 逐名登記）。
- rateCard `_ratingLock=false` 不在 finally ＝ C10 本尊（佇列下一顆）。
