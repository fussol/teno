# Subagent log — D16（2026-08-28, PM3）

## 任務目標
D16：TENOC 容器 `unpack_db_container`（src-tauri/src/lib.rs:580-609）三寬容缺陷——
version byte 讀而不斷（TENOC\xFF 照收）／log 段後 trailing garbage 靜默丟棄回 Ok／
log 長度欄缺位 if-let 靜默降級為無 log（首相掃描補登第三病灶）。
入口嚴格原則：此函式解包後直接覆寫 teno.db＋app-log.db，靜默容錯＝半壞資料覆寫好 DB。

## 修法（v1.1，lib.rs 三守門＋cargo 測試）
- h1 `data[5] != 1` → Err「容器版本不支援: vN（本版 Teno 僅支援 v1，請升級）」
- h3 log 欄 let-else 拒缺位（含「請重新匯出備份」教學，v1.1）
- h2 `pos != data.len()` trailing 必拒（含 CLI import-db 救回指引，v1.1）
- raw SQLite fallback 分支零碰（D19/E10 立法契約）、pack 端零碰（反向相容釘）
- 新 cargo 測試 d16_container_strict_gates（三守門＋零誤殺釘＋round-trip 位元組級＋
  守門 2/3 訊息級斷言＝R1#3-次要3 吸收）

## 委員（R1，3 席 sequential leaf delegate——F12 波 429 教訓）
### #1 修法正確性：✅（2 次要 3 nit）
- 三病灶 git show HEAD 實碼屬實；三 hunk 與計畫 §3 逐字一致；三消費者
  （write_db_bytes/import_db_dialog/drive_download）皆先 unpack? 後寫＝拒絕路徑零寫盤
- 重跑：verify-d16 30/30、cargo container 3/3、verify-d19 32/32 d7 18/18 e10 9/9 d11 16/16
- 自建 15 邊界向量（空輸入/magic 恰 5/len 欄 0xFFFFFFFF/雙容器拼接…）零誤殺零漏網
- i686 交叉編譯新舊對跑：32-bit 溢出 panic 屬 pre-existing（次要2→§6 另單）
- 負控制真實性：81125ff unpack 與 HEAD byte-identical、守門 token 零殘留屬實
- 次要1（段級 magic 殘洞：結構合法垃圾段仍 Ok，app 兩入口無 D19 式段級守門）→§6 另單
- nit：§1 行號再漂移（unpack :580-609，病灶 :588/:601/:608）→§7 勘誤登記

### #2 零誤殺＋消費者＋存量：✅有條件（1「阻斷級」存量發現，政策預許＋v1.1 補救）
- 消費者 3 呼叫點拒收 UX 逐列：toast 可見錯誤零崩潰、import 前已 backupDb、
  drive 端守門先於 fs::write（D11 契約維持）
- **真機 182 檔 byte-level 掃描**：136 raw（fallback 照收）＋7 TENOC 嚴格 OK＋
  **3 檔 0815 手動修復產物被拒**（尾部 4092B 全零，非 pack 產物）——舊碼吞零尾匯入
  結果正確，新碼拒收＝§5 政策「拒收=目的」，但需救回路徑：救回已實證
  （CLI import-db 寬容版吃入→export-db 重產嚴格容器→新碼 OK）→ Err 訊息教學化（v1.1）
- Rust pack vs CLI packContainer 位元組級同構（結構性零 trailing、version 常數 1）
  ＋交叉餵實跑；反向相容（pack 零 hunk）成立
- 次要：Err 訊息技術黑話無下一步指引→v1.1 修（「請重新匯出備份」＋CLI 救回指引）
- **委員事故誠實登記**：誤用 `export-db` 位置參數覆寫 ~/桌面/teno-backup.db
  （CLI 預設匯出路徑），已即時 live DB 重匯出逐位元組一致還原；倉庫零污染零 git 寫

### #3 變異牙檢：✅（3 次要）
- /tmp 副本 env 化 ROOT（30 斷言邏輯 1:1 未動），11 變體 0 逃逸：
  M-a 單守門/M-b `>1` 誤收 v0/M-c 恆假守門/M-d 缺 pos+=4 錯位誤殺/M-e 死碼順序/
  M-f 刪 cargo 測試（計數下限釘生效）/M-g1,g2 pin 浮動/M-h 訊息改詞/M-i 守門錯位/
  M-k 奇偶狡猾版——行為斷言獨立有牙非文字式；反向 pristine 30/30 復證
- 次要1：T3 負控制無 v0 向量（M-b 僅 T1-1 單臂）→v1.1 補 v0 雙臂
- 次要2：T3a 只釘 hash 格式不釘內容（浮動 7-hex 逃逸，靠 T3b 兜底）→v1.1 補 T3b2
  內容級釘（舊碼必含 if let Some(log_len)）
- 次要3：cargo 測試守門 2/3 只 is_err 不驗訊息→v1.1 補訊息級斷言
- commit 純度：lib.rs diff 恰 3 hunk 55+/7− 零夾帶屬實

## 裁決與處置
- R1 三席全 ✅（#2 條件＝訊息教學化＋存量登記，#3 次要×3 皆簡單吸收）→
  v1.1 吸收後不重送（改動＝字串文案＋驗證腳本補釘，零語意變化；三委員原案預授）
- 驗證 v1.1：tools/verify-d16-container-strict.mjs **32/32 ALL PASS**（T3 加 v0
  向量＋T3b2 內容級釘＋T3e/f startsWith 防漂移）＋cargo container 3/3
- 回归：d19 32/32、d7 18/18、e10 9/9、d11 POST 16/16、npm run build 721ms 全綠
- commit：lib.rs＋verify 腳本＋計畫書＋本 log（SR-C4 cli.mjs hunk、scope-requests.md
  共享檔、C4-fix-plan.md 他相髒檔零夾帶）

## 範圍外（計畫書 §6 全登）
- 段級 magic 殘洞（app 兩入口）＝另單（建議 D16b）
- 32-bit len 溢出 checked_add＝另單（pre-existing）
- drive_sync.rs:375 過時註解（PM4 域）
- pack 端 sha256（併 F9 §6 既呈總統）
- 容器 version 協定文件化

## 教訓
- 「驗證腳本 ROOT 硬編」＝變異測試的隱形陷阱：委員若直接把腳本丟副本跑，
  讀的還是真倉碼，變異全部失效。方法學必須先核對腳本的資料來源路徑。
- 存量掃描（真機檔 byte-level）是「只變嚴不變鬆」類修法的必修課：本單 182 檔
  掃出 3 檔拒收，無掃描則上線後才炸＋無救回指引。
