# F6 R2 單席複核（c1）— 處方落實＋攻擊重現

日期：2026-08-29 ｜ 基線：HEAD=ee93ffa（工作區 manifest/gradle 未動，`git status` 對兩檔乾淨＝PRE 態，實測確認）
前情：R1 三席 ✅✅❌；#3 兩必須項（S1/D1）＋#1 一勘誤（E1）已於 v1.1 修畢。判例＝F4/F5 單席複核（僅驗處方落實＋攻擊重現）。
紀律：repo 唯讀，突變體全在 /tmp/f6-r2/mut/，clone 實驗在 /tmp/f6-clone-r2。

## 0. 任務目標
(a) 腳本 diff 對照 c3 處方逐項核對＋計畫 v1.1 落檔查證；(b) 雙態重跑＋八種指定攻擊重現＋自發三新攻擊＋S1 合併態 clone 回歸＋可選編譯閘；裁決。

## 1.(a) 處方落實核對表

| 項 | c3/R1 處方（v1.1 版本紀錄宣稱） | 腳本/文件實態 | 判定 |
|---|---|---|---|
| S1 拆串 | needle 以 CONSUMER_SYMBOL 拼接 | :33 `'isExternalStorage' + 'Manager'` 拼接，grep 目標經變數傳入 | ✅ |
| S1 exclude（雙保險第二層） | pathspec `:(exclude)` 排除自身 | :38 `':(exclude)tools/verify-f6-manifest.mjs'` 在列；**clone 合併態實測**：裸 pathspec `git grep isExternalStorageManager HEAD -- …tools` rc=0（自爆命中）→ 加 exclude rc=1（零命中）＝exclude 層有效且必要 | ✅ |
| D1 錨 | `ALLOW_BACKUP_RE` 錨定 `<application\b[^>]*` | :28 `/<application\b[^>]*android:allowBackup="false"/g` **逐字一致** | ✅ |
| D1 計數用錨版 | T3 計數改走錨版正則 | :75 `MF.match(ALLOW_BACKUP_RE)`（NC2 :108 共用同常量，無漂移——F5 v1.2 判例沿用）；錯置攻擊復測 T3 紅（§2 c1/c2） | ✅ |
| T2 訊息 E1 改寫 | 與 v1.1 ①b E1 論據一致 | :62-64 註解＋:66 斷言訊息均為「MediaStore$Downloads 為 API29+ 類／24-28 先 NoClassDefFoundError／保留＝零風險保守＋O3 修復後回真消費者」語意，與計畫 :14 ①b 終態逐字同調 | ✅ |
| 計畫 §2③ 措辭 | 「十項內」→「除 no_backup/cache 外幾乎全部 app data」 | :16 落檔（含 R1#2 一手 PathPlugin.kt dataDir 佐證入證據欄）；版本紀錄 :75 所述與現檔相符 | ✅ |
| 計畫 ①b E1 終態 | 不刪・論據改採零風險保守 | :14「**不刪（v1.1 E1 終態）**…」落檔 | ✅ |
| scope-requests F6-SR1 | dataExtractionRules 改派行 | :22 在列（res/xml 新檔＋屬性原子 commit 約束＋PM5＋待裁示） | ✅ |
| scope-requests O3 | TtsPlugin API24-28 匯出必斷另單 | :24 在列（E1 根因、非 F6 範圍、待承接） | ✅ |

**結論：R1 全部處方（S1 雙層／D1 錨＋計數／E1 文字／#2 措辭）如 v1.1 版本紀錄所稱悉數落檔，無空頭宣稱。**

## 2.(b) 雙態＋攻擊矩陣（突變體全 /tmp，repo 唯讀）

雙態基線：PRE（HEAD 預設）→ T0✓T1✘T2✓T3✘T4✓T5✓NC⊘⊘，**exit 1** ✓；POST（SRC_MF=/tmp/f6-post）→ **8/8 綠 exit 0** ✓。

| # | 攻擊 | 預期紅域 | 實際紅域 | 收斂 |
|---|---|---|---|---|
| a1 | 半成品：POST 還原 MANAGE 行 | T1 | **T1** 單紅 | ✓ |
| a2 | 只加 allowBackup 忘刪 MANAGE（HEAD 態注入屬性） | T1 | **T1** 單紅（T3 綠＝形對權未刪被抓） | ✓ |
| b | allowBackup="true" 形對值錯 | T3 | **T3＋NC2**（NC2 fail-loud 樣本形態漂移，c3 加分行為同款） | ✓ |
| c1 | **D1 錯置 `<activity>`**（v1 逃網項） | T3 必紅 | **T3** 單紅 | ✓ **v1 漏網已封堵** |
| d | WRITE 同刪（一刀切） | T2 | **T2＋NC1**（fail-loud 同款） | ✓ |
| e | ${usesCleartextTraffic} 寫死 | T5 | **T5** 單紅 | ✓ |
| f | gradle release 注入 cleartext=true（SRC_GR） | T4 | **T4** 單紅（須同指 SRC_MF——c3「f 首轮全紅」harness 誤同款條件，本席直接雙指，單紅復現；雙保險 count=2∨doesNotMatch 皆命中） | ✓ |
| S1 回歸 | /tmp clone、新腳本＋POST manifest commit 入 HEAD、clone 內跑（HEAD 態＋SRC_MF 雙跑） | 8/8 綠（v1 恒紅治癒） | 兩態均 **8/8 綠 exit 0** | ✓ **自爆釘除死** |

### 自發新攻擊（本席）
| # | 攻擊 | 動機（威脅模型：無意回歸＋偷工） | 結果 | 判定 |
|---|---|---|---|---|
| N1 | allowBackup 錯置 `<manifest>` 根元素（D1 錨的另一錯位形態） | 錨只認 application，根錯位是否同抓？ | **T3 單紅** | ✓ 收斂 |
| N2 | 標籤終止符邊界：allowBackup 前插入屬性值含字面 `>`（XML 合法） | `[^>]*` 遇 `>` 截斷是否假綠？ | **T3 紅**＝安全向誤紅（真備份已關但閘拒收），非逃網 | ✓ 登記資訊 |
| N3 | `android:allowBackup = "false"`（等號兩側空格，XML 合法、regex 字面不匹配） | 偷工手滑空格是否假綠？ | **T3＋NC2 紅**＝安全向拒收，非逃網 | ✓ 登記資訊 |

三新攻擊全部朝「誤紅」方向收斂（fail-closed），無一假綠逃網。空格/`>` 屬性值形態若未来真出現，屬安全向錨緊化項，非阻斷。

## 3. 編譯閘（可選，已跑）
`processArmDebugMainManifest --offline -q`（工作區 manifest＝PRE 態）→ **rc=0 綠**，AAPT2 鏈可用確認；跑後 `git status` 兩檔仍乾淨＝repo 唯讀紀律未破。

## 4. 觀察項（全部非必須）
- **O-1（資訊）**：S1 拆串層（CONSUMER_SYMBOL 拼接）單獨不足以自救——:58/:63 斷言訊息仍含完整字面 `isExternalStorageManager`（c3 處方原文明示「推荐 exclude，訊息字面可保留」＝授權路徑）；**exclude 為必要層**，未來重構勿以「已拆串」為由移除 exclude。建議（非擋）：腳本頭註解加一句此依賴。
- **O-2（低·沿用 c3 D2-D5）**：GR 未去註解（gradle 註解含 placeholder=true 會誤紅，安全向）、:32 pathspec 缺頂層 scripts/public/index.html、T2 屬性順序釘死（安全向）——本輪處方域外，未劣化，續登記。
- **O-3（本席 N2/N3）**：錨對 `>` 屬性值／空格屬性両形態 fail-closed＝安全向；POST 產線形態（:15 無空格、無 `>`）不受影響。

## 5. 未完成事項
- 計畫 §4 回歸鏈（verify-f4/f5、tts-contract、npm run build）＝合併執行者職責，複核席域外（F4/F5 判例同款）。
- 真機備份行為 e2e 不可行＝靜態釘＋官方文檔代替（F5/F17 同款登記，R1 已有）。
- POST 樣本未經 AAPT2 實跑（紀律禁 repo 寫入；本席 PRE 態編譯閘綠＋R1 c3 merged 產物實證已覆蓋鏈路）。

## 6. 裁決：✅
處方九項全落實（§1 表，含 clone 合併態雙保險實測）；八種指定攻擊＋三自發攻擊全收斂對應紅域（§2，无假綠逃網）；S1 自爆釘於合併模擬中確證除死（8/8 綠）；D1 錯置項由 v1「8/8 綠漏網」轉為 v1.1「T3 單紅」；雙態與 c3 R1 結果逐項一致；編譯閘綠。**無新必須項，觀察項皆非阻斷——依 F4/F5 判例可逕判 ✅，無需 R3。**
