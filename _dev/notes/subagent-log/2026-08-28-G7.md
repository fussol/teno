# G7 修復紀錄 — 每評一卡熱路徑（2026-08-28，總統直修）

## 改動（3 檔 +55/-13）
- db.js：`getNewRatedTodayAll`（GROUP BY 單查替代三條 COUNT，NULL-mode 孤兒行組群丟棄≡原 `mode=$1` 等值語意）
- scheduler.js：`getDueCards` 第 11 參 `countsOnly`（mc/spell 徽標只要數字不要清單；採納數逐字複製 break 迴圈防邊界發散）；`computeRetention` 反掃描早停（時序單調由 db.js:493 ORDER BY＋尾插保證）
- store.js refreshDerived：3 round-trip→1；mc/spell countsOnly

## 驗證全記錄
| 層 | 結果 |
|---|---|
| L1 countsOnly≡full（100 隨機場景×cap/newPerDay=0,NaN,null,負/rated溢出） | ✅ |
| L1-NC 偷簡變體灌水=rated 被抓 | ✅ |
| L2 GROUP BY≡三查詢（真 node:sqlite，含 NULL mode、cutoff=240） | ✅ |
| L2-NC 丟 mode 維度被抓 | ✅ |
| L3 retention 反掃描≡前掃描（20k、缺 reviewed_at、視窗邊界） | ✅ |
| L4 10k 詞 50 評分 | POST 647ms（絕對閘值內）|
| 回歸 c1(21/21) a10 a5 c3 a4(35/0) | 全綠 |
| npm run build | ✅ 771ms |
| **真庫對拍**（使用者備份 18,080 log，TENOC 剝頭） | SQL 恒等✅ retention 6512/10311 兩側逐筆等✅ |
| 瀏覽器內測 | 啟動零 JS error、儀表板/側欄 render 正常；評分鏈瀏覽器無 DB（Tauri 限定），以 node:sqlite 真 store harness 覆蓋 |

## 誠實登記
- **PRE≈POST**（664 vs 647ms）：本機 harness SQLite 在進程內，IPC 成本≈0，故 JS 端節省被沖淡。本修的主價值在 **Tauri IPC 橋**（省 2 趟往返 ≈10-60ms/評，實機 Android 更明顯）＋真庫 18k retention 前掃描消除＋countsOnly 跳過 ~6k 物件展開。實機每評延遲改善需使用者 A55/桌面版驗收（列待辦）。
- 修正測試自身 3 bug（誠實歸責）：fixture 時序非單調、GOOD 常數誤硬編碼為 3（實=2，本專案 rating 0..3）、NC 表示式 `&&` 優先級錯——均為測試錯，非實現錯，全部由驗證層自己抓出。
- 範圍外：computeCombinedStats 增量、sidebar renderInPlace、subscribe key stringify、review_log 索引審查（另案）。

## 憲法⑩
無同類 edge 連續退回，不需結構重估。
