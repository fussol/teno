# OCR-CD C subagent-log — 黑灰名單 override 強制加入

- 日期: 2026-08-30
- 專員: OCR-CD（OCR 優化 C/D′）｜範圍: C 段（黑灰 override）+ D′ 段（入庫自動填欄位）
- 任務書: `_dev/notes/PM-OCR-CD-MISSION.md`
- 計劃書: `_dev/notes/OCR-OPTIMIZE-plan.md` §C（本 log 只記 C；D′ 另記 `2026-08-30-OCR-CD-DPrime.md`）
- 版本: **5.9.4**（C commit）

## 動工前確認
- store.js working tree 乾淨（BH-FIX 首相已 commit BH-01~04 釋放）✓
- ocr.js 混疊「OCR-AB 首相的 A 段螢光筆 UI（未 commit，死在中途）」＋「OCR-EF 的 F′ 相關 dirty」→ 採 `git apply --cached` 只 stage C hunk 分離；commit 後以 `/tmp/ocr-ABC-full.js` 還原 working tree 的 A 段供接續者接手。

## 動工範圍（C 段）
- `src/lib/store.js`：
  - `importWords`（:1355）：加 `options` 參數；黑灰擋 `:1372` 改 `const isOverride = options?.override?.has(w); if (!isOverride && ...)`。
  - `importOcrText`（:1446）：加 `options` 參數（`deckName='OCR Inbox'` 後）；`blSet` 剔除 `:1456` 改 `if (!override.has(w) && blSet.has(w))`；`override = options?.override || new Set()`。
  - `:1483` 呼叫 `importWords(parsed, undefined, { override })` —— **第二道也傳 override（plan C.6 高風險封閉）**。
- `src/pages/ocr.js`：
  - render 候選 `:447`：`isBlacklisted?.(t)`→🔒黑名單、`!bl && isGraylisted`→🔒灰名單 badge；遮蔽字 `checked=''`（預設不勾）；`ocrMaskSummary` 摘要列。
  - 入庫 `:488`：`overrideWords = picked.filter(遮蔽)` → `importOcrText(picked, undefined, {override: new Set(overrideWords)})`；`maskedBlocked = res.blacklisted` 顯示（修盲區）；override toast `已強制加入 N/M 個`（N=実際新增）。
  - 補 CSS `.ocr-mask-badge{font-size:11px;color:var(--text-tertiary)}`。
- `tools/verify-ocr2-override.mjs`（新增 harness）。
- `tools/verify-ocr-import.mjs`：T0c 釘同步（`importWords(parsed)` → 帶 override 三參數）。此檔同時含 OCR-EF 的既有必要更新（neon/zebra 避黑名單、ocrCambridgeVerify=false、T4 apple→neon）——死首相無人接手，一併接管並入 C commit（註記），否則 OCR harness 在現狀黑名單下無法正確跑。

## override 一次性授權
- 不 permanent 改 blacklist/graylist；override 僅 per-call Set 傳參，下次 OCR 同字仍會被擋（或若已入庫走 dupSkipped 去重）。符合計劃書 C.4 §4。

## 驗證
- `node --check src/lib/store.js src/pages/ocr.js` → OK
- `node --experimental-test-module-mocks tools/verify-ocr2-override.mjs` → **ALL PASS 27 checks**
  - T0a-c 雙層透傳靜態 / T1 override 字 cat 入庫＋dog 仍擋＋apple 加入 / T2 空集現況 / T3 NEG undefined 擋回 / **T4 importWords 第二道層（C.6 高風險負控制）** / T5a-d ocr.js badge+override 靜態 / PRE/NC git HEAD 動工前實態→override 被無視、cat 擋回＝原 bug 重現（用 `git show HEAD:src/lib/store.js` 讀動工前實態）
- 既有回歸：verify-ocr-graylist ✓、verify-ocr-blacklist(_dev/notes) ✓、verify-ocr-import ✓、verify-ocr-v2-integration ✓、verify-restore-dict ✓
- `npx vite build` → **EXIT:0**

## 審查
審查輪數: **1 輪 3 人次（全 ✅ 過審）**，store.js 共享核心採 3 席（任務書規定不可降席）。
1. Reviewer #1 ✅：override 雙層皆傳、一次性授權正確、badge 短路正確、越界檢查（A 段混疊已分離）。
2. Reviewer #2 ✅：資料流正確、PRE 負控制敏感度實證（委員自製拔牙測試：HEAD 實態餵 T1/T4 正向期望→5 項全紅）。
3. Reviewer #3 ✅：C.4 五項全落實、A 段分離確認、未勾選遮蔽字仍走淘汰→灰名單。
- 3 席一致非阻斷發現全數修正：① `.ocr-mask-badge` 無 CSS → 已補；② maskedBlocked 公式概念錯 → 回歸 plan 原版 `res.blacklisted`；③ override toast 高估 → 改 `addedOverrideIds.length/overrideWords.length`。

## commit
`git add` 明確檔案：src/lib/store.js src/pages/ocr.js tools/verify-ocr2-override.mjs tools/verify-ocr-import.mjs package.json src-tauri/tauri.conf.json src-tauri/Cargo.toml src-tauri/Cargo.lock _dev/notes/subagent-log/2026-08-30-OCR-CD-C.md
Commit message: `fix: OCR2-C 黑灰名單重疊字可強制加入 override（雙層透傳+UI badge + harness 27項含 PRE 負控制）— 一次性授權不永久改單 (5.9.4)`

## 未完成
- D′ 段（入庫自動填欄位：enrich overwrite+多sense+await UI 同步）由同 session 下一個 commit 推進。
- ocr.js 的 A 段螢光筆 UI（OCR-AB 首相死在中途未 commit）已從 /tmp/ocr-ABC-full.js 還原回 working tree，供接續者接手。