# Subagent 紀錄 — G9（2026-08-28，PM5 軌）

## 任務目標
G9（audit 2026-08-13:136）：tts.js ①native 失敗後 30s 靜默無聲②_enVoice/pick 死碼
③ttsAvailable 誤報。修法：每次必重試＋toast 30s 節流、死碼全除、ttsAvailable 誠實化。

## 委員配置與輪次
R1 三席（多部件＋出口 API 語意變更，非低風險，不符降席要件）→ ✅❌✅；
#1 必須項修畢 → R2 單席複核（F17 判例：處方明示「修畢轉✅」＋結構結論不變）→ ✅。
合計 2 輪 4 人次。全程唯讀、實驗限 /tmp。

## 裁決摘要

### R1 #1（代碼事實＋消費者穷举）：❌→（R2 核銷轉✅）
- 🔴 必須 F1（本波最重要發現）：`window.__toast` 為**幽靈信道**——全 repo 零賦值端
  （真信道 main.js:434 `window.toast`；CSS 無 .toast-warn）。舊 toast 自 dabfea1 起
  真 app 從未顯示；PRE 真態＝每次失敗皆雙重靜默（audit 低估）；舊驗證 harness 自供
  `globalThis.window.__toast` stub 把假綠掩蓋成綠。**harness 嚴禁供給被驗證的信道本身。**
- 消費者穷举背書：speak 8 呼叫點全 fire-and-forget 或值即拋棄 await；ttsAvailable
  src/ 零消費者（語意翻轉零破壞）；Android 零路徑誤入（isAndroid import 常數＋提前 return）。
- 建議採納：S1 時鐘後跳 clamp、S2 -Infinity 哨兵、R3 pauseAfterPron=0×死 native invoke
  節奏登記（不改碼）、R4 extractEnglish 零外部消費者＝既存死匯出另案。

### R1 #2（驗證腳本鑑識）：✅（7 攻擊向量全擋，0 阻斷）
a1 吞 toast→T1/T2/T3 紅；a2 反向棘輪→僅 T4 紅單點精準；b ?v=N 隔離自證 PASS；
c 平台開關誤設布爾 false→contract 10/10 全紅響亮（哨兵非 bug）、'false'/0 仍 Android
（strict !== 語意正確）；d splice 錨點各恰 1 處命中語義本體、漂移 fail-loud；
e1 去 mock 真 Date 確定性紅（結構性依賴如設計）；e2 setInterval 偽裝→T3 紅但揭未來
通道→**強烈建議①已採納**（T7 機制釘）；f1 刪平台自釘行→全紅。

### R1 #3（治理）：✅
- loader（verify-tts-contract-loader.mjs）glob 字面收錄＝合法非越權（区别 F14-SR1 的
  cli.mjs 根本不在 glob 內）；增量改動＋預設零變動＋contract/f18 復跑綠三重背書。
- 計畫七要件＋PRE pin 行號五點對拍合規；`fix:` 前綴正確（對照 F18 零代碼 `test:` 恰成對照）。
- 必須項：嚴禁夾帶他軌髒檔（lib.rs/cli.mjs/scope-requests.md）；version 升版登記
  G9-SR1 不逕改（白名單外＋波次慣例波尾集中）。

### R2 複核席：✅
處方①-④＋S1/S2/#2①逐條核銷全中；幽靈信道攻擊自證：真碼 sed 改回 __toast → T7b 精準紅
＋T1/T2/T3/T9 四條動態斷言同步紅（stub 只供真信道，幽靈態運行時零反饋無從矇過）。

## 驗證命令＋實際輸出
- `node --test tools/verify-g9-tts-fallback.mjs` → **tests 13 / pass 13 / fail 0**
  （T0 PRE 三重現：窗內零重試＋真信道形態雙重靜默＋誤報；T1-T6 POST；T7/T7b/T8 靜態釘；
  T9 時鐘邊界；NC1/NC2 負控制）
- `node --test tools/verify-tts-contract.mjs` → 10/10（loader 改動零回歸）
- `node --test tools/verify-f18-timeout.mjs` → 11/11
- f11 29/29、d19 32/32、b6 72/72、`npm run build` 754ms、node --check 綠

## 未完成事項
無。範圍外如計畫 §6 登記：Web Speech fallback、Kotlin 事件面、pauseAfterPron=0 極端
invoke 節奏（R1#1-A3 實測 toast 有上界 ⌈窗/30s⌋+1，接受）、extractEnglish 死匯出（R4）、
升版（G9-SR1）、toast 元件佇列策略。
