# Subagent 紀錄 — F18（2026-08-28，PM5 軌）

## 任務目標
F18（audit 2026-08-13）：tts.js Android TTS promise 無 timeout 兜底 → 事件遺失永久 pending。
判定其現態並結案。首相結論：已被 b620d46（fix: F2，2026-08-15）槽物件化＋armTimeout 吸收，
零代碼改動，以驗證腳本＋absorbed 登記結案。

## 委員配置
結論型判定（誤報「已修」＝bug 帶已結案標籤溜出佇列），不符法①降 1 席之「低風險」要件
（R1#3 裁決背書）→ 3 席並行，Hermes delegate_task leaf，全程唯讀、實驗限 /tmp。

## 裁決

### R1 #1（代碼事實查證席）：✅（3 條非阻塞建議）
- 時間線實錘：dabfea1 版 `grep -c 'setTimeout|armTimeout|timer'` = 0、無 stopped 監聽；
  b620d46 diff 含 `+function armTimeout`/`+armTimeout(slot)` ×2；`git diff b620d46..HEAD --
  src/lib/tts.js` 僅 G10 一行 selector＝F2 後 Android 槽邏輯零漂移。
- §3 路徑表 P1-P8 逐條行號對 HEAD 吻合；自製 6 攻擊場景全收斂（無殘留永久 pending）。
- 新向量：幽靈 start 搶佔 id → 30s 誤殺非懸掛（A2 實測）→ 採納入 §6 範圍外。
- 建議#1 absorbed 行帶實證引用（採納）；#2 §6 補行（採納）；#3 T0 Map 現值例外註釋（採納）。
- 威脅模型定文：接受。

### R1 #2（驗證腳本鑑識席）：✅ 有條件通過（1 必須項，已修）
攻擊矩陣（摘要）：
- M1 陽奉陰違（timer 永不 reject）→ 擋住但**裸 node --test 無限掛死 exit 124**＋連鎖
  ERR_INVALID_STATE → **必須項**：test 加 timeout／after() reset 兜底。【已修：11 test 全加
  `{ timeout: 8000 }`＋after() try{reset}catch，復跑 11/11 綠】
- M2 pause 分支偷加 clearTimeout → T5 精準紅。
- M5 真呼叫改註解 → T8 靜態釘被骗綠（定文邊界內）但 runtime＋NC1 雙閘擋住。
- M3/M4 源碼漂移 → assert.notEqual 失護警語如設計彈出。
- M6a 換 PRE blob → before() 第二釘紅。M6d 精工偽造（藏 setInterval）→ 穿透（需 forge
  blob＝蓄意偽裝，定文排除面外）→ 建議擴字面集【已採納：釘加 setInterval|rAF|queueMicrotask|
  Worker|Atomics】。
- M7 共用 Map 投毒 → 快照路由防禦成立（實測自證）。
- 威脅模型定文：恰當。

### R1 #3（治理與程式席）：✅（2 必須項，均已做）
- absorbed＋`test:` 前綴合規：法源 b1e1399（dispatcher v4.1 讀 absorbed.txt，註解明文
  「PM 發現 ID 已被別案修掉時自行加行」）；先例 8a0b92f `test: A3 收尾登記`；done_pm 正則
  `fix: F2([ :]|$)` 命中 b620d46 驗證；PM5-MISSION「全數 fix: commit」被總統層事後增補的
  吸收通道構成正當例外（只豁免前綴形式，不豁免實證義務）。
- 檔案所有權：觸碰三檔全白名單內；髒檔三檔（scope-requests/lib.rs/cli.mjs）他軌未觸。
- 3 席配置恰當；計畫書七項俱全；commit 體例建議已採用。
- 必須項1：F18=F2 與 test: commit 同 commit 原子入庫（已做）。
- 必須項2：本紀錄落盤（本檔）。

## 驗證命令＋實際輸出
- `node --test tools/verify-f18-timeout.mjs` → `tests 11 / pass 11 / fail 0`
  （T1-T7 POST 七路徑、T8 靜態三釘、NC1/NC2 負控制、T0 PRE 紅基線；R1 修訂後復跑仍 11/11）
- `node --test tools/verify-tts-contract.mjs` → pass 10 / fail 0
- `node tools/verify-f11.mjs` → 29/29 ALL PASS
- `node tools/verify-d19-import-magic.mjs` → 32 PASS / 0 FAIL
- `node tools/verify-b6-exam-delete.mjs` → 72 通過 / 0 失敗
- `npm run build` → ✓ built in 753ms
- `node --check tools/verify-f18-timeout.mjs src/lib/tts.js` → OK

## 未完成事項
無。src 代碼零改動（宣稱由 b620d46 涵蓋，本軌僅交付驗證閘＋登記）；
真機事件丟失注入＝範圍外（任務書准 code 事實＋JS harness）；
幽靈 start 搶佔降級態＝§6 範圍外登記不修。
