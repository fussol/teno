# 2026-08-30 RENDER 專員 — U-LEARN-RENDER 調查/修復

## 任務目標
重現學習卡片間歇性渲染不出（字可反白但看不到）→ 定位 root cause → 修復 → 驗證 → 回報。

## 裁決 ✅ / 理由
✅ 結案。commit `bb1a0d8`（v5.8.1）。卡片渲染機制已從根移除 compositing 觸發，rest 恆 opacity1。

## Root cause
study 頁每次 rating/翻卡 `rip()` 全量 innerHTML 替換 `#pageContainer`，`.study-card/.study-buttons/.study-result` 每卡並行重跑 `studyFadeIn`（opacity+transform）合成層動畫 → WebKitGTK/WebView compositor 偶發把新卡 paint 掉（有layout可選取、沒paint、rest 停opacity0）。間歇性 + 互動後恢復吻合。

## 修法
`src/styles/base.css`：移除 `.study-card/.study-buttons/.study-result` 的 `animation:studyFadeIn`（3 處屬性質）。保留 `@keyframes studyFadeIn`（`.exam-card` 用）+ `.page.active` `pageIn`。不動 JS/FSRS/OCR/DB。

## 驗證命令＋實際輸出
- `node node_modules/vite/bin/vite.js build` → `✓ built in 961ms`, exit 0。
- browser 注入 `.study-card`：`animation:name none, opacity 1, offsetWidth>0, checkVisibility true`，console 0 error。
- 真 WebKitGTK 4.1 harness（/tmp/render-harness）：BUGGY rapid-swap+resize 暫態空白反覆；持久卡住被 get_snapshot 強制重繪蒙蔽（BUGGY 60 cycles settle_blank=0）。snapshot 法無法直接看螢幕 stale 層 → 真機確認保留給 user。

## 未完成事項
- 真機（桌面 WebKitGTK + 手機）user 實機確認不再間歇消失。
- `.exam-card` 同款 `studyFadeIn` 未動（另一顆，範圍外）。
- 附帶：`tools/version.sh patch` 於 patch=0 時 `((patch++))` 回 0 被 `set -e` 中止（此顆用 `./tools/version.sh 5.8.1` 繞過）── 建議後續修。

## 執行細節
- 計畫書 `_dev/notes/U-LEARN-RENDER-fix-plan.md`；調查書 `_dev/notes/USER-BUG-LEARN-RENDER.md`。
- git 併行防護：base.css 並行首相的 scrollbar 區塊走 `git add -p` 只 stage 我的 hunks；`src-tauri/src/lib.rs`（別首相已 staged）用 `git restore --staged` 退出；commit 僅 4 檔（base.css + package.json + tauri.conf.json + Cargo.toml）。並行改動仍完整留 worktree。