# Subagent log — F14（2026-08-28, PM3）

## 任務目標
F14：`log_msg` 把前端 console 任意字串附加寫入固定路徑 `/tmp/teno-monitor.log`
（世界可寫目錄＋固定檔名＝symlink 預植 arbitrary-append 攻擊面）。
修法：`open_monitor_log` 純函式（O_NOFOLLOW per-arch 常數表＋0600）＋落點改
`app_log_dir()`。產品碼＝工作區 `src-tauri/src/lib.rs`（本 log 時點未 commit）。
計畫書 `_dev/notes/F14-fix-plan.md` v1.0→v1.4 全程凍結升版歷程見 §7。

## 審查歷程（R1/R2 前 session 紀錄見計畫書 §7；本 session＝R3/R4/R5）
| 輪 | 席次 | 裁決 | 要點 |
|---|---|---|---|
| R3 | #2 變異牙檢 | ❌ | 三逃逸實錘 M1（登記冊模式集過窄＋閉包釘空轉）/M2（`"}"` 毒彈癱瘓括號計數，runtime 攻擊證）/M3（decoy＋shadow 常數劫持 T4 鏡像，arm64 守門死碼全綠）；附 A5/riscv64 advisory |
| R3 | #3 跨平台 | ✅ | T4 兩變異體實測有牙；libc-0.2.189 逐 ABI 復證常數表；app_log_dir Android＝getConfigDir()/logs＋plugin-log create_dir_all 源碼釘；消費者窮舉閉合 |
| R4 | #2 | ❌（單項） | A1 raw-string /tmp 後门（maskEngine raw 分支無視 blankStrings→字面值層失明，38/38 綠實寫 LEAK）；七新向量六堵；白名單×10 逐員目視誠實 |
| R4 | #3 | ✅ | 五向合法演化假紅探針零誤傷；T3i-d 單點有牙；git diff 純度合規 |
| R5 | #2 單席複核 | ✅ 閉合 | 修復與處方逐字一致（pin 文字精確、引擎正反向對拍、A1 重放 RED）；advisory 三項處置認可；附 T3e 標籤誠實化建議（已採納） |

## 憲法⑩事件（本單最重要教訓）
R1→R2→R3 連續三輪同類缺陷＝靜態掃描器字符串無感知→假綠。R3 後停止打補丁，
掃描層**結構重做**：刪除式 stripper →「抹除式」位置保持遮罩（maskAll/maskLits，
等長契約、結構掃描全跑遮罩文）＋寫源登記冊逐函式誠實白名單×10＋T4 鏡像綁定
使用點。威脅模型成文（F17 先例）：防無意回歸＋已知對抗偽裝，不抵禦惡意
proc-macro 注入級攻擊。

## 驗證命令＋實際輸出（本 session 實跑）
- `node tools/verify-f14-monitor-log.mjs` → **38 PASS / 0 FAIL**（v1.4，T0 cargo
  ＋T1 真碼向量機＋T2 負控制 pin 776cb5c＋T3 結構釘×16＋T4 六 target 選值釘）
- `node _dev/notes/F14-mutation-replay.mjs` → M1/M2/M3/A1 四變異 **全 RED**＋
  正宗基線 **GREEN**（歸檔重放器随單入庫，錨點漂移會响亮報 MISS）
- `cd src-tauri && cargo test --lib f14` → 1 passed；全套 39 passed / 3 failed
  （sim_tests＝外部 fixture 缺失預存紅，非 F14 帳，R1#3 已認定）
- `cargo check` 綠；`npm run build` 744ms；`node --check` 兩腳本 OK
- 回歸 verify-f12 22/22、verify-d17 32/32、verify-d16 32/32 全綠
  （注意：android target 腿需 `export PATH=$HOME/.cargo/bin:$PATH`，
  系統 cargo 無 target 會假紅——環境非代碼，先復測再歸因）

## 首相自身錯誤誠實登記
1. 自簽釘 `names.size`（Set API 誤用於陣列）→永 abort，首跑自爆。
2. 契約釘閉合引號內多空格（`" #`）→首版釘自身失明，自修。
3. v1.4 重寫掃描層期間兩次合成 probe 文本非法（未閉合字符串）誤殺基線——
   教訓：自簽 probe 也是代碼，需先過對拍再信其裁決。

## 未完成事項
- F14-SR1（tools/cli.mjs `teno logs` 預設路徑）待總統裁示改派（他軌髒檔禁夾帶）。
- 升版（package.json/tauri.conf.json version）非 PM3 白名單→已登 scope-requests
  待波尾集中升版（G9/D10/OCR-IMP/F17 先例）。
- Android 真機鏡像檔落點（app_log_dir）人工抽驗＝使用者責任。
