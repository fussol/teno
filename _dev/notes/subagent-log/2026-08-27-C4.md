# Subagent Log — C4（2026-08-27 波次, PM1）

## 任務目標
審查 C4 修法：學習頁 interval 預覽未套 maxIvl cap（session 三 utils makeFSRS 補 FSRS 第 4 參 + ensureQueue live sync）。3 委員（修法正確性／消費者完整性／Anki 官方語意），全程唯讀、獨立重跑驗證。

## 裁決
- 委員 #1（修法正確性）：✅ PASS
  - clamp 鏈實錘：fsrs.js:259 constructor → review() :410-415/:438-439 fuzzInterval/constrainedFuzzBounds 即時讀 this.maximumInterval → live sync 必然生效
  - 與 store.js:651-656 rateCard 第 4 參逐字同構
  - **獨立負控制**：/tmp 副本剝除修法實跑 → 15/37 PASS、22 FAIL exit 1（T0-T6b 大面積紅燈，T1 預覽 GOOD=1022d vs cap 365 = bug 原形）→ 負控制真實有效
- 委員 #2（消費者完整性）：✅ PASS
  - 全庫 `new FSRS(` 窮舉 13+ 構造點：session×3（本 fix）✅、store×3 ✅ 原有、session-v4 fallback 零觸發、fsrs-optimizer 不觸及 cap、deprecated 零 import、cli.mjs:133 ❌→scope-request（併 E4）、cli 2176/2483 已有 cap、simulator.js 用 clamp 正確
  - maximumInterval 讀取路徑唯一（computeIntervals→review），一處 sync 全覆蓋
  - 三學習頁 ensureQueue 恒傳 storeState 實錘；_maxIvl 死代碼零消費者實錘
- 委員 #3（Anki 官方語意）：✅ PASS
  - 上游 ankitects/anki@main sparse clone 實錘：按鈕標籤=排程狀態同一物件（answering/mod.rs:259-336），maximum_interval 單源 clamp（states/mod.rs:121-124, fuzz.rs）
  - Math.max(1,…) 對 0/負值 = Anki `maximum_review_interval.max(1)` + 官方 test（states/mod.rs:213-218）同義
  - live sync = Anki 每張卡現取 config 語意在 teno singleton 架構下的正確對映
  - fsrs.js 預設 36500 = Anki 出廠值（deckconfig/mod.rs:52），不動它屬 API contract，論證正確

## 不阻斷觀察（登記後續）
1. cli.mjs:133 缺 cap → 已登 scope-requests.md C4-SR1（併 E4）
2. ensureQueue `session.running` 早退在 sync 行前 → session 進行中改 cap 需重進學習頁才生效（與既有 newPerDay 同模式非新增回歸）；計畫書風險節已補註
3. session-spell-utils.js:49 `maxIvl:` 幽靈參數（constructor 未解構）— 既有死代碼，非本 diff 引入，下次順手清
4. F-4：settings.js UI max=3650 vs Anki 36500 — 範圍外

## 驗證命令＋實際輸出
```
$ node --experimental-test-module-mocks tools/verify-c4-maxivl-preview.mjs
C4 verify: 37/37 PASS ALL PASS        (exit 0, stderr 乾淨)

$ ./node_modules/vite/bin/vite.js build
✓ built in 762ms                      (exit 0)

回歸：verify-c3-rating-lock ALL PASS / verify-a6-sort 19 pass 0 fail /
verify-a9-duration-cap ALL PASS / verify-b9-progress 59 通過 0 失敗
node --check：session-utils/mc/spell 三檔 OK
```

## 未完成事項
無（本顆循環完整）。觀察項 1-4 已登記不遺漏。
