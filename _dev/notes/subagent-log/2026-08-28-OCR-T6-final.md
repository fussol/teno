# PM-OCR2 · T6 收尾 — 全流程驗證＋全套回歸＋OSV 核銷（2026-08-28）

接班軌最終檢查點。T4（ui）＝上一 commit、T5（importOcrText）＝上一 commit、
本檔收 T6。計畫 v1.3 P1（電腦端）範圍全部落地。

## T6 交付
- `tools/verify-ocr-persist.mjs`（新）：ocr_engine 設定往返＋引擎解析鏈
  （真 db.js 層＋node:sqlite 真 SQL）8/8 ALL PASS
  P1 setSetting→getSetting 往返／P2 未設定→null→現讀預設 tesseract／
  P3 paddle 落庫→佔位不可用→執行回退 tesseract 且設定值保留（下次辨識現讀）／
  P4 幽靈 id 髒資料落庫→回退不崩／P5 覆寫 ON CONFLICT 收斂／
  NC db 層讀取炸→回退鏈非崩潰（整合態，補 engine E3 单元態之上的端到端）。
  註：node 無 Worker/document → 真 tesseract-adapter available()=false（E7
  正確語意），本腳本以 fake 頂位 tesseract（同 T3 E 系慣例）、paddle 留真佔位。

## Browser 全流程內測（dev:5199 真跑）
- 上傳→辨識→勾選→confirm：journey/example 真辨識→勾選清單→
  confirm 觸發 importOcrText→無 DB 環境優雅 toast「DB not connected…」、
  候選保留可重試（不崩、不清場）
- busy disable：dispatch 後同步探針 capBtn.disabled=true＋「辨識中...」；
  完成後恢復（btnReEnabled）
- 引擎選單：cs-trigger→選 paddle→**captured console**「[ocr] engine set: paddle」
  （console.log wrap 抓獲，change 監聽器實證觸發）；選回 tesseract 收斂
- 重載持久化（無 DB 態）：切 paddle→reload→優雅回退預設 tesseract、
  console 零新增 error。DB 層持久化正向段由 verify-ocr-persist P1~P3 釘死。
- 負控制 A（§7）：全黑圖→「未偵測到有效單字」、零候選、不拋（T4 已測，本轮確認）
- 學習主流程冒煙（T5 已測）：OCR 入庫詞自動入佇列→翻卡→評分

## 全套回歸
- 六支 OCR 腳本：assets／engine／ui／import／persist／imp 全 ALL PASS（FAIL=0）
- 既有抽跑：d19 32/0、d6 14/14、c3 連按 ALL PASS、a10 ALL PASS、b6 72/0
- npm run build 744ms 綠

## OSV 核銷（T2 登記項）
GHSA-83rx-c8cr-6j8q（tesseract.js Insecure Default Configuration）：
一手查 OSV API——受影响範圍 **<1.0.19**（2019 crossorigin.me 代理預設），
fixed 1.0.19。本專案 7.0.0 远在範圍外；且執行模式為純離線
（workerPath/corePath/langPath 全本地＋cacheMethod:'none'，零網路請求）。
**使用面零影響，結案。**

## Desktop 實跑嘗試（誠實登記）
試跑 target/debug/teno（XDG_CONFIG_HOME/XDG_DATA_HOME 全隔離→隔离 DB 建於
/tmp，生產 teno.db 零觸碰 ✅ 隔離有效）：debug binary 無 devUrl 連線
（ss 無 5173 ESTAB，載入編譯期內嵌 dist），native GTK 檔-picker 自動化需
ydotool daemon＋視覺迴圈，不可靠且超出本軌能力圈 → 終止程序＋清隔離目錄。
**「desktop 真機：上傳→辨識→入庫→重載持久化」＋「Android 實機（Samsung A55）」
＝使用者待辦**（計畫 §8 P2 驗收標準同款）。

## 三軌總結（T4/T5/T6）
| commit | 內容 | 驗證 |
|---|---|---|
| T4 feat(ocr): ui | OCR 區塊＋引擎選單＋camera 圖標 | verify-ocr-ui 26/26＋browser 實跑 |
| T5 feat(ocr): importOcrText | §6.2b 唯一入庫路徑 | verify-ocr-import 16/16 雙態＋confirm 轉綠 |
| T6 test(ocr): persist | 設定往返＋引擎鏈釘 | verify-ocr-persist 8/8＋全套回歸 |

範圍外（既有登記）：剪貼簿貼上/bbox 高亮/信心閾值（P3）、真機兩項（使用者）、
無 DB 態 state 記憶體污染（importWords 既有基線）、worker 退出主動 terminate（P3）。
