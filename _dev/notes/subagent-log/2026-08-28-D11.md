# 2026-08-28 D11 — drive_download 零內容驗證（PM4）

## 任務目標
修 D11：drive_download 下載位元組無任何驗證直寫覆蓋 teno.db＋拆 WAL/SHM →
空檔/損壞檔/垃圾檔直接毀本機資料庫。
計畫書 `_dev/notes/D11-fix-plan.md`（v1 送審凍結）。

## _code 事實查證（動工前）_
- 行號漂移實錘：佇列說 :296-303，實際 drive_download 在 :371-395（F11/D9 修復後漂移），計畫書已註明實際行號（鐵律⑥）。
- lib.rs:559-609 `unpack_db_container`（crate root 私有 fn）：非 TENOC 資料走「雙 magic 皆不中→Err」天然斃空檔；TENOC 容器分支直接切內段**不驗內段 magic**＝殘洞（計畫書 §1 雙判補釘封之）。
- 可見性：drive_sync 是 lib.rs（crate root, teno_lib）之子模組（lib.rs:9 `mod drive_sync;`），Rust 子模組可見 parent 私有項 → `crate::unpack_db_container` 合法。委員最小 crate 實編證明＋本倉 cargo test 一跑即綠雙錘。

## 修法
1. drive_sync.rs 新增純函式 `validate_drive_download(buf) -> Result<Vec<u8>, String>`：unpack_db_container 雙態解析＋db 段 >=100B+SQLite magic 雙判（與 CLI import 同判別子，D19 同源）；錯誤訊息「本機資料未變」。
2. drive_download 委派：守門先於 write tmp／拆 WAL／rename——拒絕路徑零副作用。
3. 寫盤改 `fs::write(&tmp, &db_bytes)`（解包後內段，TENOC 來源寫盤保持裸 SQLite）。
4. mod tests 新增 `d11_validates_drive_download_forms` 七形態（空/垃圾拒、裸恆等、TENOC 解包、截斷拒、內段垃圾拒、99B 拒）。

## 委員裁決（1 席，簡單 bug 四標準：單檔/非共享/低風險/產物碼 <20 行）
- 輪次：1 輪 1 人次 ✅ 准（0 阻斷 / 0 次要 / 3 nit）。
- 委員實測：可見性最小 crate 實編 VIS_OK；lib.rs:559-609 逐字核對屬實；round-trip 恆等閉環（drive_upload 裸檔 :357-364 實錘）；提取器不受工作區未 commit hunk 干擾（diff hunk 最近止於新側 550，未觸 559-609）；T4 反換正則嵌套大括號實測恰切函式尾；>=100 距真實 DB（7.9MB）/SQLite 頁下限（512B）無誤殺空間。
- nit 吸收：nit-1 PATH 雙斜線已修；nit-2 行號 1-2 行漂移登記（§Bug 定義段框為準）；nit-3 log 長度欄缺失靜默忽略已在守門註解登記無害論證。

## 驗證（tools/verify-d11.mjs 先行實跑，法律④）
- 送審前 PRE 態：10/10 PASS（bug 在場四釘＋基線未搶跑釘）。
- 動工後 POST 態：16/16 PASS。含：
  - T1 源碼釘（委派在場＋順序釘 守門<寫tmp<拆WAL＋舊 &buf 零殘留）
  - T2 倉內 cargo test drive_sync 7/7 綠＋d11 測試名釘
  - T3 真碼提取 rustc 微編譯七腿（含 TENOC 內段垃圾殘洞釘）
  - T4 負控制＝守門換直通版→空檔直入必復現（測試有牙反換釘 ncImpl!==implFixed）
  - T5 基線釘 pin 9e3116b（F9 教訓：負控制防腐化，commit 後恆常有效）
- 驗證腳本自身 bug 誠實登記：harness `Vec<u8>.into_bytes()` 誤用＋提取 slice off-by-one（`vj+2` 應 `vj+3`，`indexOf('\n}\n')` 起點语义）→ T3b/T4a 首跑紅，兩發修復後全綠。修法本體碼零返工（一跑即綠）。
- 回归：verify-f11 29/29（同檔零回歸）＋ verify-d19-import-magic 32/32（magic 判別子同源）＋ verify-e4-rate-log 24/24 ＋ npm run build 747ms OK。
- 嚴禁連 Google：全程零 Google API 呼叫（純靜態/單元/微編譯）。

## 未完成/範圍外
- secret 輪換＋舊 token 撤銷（任務書明示範圍外，交付清單）。
- 下載源為「有效但更舊」DB 的 last-write-wins → D10 域（本佇列下一顆）。
- Drive 端垃圾檔產生源（同步上游寫壞）非本檔職責。
- drive_download 呼叫鏈前端（settings.js Drive 區塊）零改動需求——回傳仍是 Result<String,String>，錯誤字串 UI 直接顯示，無需 scope-request。

## 交付
- commit：`fix: D11 …`（drive_sync.rs＋tools/verify-d11.mjs＋D11-fix-plan.md＋本 log）
