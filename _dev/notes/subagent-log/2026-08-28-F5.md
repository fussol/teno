# F5 subagent 紀錄（整合）— 2026-08-28

## 任務目標
修 IconPlugin.kt（實際路徑 `src-tauri/gen/android/app/src/main/java/com/teno/app/IconPlugin.kt`）：
① resolve=ResolverActivity 時清理不執行（死鎖環）② DEFAULT 態一刀切誤判（PM 擴充）
③ getCurrentIcon fallback 對 ResolverActivity/DEFAULT 瞎猜。基線 ace9306（F4 commit）。

## 誠實登記（本 session PM5-5）
R1 三席（✅❌✅）發生於前一 PM5 session，**各席獨立 md 未落盤**（預算中斷，鐵律⑧違反既成事實）；
R1 全部處方與裁決理由已即時寫入計畫書「版本紀錄」v1.1 段（逐項可查），本檔整合補齊總账。
本 session 僅做：複核現場（雙態/編譯/回歸全重跑）→ 委派 R2 單席 → R2 建議吸收（僅動驗證腳本）→ commit。

## 審查歷程
- R1（3 席，前 session）：#1 ✅／#2 ❌（四必須項：T3 窗口 500→200 真 guard 釘、T2 load running
  分支＋三元定義釘、否定式閘/early-return 結構釘、SPEC S3 live 模型）／#3 ✅（兩文字建議）。
  處方全數 v1.1 完成（計畫書版本紀錄逐條）。
- R2（1 席 c1，本 session，F4 判例：複核僅驗處方落實＋攻擊重現）：**✅**。
  處方六項逐點落實；d/c/b/a 四攻擊各收斂單點紅（T2/T2/T3/T5）且觸發斷言與設計意圖逐字一致；
  gradle --rerun-tasks 66/66 真編譯綠。新攻擊 e/g 逃網→定級非阻斷＋附補針建議。
  詳報 `2026-08-28-F5-R2-c1.md`。

## 驗證命令＋實際輸出（本 session 重跑證據）
```
$ node tools/verify-f5-icon-cleanup.mjs                      # PRE 釘基線
  T0✔ T1✘ T2✘ T3✔ T4✘ T5✘ T6✔ NC1✔ NC2✔  fail 4, exit 1（紅集=bug 事實）
$ SRC=<worktree IconPlugin.kt> node tools/verify-f5-icon-cleanup.mjs   # POST
  9/9 全綠, exit 0
$ ./gradlew :app:compileArmDebugKotlin --offline --rerun-tasks  (JAVA_HOME=~/jdk21)
  BUILD SUCCESSFUL（66/66 tasks，R2 席重跑；本席 --rerun-tasks exit 0）
$ node tools/verify-f4-icon-restart.mjs  → 11/11 EXIT=0
$ node tools/verify-tts-contract.mjs     → EXIT=0
$ node tools/verify-g9-tts-fallback.mjs  → EXIT=0
$ npm run build                          → ✓ built in 784ms
```
R2 補針吸收後（v1.2，僅動腳本）：
```
POST 9/9 綠；PRE 5/9 紅集不變；
attack-e.kt → 僅 T2 紅（單點收斂）；attack-g.kt → 僅 T4 紅（單點收斂）；
attack-{a,b,c,d}.kt 紅域不變（red=3 行=單 test＋摘要）。
```

## 未完成/範圍外
- 真機 e2e 不可行（Android），以靜態釘＋規格決策表＋真編譯閘代替（計畫 §4 既定立場）；
  真機冒煙＝使用者責任（F17 同款登記）。
- R1 登記不修項：受限自癒邊界、~40 次 binder 讀快照化、launcher 第三方快取延遲。
- settings.js:823 投機寫入 DB（JS 域另單）；setIcon 區塊（F4 域已修）。
