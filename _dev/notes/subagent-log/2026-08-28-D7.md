# D7 委派紀錄（2026-08-28，首相2／PM2）

## 任務目標
CLI 覆寫 teno.db 前不刪 `-wal`/`-shm`（殭屍 WAL 混入新主檔＝未定義行為）。
佇列主因 `cmdRestore`，實為類缺陷：**四條覆寫路徑中三條有病**——
`cmdRestore`（無備份無 rmWal）、`cmdBackups` restore 分支（Discord bot
/restore 實際路徑，缺 rmWal）、`cmdDrive` download 分支（R1 審查新增，
Rust 鏡像 drive_sync.rs:360-364 remove wal/shm 同源鐵證）；`cmdImportDb`
正面教材（本地 rmWal 頂層化共用）。

## 委員裁決
### R1（3 名）：#1 ❌ / #2 ✅有條件 / #3 ❌ → v1.1
- **#1**：逮第四條路徑 drive download（Rust 鏡像同源）；bot 鏈路實錘
  （bot.py 僅 backup/dash/backups restore 三呼叫點）；backupDb 時機正確、
  平面拷貝撕裂＝既存語意判定。
- **#2**：五面 ✅ 但阻斷級：bak regex `\d{14}$` 對真實分鐘戳
  （ISO slice(0,14)→`YYYY-MM-DDHHMM`）永不到達＝T2c/T2d 永紅＋T5c 虛斷言；
  rmWal snippet 必須含 per-file try/catch；假 HOME 生效性／Linux ro handle
  unlink 零妨礙／prune 兼容全部實測背書。
- **#3**：變異牙檢 A–E 有牙（漏 backups 雙牙、漏 backupDb 三牙、備份錯序
  T2d 獨紅、漏 -shm 三連紅）；rmWal 錯序 runtime 無牙→契約字面量為唯一
  enforcement（P4 登記）；P1 regex／P2 契約凍結進計畫書／P3 T5a 反空洞／
  P5 防 function 式雙定義。

### R2（#1、#3 複審）：全 ✅ 放行
- #1：drive 補列閉環實測到位；切片錨點唯一性實錘。
- #3：fixclean 四步逐字 ×v1.1 → 18/18 零摩擦；mutF 恰紅 T1f；mutB 錯序契約
  精準攔截＋T2d 意外實牙；無假綠逃逸路徑。

## 驗證命令＋實際輸出
```
$ node --check tools/cli.mjs                 → SYNTAX-OK
$ node tools/verify-d7-restore-wal.mjs       → 18 PASS / 0 FAIL — ALL PASS（×2 穩定）
  （修前基準：9 PASS/9 FAIL，紅集＝修法釘、T5 負控制群全綠精準重現）
$ node tools/verify-d6-import-order.mjs      → 14/14 ALL PASS
$ node tools/verify-e4-rate-log.mjs          → 24/24 ALL PASS
$ node tools/verify-e8-selftest.mjs          → 18/18 ALL PASS
$ node tools/verify-db-version-trace.mjs     → ALL PASS
```
（vite build 結果補記於 commit log）

## 未完成／範圍外（詳計畫書 §6）
- backupDb 改 checkpoint 快照（export-db :2163 範例）跟進單。
- drive download tmp→rename 對齊 Rust。
- DB 不存在頂層 crash edge、cmdBackups 死正則 :3230、api.db 過期 handle、
  殭屍 wal 毒理（SQLite 未定義行為不賭）。
- SR-C4 hunk commit 前反剝事後還原（既定程序）。
