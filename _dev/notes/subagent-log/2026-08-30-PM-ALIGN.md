# PM-ALIGN 對齊任務 log（2026-08-30）

- 任務：字本瀏覽器 browser.js 與字庫瀏覽器 deck-browser.js 兩套新增/編輯 modal 對齊＋直覺化。
- commit：`41c9e6c`（v5.7.0 → 5.8.0，version-gate OK，三檔一致）
- 審查：本任務為跨檔 UI 對齊（動 code），依 mission 驗證門執行，未開獨立審查委員。

## 改動
| 檔 | 行 | 內容 |
|---|---|---|
| deck-browser.js openAddModal | 字本欄位 (原405) | `readonly input` → `<select id=deckAddDeck>`，可切換字本。預設當前字本 `_deckName`，全選視圖 fallback `Default`，兜底合成缺失項目（curDeck 必在選項內且恰一個 selected）。 |
| deck-browser.js openAddModal | deckAddWord (369) | 補 `autofocus`（開 modal 即聚焦單字欄）。 |
| deck-browser.js openAddModal/openEditModal | 兩個 `_tagInput` keydown Enter (465/870) | 補 `e.stopPropagation()`，與 browser 一致。 |
| browser.js openModal | fWord (746) | 補 `autofocus`（新增/編輯開啟即聚焦）。 |

## 覆核實錘
- mission 稱 deck-browser 新增 modal 詞性 chips「無任何 selected 處理」→ **stale audit**。實錘：deck-browser.js openAddModal 已有 `_posCN`/`_normalizePos`/`_selectPosChips`/`_getPosVal`(492-514)＋`deckAddPosGroup` click handler 切換 `.selected`＋accent 樣式，功能與 browser 已對齊，視覺一致，未再動。
- deck-browser openEditModal 字本本就 `<select>`(808)，僅其 `_tagInputEdit` 缺 stopPropagation → 一併補齊。

## 直覺化優化（擇優）
- 開「新增單字」即 `autofocus` 到單字欄（兩檔新增流程），減少一步滑鼠點擊。
- 未抽共用 `_tagInput` helper：三份實作近乎相同，但抽取為跨檔 lib 共用屬中風險重構，需重驗 3 呼叫端＋fill 流程一致；本任務紅線「不大重構」＋離線自主回歸成本高 → 明確不做（憲法⑦可選項定案），留待未來。

## 驗證（內測門）
- `node --check` browser.js / deck-browser.js：PASS
- dev server（npm run dev, localhost:5173）HTTP 200；vite 轉換後 curl 兩檔源含改動：
  - browser.js: `autofocus`×1
  - deck-browser.js: `autofocus`×1、`deckAddDeck`×3、`deckOpts`×2、`e.stopPropagation()` 共 10（含既有），`readonly` 消退
- `npx vite build`：exit 0，僅既有 chunk 大小警告（非本次）
- deckOpts select 邏輯 5 場景（node script）全 PASS：當前字本 / 全選→Default / 無Default→首字本 / 缺字本→Default / 空字本→合成。每場景恰一 selected 且 curDeck 在選項內。

## 治理修正（重要）
- 首次 commit（2870a6b）誤掃入他人預 staged 的 `src-tauri/src/lib.rs`（F10 piper Android SAF import 修復，屬 SR3 領地，非本次）。原因：commit 提交整個 index，我用 `git status --short <我的檔案>` 篩選檢視而漏看預 staged 檔。
- 回收：非破壞性 `git reset --soft HEAD~1` → `git restore --staged src-tauri/src/lib.rs` → 重 commit（41c9e6c）只含我的 5 檔 → `git add` 還原 lib.rs 為原本 staged 狀態給原 PM。內容零損失、未 push、未 destroy。
- 教訓：commit 前**必跑完整 `git status`**（非篩選），揪出預 staged 外來檔；`git commit` 提交整個 index，非僅本次 `git add` 者。

## 範圍外
- 未動 OCR / FSRS / DB schema / store actions / 共用 lib。僅兩個頁面檔＋三版本檔。