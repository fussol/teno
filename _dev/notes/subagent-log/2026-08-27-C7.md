# C7 審查紀錄 — 2026-08-27（PM1）

## R0 送審 — 未遂（基礎設施故障，4 次委派全失敗）

### 已送出的委員任務（3 名，全部未執行）
- #1 修法正確性：RC1/RC2/RC3 行號與機制核實＋/tmp/c7rev1 親套修法雙向重跑
- #2 消費者/時序完整性：`_undoSnapshot`/`_completionShown` 消費者窮舉、handler 生命週期、跨模块污染、C1/C3 相容性
- #3 驗證方法學：verify-c7-undo-gate.mjs 真實度/恆真斷言搜尋/覆蓋缺口＋/tmp/c7rev3 三態重跑

### 失敗實測（實際輸出）
```
Error during OpenAI-compatible API call #34/#39/#50/#55:
Model qwen2.5-coder:7b has a context window of 32,768 tokens, which is below
the minimum 64,000 required by Hermes Agent.
```
（agent.log 錘：`[20260827_204354_b9794a] ... OpenAI client created ... provider=custom base_url=http://localhost:11434/v1 model=qwen2.5-coder:7b` — PM2 session 子代理同款死法，非 PM1 獨有）

### 根因（已查明）
共享 `~/.hermes/config.yaml` 的 `delegation` 段在今日被污染為 ollama（`base_url: http://localhost:11434/v1` + `model: qwen2.5-coder:7b`，旁證 `config.yaml.bak-20260827-e5`）。首相進程啟動時快取髒值；`hermes config set` 探针（probe-xyz-999）實測對運行中進程**無效**（`delegate_tool._load_config` 優先讀進程內 CLI_CONFIG）。

### 處置
- delegation 已釘死 `model: deepseek-v4-flash / provider: opencode-go / base_url: ''`（2026-08-27 23:1x）
- 本 session 依鐵律⑫收尾；runner 下一輪新 session 直接開委（交接見 PM1-checkpoint.md）
- 嚴禁動工：修碼 0 行落庫，HEAD 仍 3cca336；送審凍結物＝C7-fix-plan.md v1.0 + verify-c7-undo-gate.mjs（未修碼雙向 24/24 ↔ 15 FAIL 實錘）

---

## R1 送審（v1.0，3 委員，delegate_task leaf 全成功）— 裁決 1✅/2❌ → 未過審

### #1 修法正確性 ✅（附驗證腳本缺陷呈報）
- RC1-RC3 行號機制逐處對齊 code 事實（session-utils.js:219/:193/:180、mc:240/:214/:197、spell:231/:201/:187）；mount 早退前三行 `keyCleanup()` 先清舊 handler 實錘「完成畫面零 handler」。
- 放寬 gate 安全性逐鍵核對成立（flip space/1-4/p、mc 1-8、spell 1-4/Enter/[ 全部各有 state gate；完成態唯一活鍵 Ctrl+Z）。
- /tmp/c7rev1 親套 9 處修法 → **22/24**：全部修法意圖斷言（T1/T2/T3/T5/T6/T7）轉綠，唯 **T4×2 FAIL** 實錘＝腳本 T4 前置假設缺陷（執行點快照非 null，斷言「不 undo」與修法目標矛盾）。獨立 probe 證真 null 態下修法 no-op 安全屬性成立。
- 三 RC ablation（rc23/rc13/rc12）各自獨立必要。

### #2 消費者/時序完整性 ❌
- 消費者清單窮舉 ✅：`_undoSnapshot` 僅三檔（store.js `_undoSnapshots` 為 C1 另一符號非消費者）；`_completionShown` 僅三檔 ensureQueue；undoRating 呼叫端 6 處；handler 生命週期無洩漏無雙綁定（mount 頭 keyCleanup＋__pageCleanup＋renderPage 必調鏈）。
- 跨 mode 隔離 ✅：真實 App pageCleanup 致 foreign handler 不可能並存；undoLastRating mode 選槽雙重隔離。
- C3 相容 ✅：in-flight 期間 state 恆 ANSWER，舊 gate 本已放行 in-flight undo → C7 未擴大暴露面；「不加 !_ratingLock」定案成立（C8 域）。
- **❌ 主因**：T4 修法後必假紅（實測 22/24，計畫書宣稱 24/24）＋harness stale handler 靠 T4 誤 undo 湊巧掩蓋＋T6 mode 斷言自參考。附精確處方（T4 移至真 null 態＋全序列斷言＋模擬 pageCleanup）。

### #3 驗證方法學 ❌（變異體牙檢＋親測處方）
- 變異體牙檢（v1.0 腳本）：A 收回 RC1 gate→12 FAIL、B 收回 RC2→4 FAIL、C 刪 RC3→T3 紅——三 RC 皆有獨立辨證斷言，負控制有牙，予以保留。
- **❌ 阻塞**：T4 是「測不到聲稱行為」壞斷言（在舊碼綠是「為對的理由而綠」：等到的其實是 handler 不存在/舊 gate 擋死場景）。
- 處方（於 /tmp/c7rev3 親測雙向全綠）：T4 移至 T2 undo 後、T3 重評前（快照確定 null）＋每相位獨立 document（mkDoc 重建 jsdom）消 stale handler。
- 覆蓋缺口（不阻塞但須補）：RC2/RC3 僅 flip 測過，mc/spell 零斷言。
- 次要註記：T6 legacy 自參考斷言；tick(80) flaky 低風險；store.init SQL 噪音兩態同等。

## v1.1 修正（僅動驗證腳本，產品碼修法零改動）
1. T4 重排至 T2 後、T3 前＋註解記錄 v1.0 缺陷根因。
2. `mkDoc()` 相位級 document 隔離：resetState() 首行重建 jsdom，pressUndo() 派發 globalThis.document。
3. 覆蓋補齊：T6 補完成首屏 ensureQueue＋RC3 鏡像＋零跨 mode 序列斷言；T7 補 RC2（drain→完成 mount→undo）＋RC3 鏡像＋零跨 mode；T6 mode 斷言改絕對位置 `undoCalls[u6b]`。斷言 24→32。
4. 三態實跑（HEAD=2baa519 未修態）：正常 19 FAIL exit 1（紅集 T1×3,T2×2,T3,T5×3,T6×5,T7×5＝辨證全集，T4 恆綠無誤報）；`--expect-legacy` 32/32 exit 0；/tmp/c7v11 修法副本 32/32 exit 0。

## R2 送審（v1.1，2 委員＝R1 ❌ 兩席複審）— 裁決 全 ✅ 過審

### #2 複審 ✅
- 三條發現逐一落地反證成立；三態與計畫書逐字吻合；變異收回 RC1→16 FAIL（新增 T6/T7 斷言全轉紅，牙未鈍化）、收回 RC3→恰好 3 FAIL 乾淨紅集；T4 兩態恆綠理由不同之正當性採納（安全回歸性質，判歧由 T2 承擔）。

### #3 複審 ✅
- 四項處方逐字落地核對全中；三態各 5 連跑＋變異各 3 連跑零非確定性；變異 A(16)⊇{T1,T6,T7}、B(9)⊇{T2,T6完成,T7完成}、C(3)=恰{T3,T6RC3,T7RC3} 可唯一定位三 RC；新斷言無鈍化無恆真。

## 動工與驗證（R2 全✅後）
- repo 套 9 處修法：python 逐檔三段替換＋assert（防部分套用），`git diff --stat`＝3 檔 9 insertions/6 deletions，每處 `// C7:` 標記。
- `tools/verify-c7-undo-gate.mjs` 正常模式 **32/32 ALL PASS exit 0**（負控制三態於送審段已實）。
- 回歸：verify-c3-rating-lock / c1-undo-modes / undo-cycle / next-after-undo / c5-review-cap / c6-dashboard-due / a6-sort / c4-maxivl-preview **八項 exit 0**；node --check 三檔；vite build 703ms。

## 未完成事項
- 無（本顆閉環）。C7-SR1（完成畫面 undo 按鈕 DOM，PM8 域）已登 scope-requests.md 待總統裁示；C8（in-flight undo race）佇列下一顆。
