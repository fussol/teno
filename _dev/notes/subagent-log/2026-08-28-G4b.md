# G4b 紀錄 — 里程碑/學習訊息終生制＋持久化

## 任務目標
G4（0bb9842）姊妹案：`checkStudyMessages`/`checkMilestone` 吃 `session.results.length`
（單 session 計數）→ 生涯 500 卡但本 session 評 3 卡 → 500 里程碑不出；
`_lastMsgAt`/`_milestonesShown` 模組級記憶體 → 重啟歸零重播。
修法：計數源改 `_totalRated`（生涯累計），進度態 `_eggsShown` 持久化 localStorage。
計畫書 `_dev/notes/G4b-fix-plan.md` v1.2（§8 完整審查紀錄）。

## 審查歷程（R1/R2 於前一 session 進行，紀錄同計畫書 §8）
- R1 三委員：#1 修法正確性 ❌→v1.1（發現 A 跳頂語意未明文／發現 B 無痕降級補登）；
  #2 消費者契約 ✅（呼叫面恰 engine 兩行＋main.js:368 零交集；resetAll 走
  localStorage.clear() 新鍵自動納入；發現 restoreBackup 不清 LS 既存缺口→§6 另單）；
  #3 測試品質 ✅ 有條件（F3 簽名留預設參漏網→T4b 釘、T8 計數同形→文字釘、safe() 隔離）。
- R2 #1 複審 v1.1：❌ 缺項 2（§3.3 觸發雙條件歧義殘留＋中間帶探針缺），
  「補文一句＋釘一條即 ✅」預授有條件放行。
- v1.2 響應：§3.3 雙條件明文（total >= m.at && m.at > lastMsgAt）＋T9 中間帶探針釘上裝；
  辨證力實錘＝/tmp 無守衛變體恰紅 T9 兩條、守衛版全綠。依 R2 預授條件放行。

## 本 session 執行（2026-08-28，PM1 接手）
### 接手點實錘
- 計畫書 v1.2 已過審、驗證腳本就緒、產品碼未動（easter-eggs.js 舊態 :12/:24 記憶體態確認）。
- 修前基線：normal 模式恰 16 FAIL（紅集＝辨證斷言全集，恆綠釘零誤報）、
  --expect-legacy（HEAD 碼 /tmp/g4bnc 副本）ALL PASS。

### 動工（計畫書 §3 逐字執行）
- `src/lib/easter-eggs.js`：刪 `_lastMsgAt`/`_milestonesShown`；新增 `lifetimeRated()`
  （parseInt＋isFinite＋try/catch 回 0）、`readShown()`（非物件/壞 JSON/欄位缺全降級預設）、
  `writeShown()`（try/catch 隔離）；`checkStudyMessages()` 去參數＝降序最高已達節點一跳＋
  lastMsgAt 跳頂持久化；`checkMilestone()` 去參數＝升序逐條補放（每呼一條）。`// G4b:` 標記。
- `src/engine/session-utils.js` :128-129 呼叫點去參數。

### 驗證（實際輸出）
```
$ node --experimental-test-module-mocks tools/verify-g4b-milestone-persist.mjs
=== G4b  ALL PASS ===            (×2 連跑)
$ (cd /tmp/g4bnc; … --expect-legacy)   # HEAD 負控制
=== G4b 負控制 ALL PASS ===
$ for t in c3-rating-lock c7-undo-gate c8-undo-race c9-goal-undo-race c10-lock-finally;
  node … EXIT=0 ×5
$ vite build ✓ 688ms
$ verify-g4-total-rated.mjs → ALL PASS（前單回歸）
node --check ×2 ok
```

## 未完成／範圍外（計畫書 §6）
- `_achievements` 讀端裸 JSON.parse 防呆（C10 §6 域，另單）
- checkAchievement 裸讀 localStorage 無 LS 環境必拋（既存，easter-eggs 健護單）
- restoreBackup 還原後 LS 鍵與 DB 不同步（另單）
- undo 重評 `_totalRated` 高估（G4 v1.1 已登記接受）
- 成就系統 DB 化（大改另案）
