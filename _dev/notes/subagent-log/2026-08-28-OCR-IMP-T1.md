# PM-OCR2 · T1 `fix: OCR-IMP` — importWords 幽靈入庫修復（2026-08-28）

## 任務目標
照 OCR-plan-v1.3 §6.2a：store.js importWords 於 saveWord 拋錯 ROLLBACK 後仍回傳
`added>0`（幽靈成功）。修法 = `txFailed` 旗標 → `if (txFailed) { added = 0; }`。
skip/deck 計數不受波及。消費者穷举：`src/pages/import.js:550`（只讀 `.added` 顯示）、
importOcrText（T5 將建，同一簽名）。零簽名變動。

## 實碼錨定（行號實錘）
- store.js importWords @ :1214；事務段 :1268-1278（計畫書宣稱 :1269-1282，漂移 ≤2 行，同一 block）

## 四層內測門
### 1. 靜態
- `node --check src/lib/store.js` ✅ / `node --check tools/verify-ocr-imp.mjs` ✅
- `npm run build` ✅（705ms，僅既存 chunk-size warning）

### 2. 雙態驗證 `tools/verify-ocr-imp.mjs`（真實 store + 真 SQLite node:sqlite，FakeDatabase 於 execute() 攔截 INSERT INTO words 模擬寫入中斷）
- **PRE（修法前）**：`EXIT=2` — T0a/b/c 靜態釘 FAIL ×3 ＋ **T1 幽靈再現 `got=3 expect=0`** ＋ NC 錨找不到（合理：修法未套用）
- **POST（修法後）**：`ALL PASS 12/12`
  - T1 失敗事務 added=0／skipped=1 不受波及／DB 實查零新行（words 仍 2）
  - T2 成功路徑 added=3／DB 實查 5 行／decksCreated 含 OCR Inbox（零回歸）
  - T3 負控制：剝除 `if (txFailed){added=0;}` → **added=3 幽靈精準再現**＋DB 實查仍 2 行（回報≠落盤＝敏感）
  - T4 NC 反換釘：NEG 成功路徑 added=1（NC 只打失敗態）
- 證據：/tmp/ocr-imp-PRE.log / /tmp/ocr-imp-POST.log

### 3. Browser 內測（vite dev :5199，實際載入與點擊）
- 頁面載入（Teno - Era 5）：console 零 error、js_errors=0
- **學習主流程冒煙**（共享檔 store.js 加測）：注入 3 字 3 卡（純 browser 無 Tauri DB，
  state 層 in-memory）→ 點「學習」→ 選 flip 模式 → queue 3複 → 「顯示答案」→
  評分 Good 4d → 斷言 `reps 0→1, state=2, scheduledDays=4, due=2026-08-31,
  lastReview=true, reviewLog=1` ＋ UI 自動切下一張 ✅
- 匯入頁（importWords UI 消費者）render：CSV/TSV、Quizlet 按鈕、選擇檔案可點 ✅
- 失敗路徑直調 importWords：拋 `DB not connected`（requireDB @ ROLLBACK 行）—
  **既有行為，修法前後一致**（見範圍外）

### 4. 證據
- 本檔 ＋ subagent-log 目錄 ＋ git commit

## 範圍外／既有發現（誠實登記，未擴權）
1. importWords catch 內 `await db.executeSQL('ROLLBACK')`：DB 完全斷線時 ROLLBACK
   二次拋錯 → importWords 整體 reject（PRE 同行為，非本次引入）。計畫書 diff 未涵蓋，
   不動；若元首要修需另案。
2. 負控制 harness 修正：resetState 需清 state.decks，否則上輪 createDeck 污染
   decksCreated 斷言（T2 假紅根因，屬腳本 bug 非產品 bug）。

## 回歸（抽跑既有 verify）
- d6-import-order 14/14 ✅（其「FAIL」5 條為負控制標籤非真失敗）
- d19-import-magic 32/0 ✅
- c1-undo-modes 21 pass / 0 fail ✅
- next-after-undo 全綠 ✅
- g21-import-fields ❌ **既有失敗**：stash 對照（無我改動）同樣紅 — HEAD 上
  fd170dd(D1) 改了 import.js 欄位映射格式，g21 NC 錨（`'標記': 'tags',` 行式）漂移。
  屬 G21/D1 軌道腳本維護，非本軌回歸，登記不修。

## commit
`fix: OCR-IMP importWords DB 失敗幽靈入庫 — txFailed→added=0`（diff 照計畫書 §6.2a）
