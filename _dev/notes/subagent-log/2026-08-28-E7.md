# Subagent 紀錄 — E7（2026-08-28 波次，PM2 佇列第 4 顆）

## 任務目標
修 `tools/cli.mjs` cmdStudy mc/spell 模式用 base card 評分覆寫 → 污染 flip 卡狀態、
mode 自身狀態從未持久化。修復＝卡圖改 modeCardMap（mode 視圖）＋存檔容器欄分流
（mc_data/spell_data，flip 欄永不觸碰，容器模板逐字鏡像 store）。
計畫書 `_dev/notes/E7-fix-plan.md`（v1.0→v1.1）。

## 委員（delegate_task leaf，唯讀）與裁決
| 委員 | 審查面 | 裁決 | 重點 |
|---|---|---|---|
| #1 | 連帶消費者＋演算法一致性 | ✅ | 消費者表獨立重核全過；B4 反轉實錘 app futureCounts 本傳 mode 卡圖（兩端同構）；B5 due 錨定屬既有分歧 |
| #2 | 存檔路徑＋app 相容性 | ✅ | 五面過；發現容器行 S/D 值差（2.5/0.0 vs app 0/5）→ 採納補欄 |
| #3 | 驗證變異牙檢 | ✅ | A–E 五變異全紅零假綠；F 逃生口（T8a 未來 due seed 無寫入也算過）→ 採納雙還原＋到期卡直擊測資 |

全席 ✅ → R1 一輪過審。

## 驗證命令＋實際輸出
- `node tools/verify-e7-mode-cards.mjs` → `E7 驗證: 20/20 PASS ALL PASS`（×4 連跑，
  含 v1.1 修改後重跑）
- 內建負控制：T8a（卡圖＋存檔雙還原→flip 欄覆寫 dirty===true＋mc_data 恆 NULL）✅、
  T8b（統一 upsert→spell_data NULL＋base 被覆寫）✅
- 委員 #3 外部變異 A–E 全紅（含 C：ON CONFLICT 加 due 欄→T1/T2 逐欄比對抓到）
- 回歸：e6 22/22、e5 18/18、e4 24/24、a10/a9/c3 ALL PASS、c5 96/96、a3 7/7、
  node --check、vite build（E6 時 705ms）

## 未完成/範圍外
- 修前歷史污染回溯（無判別資料）；cmdStudy due 錨定 A10；leech CLI 全域缺失；
  mc/spell reset-card、cmdRate --mode——計畫書 §7/§8 登記。
- SR-C4 makeSession hunk：Commit 前反向隔離、commit 後原樣還原（同 E6 程序）。

## commit
`fix: E7 …`（tools/cli.mjs＋tools/verify-e7-mode-cards.mjs＋計畫書＋本紀錄）
