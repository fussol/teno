# D6 審查紀錄 — 2026-08-29（首相 PM6）

## 任務目標
D6：`src/pages/settings.js` `runImportDb()` 在 plugin-sql 連線開啟時覆寫 teno.db+app-log.db（closeDB/closeAppLog 在覆寫之後）→ WAL 髒頁回刷污染 / page cache 混合態。修法＝改與 `restoreBackup` 同構（checkpoint→backupDb→closeDB→closeAppLog→importDbDialog→reload＋catch initDB(2)）。

## R1（2026-08-29，三席）

### 委員 #1（代碼正確性）：✅
- Root cause 語意全部屬實：`import_db_dialog` 取消在 `write_db_container` 前 throw（覆寫前）、`write_db_container` 先刪 teno.db-wal/-shm 再 tmp+rename 雙檔覆寫、`backup_db` 為單檔 io::copy 不含 WAL（第二缺陷屬實）。
- 修法完備：closeDB/closeAppLog/checkpoint 內部皆自吞錯，去掉外層 try 包裹不引入新拋錯源；catch initDB(2) 復活正確、取消靜默保留。
- 非阻塞備註：①計畫書行號引用不精確（實際 runImportDb :481-495、lib.rs import_db_dialog :646-673 取消 throw :655、backup_db :1347-1370，「無漂移」宣稱不實→v1.1 改正）②邊緣＝backupDb 拋錯時 catch 對仍開啟 DB 重開第二連線（與 restoreBackup 範本同瑕疵，非本修法引入）③write_db_container 部分失敗（teno rename 成功 log 失敗）不 reload 的極窄窗口→範圍外登記。
- 實跑：verify-d6 14/14 PASS、node --check OK。

### 委員 #2（消費者穷举/影響面）：✅
- runImportDb 全 repo 唯一呼叫點 = dangerImportBtn（:1096）。
- 修法與兩個已上線同構先例一致：restoreBackup（:541-556）、driveDownloadBtn（:925-936）。
- 背景任務查證：backup-scheduler tick 走純 Rust fs＋checkpoint 對 db===null no-op；app-log flush timer 僅佇列非空時存在、500ms reload 掐死 2s timer；addAudit 外側皆 .catch(()=>{})。
- 備註 A（非阻塞建議）：import 獨有＝write_db_container 覆寫 app-log.db（restore/drive 不覆寫）→ closeAppLog 前建議加 `checkpointAppLog()`（內部先 flush 排空佇列）把「對話框等待期間 console 觸發 getDb() 重開 app-log 連線」的縮影 race 壓到近零。→ 採納（一行，白名單內 app-log.js 既有匯出）。
- 備註 B：計畫書「TENOC 容器路徑」措辭不精確（TENOC 是匯出容器 magic 非 FS 路徑）→ v1.1 改正。
- 實跑：verify-d6 14/14 PASS、常量與真實源碼逐字相等。

### 委員 #3（驗證鑑識）：❌ 必須項 M1-M3
五案實錘假綠（/tmp/d6attack 攻擊矩陣）＋一案假紅：
- **A2b**：函式體亂寫 `alert('我根本沒修')` → 14/14 全綠 rc=0（bug 態 T1 斷言常量＝重言式，漂移守衛 startsWith 簽名行空轉恒真）
- **A2**：真修法但 checkpoint 漏 await（非字面漂移）→ 走模擬態對常量斷言 → 全綠
- **A5**：真 bug 函式前放 /* */ 包裹的 FIXED 假函式 → 正則首匹配取註解內假函式 → alreadyFixed 全綠
- **A4**：模板字串內含正確序列含行首 } → 非貪婪 `\n\}` 提前截斷 → 全綠
- **A7**：catch 內 initDB(2) 只在 // FIXME 註解 → T1.6 正則匹配註解 → 全綠（復活邏輯殭屍化）
- **A6 假紅**：照 restoreBackup 範本在呼叫間插註解 → T3.5 逐字 .replace() 失配 → 假紅
- 有效面確認：A1/A3a/b/c 純註解 decoy 與三種偷工皆精準紅；T2 WAL 語義實測決定性穩定（5/5）。
- 憲法⑤：兩檔 untracked 無 git 指紋凍結 → 隨修復 commit 入庫。
- 威脅模型未成文 → 補。

## R1 裁決匯總
2✅1❌ → 未過審。處方：驗證器 v2 結構重做（M1 消重言式：POST 腿一律斷言真實源碼，bug 態必紅；M2 位置保持遮罩＋括號計數器擷取；M3 strip 負控制改遮罩後正則重排容忍註解；M4 威脅模型成文＋T2.3 不實註解刪除）。計畫書 v1.1（行號勘誤＋checkpointAppLog 採納＋TENOC 措辭＋範圍外＋2）。

## v2 重做後自證（首相實跑）
攻擊重放 /tmp/d6-attack-replay.mjs（v2 腳本）：POST✅EXIT=0 全綠、A6（原假紅）轉綠、
A2b/A4 漂移守衛 EXIT=2、A2 紅 T1.1/T1.2、A5 落 BUG 態、A7 紅 T1.6、A3a 紅 T1.4/T1.5/T1.8
——8/8 收斂。repo PRE 態：11/11 PASS＋9 N/A、EXIT=1（PRE 必紅＝bug 確認）。

## R2（2026-08-29，原 ❌ 席單席複核）：❌→書面銷帳 ✅
- M1-M4 全數銷帳（委員附 v2 行號證據：L11-15/L114-134/L232 消重言式、L29-50 mask＋L52-62
  括號計數器、L176-195 容忍式負控制、L3-9 威脅模型成文）。
- 新攻擊 7 案：N2 實錘假綠（首個＝修法＋第二同名宣告＝真 bug 版，ESM hoisting 後者覆蓋
  runtime，首匹配只斷言首個）→ 必須項 M5 重複宣告釘；N1/N3/N5a/N5b/N6 全 fail-closed
  可接受、N4（修法放 try 外）註記可接受。
- 委員明示「補 M5 一行釘＋重放腿即可升 ✅，不必再開全輪」→ 首相落檔 M5（驗證器 L98-103，
  宣告數≠1→EXIT=2）＋DUP 重放腿實跑 EXIT=2，9/9 收斂 → 書面銷帳過審。

## 動工與驗證（2026-08-29）
- 修法落檔 src/pages/settings.js runImportDb（+14/-6）：checkpoint→backupDb→
  checkpointAppLog→closeDB→closeAppLog→importDbDialog→reload；catch initDB(2) 復活＋
  取消靜默保留。
- POST 驗證器：FIXED 態 17/17 ALL PASS EXIT=0（含 P9 checkpointAppLog<closeAppLog）。
- 回歸：node --check OK、vite build 743ms、verify-g14/g5/g30/g12 四腳本全 EXIT=0。
- browser 實跑（vite dev:5173）：設定頁渲染正常（危險區域/匯入按鈕在位）→ 模擬 confirm
  點 dangerImportBtn → 走 catch 路徑 toast「匯入失敗: TypeError ... invoke」（瀏覽器無
  Tauri invoke 之預期態），證明兩行動態 import 解構（checkpointAppLog 在位）＋catch
  initDB 復活路徑全程無未捕獲異常；js_errors=0。
- 完成標準：D6 fix commit ✅
