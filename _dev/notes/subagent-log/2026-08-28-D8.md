# D8 波次紀錄 — CLI CSV round-trip 損壞（2026-08-28，首相2）

## 任務目標
修復 `tools/cli.mjs` `export-csv`/`import-csv` 與 app CSV 合同
（`src/core/import.js`）脫節之五病灶：①7 欄 SELECT→8 欄 round-trip 丟光
②pos/pron header 不一致（app CSV 經 CLI import pos/pron 丟光）③tags 雙重序列化
永久損壞 ④word 大小寫入庫丟失 ⑤逐行 parse 斷 multi-line quoted 欄。
修法＝重用單一真值源（buildCSV/parseCSVTable/resolveField），消滅 CLI 平行實作。

## 審查循環

### R1（v1.0，3 委員 delegate_task leaf，唯讀）
- **#1 ✅**：五 bug 宣稱逐條實錘（行號零漂移）；修法可行性全獨立實測（三函式
  export/簽名/鍵映射/BOM/多行引號）；parseCsvLine 消費者唯一宣稱成立；
  ORIGINAL_BLOCK 與工作區僅差尾空行。隨案三修正（採納）：FIELD_MAP 未 export
  假陳述更正（import.js:87 模組私有）、§1 標題統一 8 欄、T1c canonical 隱性
  約束轉明示。
- **#2 ✅ 有條件**：下游零依賴實錘（bot 僅 backup/dash/backups restore 三呼叫點、
  cron 零、舊 header 字面量零程式消費者）；INSERT NOT NULL 全覆蓋；lower 去重
  路徑不被大小寫保留破壞。四條件（全採納）：backupDb()/audit() 保留條款＋源碼釘
  （TENO_NO_BACKUP=1 行為測不到）、fallback 逐欄三規格（tags split,/examples split;/
  related+forms isArray 守衛）、stdout 契约明文、範圍外補三筆（歷史損壞盤點實 DB
  0 筆受災、lowercase 寫入端政策分裂、CLI.md:283 勘誤）。
- **#3 ❌（兩洞）**：①阻斷級——手刻 app.csv 转义非法（`"["red"]"`），合同合規
  實作 T2c 永紅＋反向假綠誘因（實作者可能加非合同解毒過關）→ 改真 buildCSV 產
  fixture；②mutD 假綠——export 不 parse 直傳 JSON 字串對 canonical 資料對稱損壞
  三牙全瞎（buildCSV 源碼實錘：非陣列原樣 escape）→ FIX4 非 canonical 測資＋
  T1e 全檔字節合同釘＋T1c norm() parse 正規化。變異 mutA/B/C/E 擋住✅、負控制
  byte-identical✅。

### v1.1（升版凍結前修正）
§2 FIELD_MAP 措辭、§1 標題、§3.2 字節合同凍結（＝buildCSV(canonical 映射)、
ORDER BY word BINARY）、§3.3 fallback 三規格、§3.5 保留條款、T1e/T2f/g/T6 新釘、
§4.5 修前基線、§6 補三筆。

### R2（#3 複審）✅ 可動工
沙箱實測：正確版 **23/23 全綠可達**（行序兩端自洽：SQLite BINARY sort＝JS 碼位
序、lower 排序配對同向）；mutD 唯 T1e 紅精準定位（len 605≠599）；mutDnl（mutD＋
尾換行）T1e 仍紅（容忍度剛好）；T6 四誤紅探針零誤傷（改串接/改 audit 訊息都綠，
改契约本體紅＝合同正確執行）。剩餘兩非阻斷：①T2h related 裸文字釘（mutG 溜過）
→ 已順補；②§3.5 三條 vs T6 兩條勘誤（log('WRITE') 內部日誌無釘屬可接受）。

## 實施
tools/cli.mjs：頂層 import {buildCSV, parseCSVTable, resolveField}（純模組 R1#1
實錘無 DOM）；cmdExportCsv 重寫（15 欄 SELECT、camelCase 鍵映射、JSON 欄 parse、
buildCSV 產檔）；cmdImportCsv 重寫（parseCSVTable＋resolveField、fallback 三規格、
word 保留大小寫去重才 lower、stmt 預編譯）；parseCsvLine 刪除；D8 標記註解
`// D8: CSV 合同對齊` 入檔（驗證 fixed 判針）。

## 驗證（tools/verify-d8-csv-roundtrip.mjs，全 tmp DB＋TENO_NO_BACKUP=1）
- 修前基線：12 PASS/11 FAIL（紅集＝待修項逐項、T5 負控制 5/5 綠、T2f 修前綠屬
  正當＝舊 split 對裸文字恰好等值）——R2 獨立重跑與 §4.5 逐項相符。
- 修後：**24/24 ALL PASS ×4 連跑**（含 SR-C4 剝除版單獨一跑）＋T2h 補釘後全綠。
- 負控制 T5a-e：舊三函式反換（ORIGINAL_BLOCK 與 git HEAD byte-identical 雙席
  獨立比對）→ 舊 header/雙序列化指紋/8 欄丟/app-CSV pos-pron 丟光四損壞精準重現。
- T6 源碼保留釘 4/4：backupDb()/audit/stdout 两契约。

## 回歸
d7 18/18、e4 24/24、e5 18/18、e6 22/22、e7 20/20（timeout 300 既定）、
node --check OK、vite build 723ms。

## SR-C4 程序
commit 前 git apply -R 剝 makeSession hunk（備份 /tmp/d8work-cli-sr4.mjs）、
commit 後還原（同 E6/E7/E8/D7 程序）。

## 範圍外登記（見計畫書 §6）
歷史損壞遷移（實 DB 盤點 0 筆）、lowercase 寫入端政策分裂（cmdAdd:424/
mapWords:195/mapAnkiRows:291 仍 lower）、_dev/cli/CLI.md:283 禁區文檔勘誤、
中文/TSV 匯入 CLI 化、export BOM。

## 未完成事項
無（本顆閉環）。
