# D20 執行紀錄（2026-08-28，首相2／PM2）

## 任務目標
封閉 CLI delete-deck 幽靈牌組＋孤兒歷史面（Bug D20），同類順修 rename-deck。
計畫書 `_dev/notes/D20-fix-plan.md`（v1.0 → R1 ❌×1 → v1.1 → R2 ✅）。

## 實測先行（佇列描述覆核，/tmp 真 DDL 沙箱）
- decks 表行未刪＝實錘（GUI 清單單源 getAllDecks → 幽靈牌組永留）。
- review_log 孤兒＝現行 schema **不复現**（FK CASCADE＋node:sqlite FK 預設開，
  實測 PRAGMA foreign_keys=1）；佇列條目對舊世代無 FK DB 仍真 → 顯式刪＋無 FK
  沙箱上牙（T2/T8e），計畫書誠實登記。
- 同窮發現：exam_history 孤兒（雙世代：B4 e53a3ce 08-15 後存 word_id、之前
  legacy 存文字——git 時間線實錘，B4 在 D14 **之前**，故 D14 的 text 族修法
  對 B4 後新資料是死路徑）＋rename 不改 decks.name（GUI 仍列旧名＋單字幽靈化）。
- GUI 端 deleteWord（只 text 族）/deleteWordsByDeck（只 id 族）各只蓋一族
  → **D20-SR1 登案**（src/lib/db.js 白名單外，含「D14 修法對 B4 後資料半邊
  失效」新發現）。CLI 修法兩族皆刪。

## 修法（tools/cli.mjs 單檔）
- cmdDeleteDeck：BEGIN 事務内 exam_history（word IN (SELECT word…) OR word IN
  (SELECT id…)，words 刪前）→ review_log 顯式刪 → cards → words → decks 行
  （rec 守衛）→ deckOrder 剔 id；失敗 ROLLBACK＋❌＋exit 1；備份先行不變。
- cmdRenameDeck：同事務補 UPDATE decks.name；UNIQUE 衝突整體回滾（改名不默認
  合併；邊界：words-only deck 靜默合併登 §6）。

## 審查
- R1（3 委員）：#1 ✅（獨立沙箱 29/29＋外鍵預設實測＋負控制 951B 復證）／
  #2 ✅條件（穷举無「變少即壞」讀者，cmdAudit 反而受益；F-1/F-2 補登義務）／
  #3 ❌條件（14 變異 13 殺；m9 拆 deckOrder 內層 catch 全綠存活＝無牙防線，
  處方 T10）。
- **審查事故誠實登記**：#3 探測腳本 `mk()` 漏 return → TENO_DB=undefined →
  真檔跑了一次 delete-deck Alpha --yes。首相獨立覆核：真庫無 Alpha（14 實牌組）
  → 命中零行、integrity_check=ok、words 4884 原封、唯一副作用 audit_log
  id=244 留置。教訓入 SOP：沙箱 spawn 前 assert TENO_DB 非空 tmp 路徑。
- R2（#3 複審）：✅ 轉綠——m9 恰 T10a/b 紅 29 綠、假修變異 m9fakeA/B 皆被
  T10c 斃（強釘）、m2/m7 抽測無誤傷、產品碼零改動多路佐證。

## 驗證（實際執行輸出）
- `node tools/verify-d20-delete-deck.mjs` → **31 PASS / 0 FAIL — ALL PASS**
  （送審前＋R2 後＋SR-C4 反剝後皆綠；T8 負控制 HEAD 兩函式 951B byte-identical
  獨立 eval 比對＋行為面幽靈列/雙族孤兒/legacy 孤兒×2/rename 旧名全重現）。
- m9 獨立復現：變異體恰 T10a/b 紅、餘 29 綠。
- 回歸（反剝後）：d19 32/32、d7 18/18、d6 14/14、e4 24/24、e5 18/18、e8 18/18、
  c3 ALL PASS（--experimental-test-module-mocks）、c5 96/96、node --check、vite。

## commit 程序
SR-C4 hunk 反剝→回歸→commit（cli.mjs＋verify-d20＋計畫書＋本紀錄）→hunk 還原。
scope-requests.md（D20-SR1 一行）因工作區混有 PM1/總統未 commit 之裁示改動，
依憲法⑨不夾帶，留工作區由後續結算。

## 未完成事項
無。本顆閉合。範圍外 §6 十七筆逐名在案（重点：D20-SR1、cmdDelete 單字版同病灶、
add/import decks 註冊面、統一事務化另單）。
