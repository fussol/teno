# 2026-08-27 E4 — subagent 審查紀錄（首相2 / PM2 CLI 域）

## 任務目標
修 tools/cli.mjs cmdRate：review_log 補 5 欄（duration/elapsed_days/scheduled_days/
stability/difficulty）＋ cards.elapsed_days 寫回 ＋ elapsed 以 dayCutoff-aware 日差
計算餵 FSRS（欄位語意對齊 store.js rateCard:792-803 / db.js addReviewLog:409-414）。

## 審查歷程
### R1（v1，3 委員，Hermes delegate leaf，全程唯讀）
- #1 修法正確性：✅。逐欄語意表全過（duration→null、elapsed=複習前日差、
  scheduled log 端無條件 round / cards 端 state2?round:raw 不对称鏡像、
  stability/difficulty=複習後、參數序 getToday(cutoff,tz,now) vs
  toLocalDateStr(d,tz,cutoff) 皆正確）。LOW×2：app 端 legacy naive last_review
  未 normalize（app 病，範圍外）；--date 08:00Z 錨點極端 tz 位移（列新案）。
- #2 消費者完整性：❌ MED-1 — `--date` 亂序回填 → 負 elapsed 入庫 →
  fsrs-rs `elapsed_days: u32`（lib.rs:629）serde 整批 OverflowError、
  py-fsrs FSRSReview(delta_t<0) OverflowError 實測重現 → optimize 整輪爆。
  其餘 5 項查核全過（review_log 讀者窮舉、cmdAudit replay 自洽、daysBetween
  陰影無害、spread 多餘欄無害、cmdSim 無誤傷）。
- #3 Anki/FSRS 官方：✅（MED 措辞：官方優化器由時間戳重算 delta_t
  （params.rs days_elapsed），非直讀 elapsed_days 欄；duration NULL 安全：
  py-fsrs review_duration=None 一等公民、optimizer 重播恆傳 None；
  delta_t==0 官方訓練集本就過濾）。
### R1 → v1.1
採 MED-1（elapsed 持久化前 Math.max(0,·) 夾零，位置在 FSRS 輸入＋兩處 cards
持久化＋log INSERT 四消費點之前）＋ 措辞修正（計畫書 §1、cli 註解、verify 檔頭）
＋ verify 增 T7 回填負值三斷言。
### R2（v1.1，3 委員）
- #1 ✅（夾零單點四消費無旁路、正向值恆等無回歸、T7 斷言正確）
- #2 ✅（自建三場景實測：亂序回填 log/cards=0；cutoff/tz 邊界下夾零成立；
  10 筆亂序鏈路跑真 fsrs_rs_python optimize exit=0 loss=0.4399 零 OverflowError；
  負控制單筆 -15 因 build_items 排序恰好不炸、兩筆以上炸 → 寫入點夾零是唯一靠譜修法）
- #3 ✅（措辞與官方事實一致；夾零對齊 Anki days_elapsed .max(0) 天然防線）

## 驗證命令＋實際輸出
- `node tools/verify-e4-rate-log.mjs` → `E4 驗證: 24/24 PASS ALL PASS`（exit 0，
  T1-T7＋負控制；全程 tmp DB 副本 TENO_DB＋TENO_NO_BACKUP=1，未觸真實 DB）
- `node --experimental-test-module-mocks tools/verify-a10-due-anchor.mjs` → `結果：ALL PASS`
- `node --experimental-test-module-mocks tools/verify-c3-rating-lock.mjs` → `=== C3 連按模擬 ALL PASS ===`
- `node tools/verify-a3-status.mjs` → `A3 驗證: 7 pass, 0 fail`
- `node --check tools/cli.mjs` OK；`vite build` ✓ built in 765ms
（註：a9/a10 直接 node 跑會 mock.module 報錯——需 --experimental-test-module-mocks
旗標，腳本內已註明，非 E4 引入）

## 未完成／範圍外
- app 端 store.js:687 elapsed 未夾零（僅時鐘偏移可觸發，與 R1 LOW 同列範圍外）
- fsrs-optimize.py build_items 缺顯式 max(0,·)（負值靠排序偶然掩護，列消費者佇列備註）
- cmdRate 用預設 FSRS 權重不讀 fsrsWeights、--date 08:00Z 錨點 tz 位移 → 建議列新案
