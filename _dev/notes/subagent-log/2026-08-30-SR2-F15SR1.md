# Subagent log — F15-SR1（2026-08-30, SR2-CONT）

## 任務目標
F15-SR1（CDN SRI/CSP 供應鏈加固）— cmdReport/cmdCompare HTML 模板 CDN 無 SRI＋
無 CSP＋字串欄位 `<` 未轉義。4 實例：tools/cli.mjs + _dev/cli/cli.mjs 各 2 模板
（compare/report）共用 `<script src="https://cdn.jsdelivr.net/npm/chart.js">`。

## 計畫書
`_dev/notes/F15-SR1-fix-plan.md` v1.0 → v1.1 → v1.2（審查歷程逐版記錄）。

## 審查（4 輪次，3 席實質過；R1 另兩席 429 未跑）
| 席 | 輪 | 裁決 | 要點 |
|----|----|------|------|
| 委員1 | R1 | ❌(3必改) | 抓到 3 必改：①非貪婪 regex 資料含 `</script>` 提前截斷→注入逃逸＋報告壞死 ②verify T5-I5 同款假綠 ③負控制用 `HEAD:` commit 後自毀。＋建議(補 base-uri/form-action、清死碼) |
| 覆核1 | R2 | ❌(2新) | v1.1 regex 錨定 `</body>` 實作吃 `</body>`→尾端壞死；`\s*` 零空白偽邊界 `</script></body><img>` 仍逃逸 |
| 覆核2 | R3 | ✅ | v1.2 貪婪 regex＋m[2] 補回解決尾端壞死＋偽邊界逃逸；4 樣本＋torture 親測零逃逸；CSP hash 對拍一致；良性資料不誤傷；verify 38/0 |
| 安全席 | R3 | ✅ | CSP 語意全成立：file:// self 合理（報告零 same-origin 子資源）、script-src 無 unsafe-inline（模板零 inline handler）、SRI 實抓對拍、\u003c 雙面一致、四實例無遺漏 |
| 版控席 | R3 | ✅ | git 純淨（僅 6 檔：三白名單＋三指紋）、回歸綠、邊角 fail-open 評估可接受、無殘留；建議補 T5K 模板尾釘（已採納） |

R1 起每輪都抓到真縫；最終 3 席 ✅。審查全程 delegate_task（唯一的 Hermes），無外部 CLI agent。

## 驗證命令＋實際輸出
- 動工前（PRE 負控制）：verify-f15 新 T5 對現態全紅（裸 CDN 在位、無 helper）、T1-4 回歸 18/18。
- 動工後：`node tools/verify-f15-report-surface.mjs` → **40 PASS / 0 FAIL**
  （T1-4 回歸 18 + T5 新增 22：T5A-F×2、T5K×2、T5-G/H、T5-I1..I5、T5-J）。
  T5-G 負控制 pin `a53a13c`（動工前 HEAD，commit 後不自毀）。
- SRI 對拍：`curl cdnjs Chart.js/4.4.1/chart.umd.min.js`（200,807 B）→ openssl sha512
  = `CQBWl4fJHWb...aZAWw==` 與 code integrity 逐字吻合（cdnjs 官方公告 hash 同值）。
- `node --check` 三檔綠；`node tools/cli.mjs selftest` 12/12；`verify-version-sync` PASS。
- commit 後重跑 verify 仍 40/40（T5-G 固定 pin 驗證）。

## 版控
commit `8639e09`（v5.9.3）。6 檔：tools/cli.mjs、_dev/cli/cli.mjs、
tools/verify-f15-report-surface.mjs、package.json、src-tauri/tauri.conf.json、
src-tauri/Cargo.toml。明確 add，未誤掃共享髒檔（scope-requests.md 等不 add）。
版本史：起始 package.json 5.9.2、git HEAD 5.9.1（三檔統一 5.9.1）→ 誤降 5.8.13 →
發現後拉回 5.9.3（version.sh 機械寫+自驗，三指紋一致，只升不降）。

## 未完成事項
- 無。F15-SR1 完整閉合。
- 範圍外已登計畫書：其他 CLI HTML 輸出面（fsrs-report/selftest）若同類 CDN 未涵蓋→追蹤。