# Subagent Log — F9 (2026-08-28, PM3)

## 任務目標
install_piper_model HuggingFace 網址解析修復：尾斜杠雙斜杠 404／深度不足瞎拼／
`https://hf.co/` trim 失敗段位移。修法＝純函式 `parse_piper_url` 抽出＋呼叫點委派＋
深度守門＋空段 filter＋scheme-先行 trim 鏈。計畫書 `_dev/notes/F9-fix-plan.md`（v1→v1.1）。

## 委派（R1，3 委員 leaf，delegate_task，全程唯讀）
### #1 修法正確性＋負控制真實性 → ✅
- 驗證 18/18＋cargo 12/12 獨立重跑（補釘後 13/13）。
- 負控制 ORIG_BLOCK(638B)/ORIG_TAIL(586B) 逐字節對 git show HEAD 獨立核對屬實。
- 勘誤成立：UI hint 例舊碼生成 URL 實測 200（審計「永遠404」不實）。
- 範疇：download curl 閉包 HEAD 逐位元組同，零越 F12 權。
- 下溢/穿越審查：`<6` 門檻＋末三段不可下溢；`--` 分隔封 curl 參數注入。
- 新發現死法（非回歸，登記併 F12）：`?download=true` 查詢尾巴（高頻貼法）、
  大寫網域、大寫 `.ONNX`。
- 條件：上列死法與向量 J 缺漏登案勿靜默遺忘 → 已納 v1.1 §6。

### #2 消費者窮舉＋行為等價性 → ✅
- 唯一消費者鏈 settings.js:754→api.js:66→command；bot/cron/CLI 零。
- 自寫 10 向量兩碼對跑 EQUIVDIFF 0（正規 4 層/檔網址新舊逐字節等價）。
- 變異牙檢 M1/M2/M3/M5 全斃；M4（固定 index vs 尾導向）全綠＝T1 無 5 層向量盲區，
  實倉零行為影響 → 補 cargo 五層釘 `deeper_nesting_uses_tail_three`。
- HF voices.json 全倉 175/175 結構驗證：lang/locale/voice/quality 四層＋檔名規則全域成立；
  quality 詞彙 {high,low,medium,x_low}（深度守門零誤殺；文案未列 x_low 屬 nit 登記）。

### #3 誠實性＋範圍紀律 → ✅
- 四 hunk 逐一目視零夾帶；髒檔 cli.mjs/scope-requests.md 屬他案（SR-C4/E13/D5-SR1）未吞。
- 中毒連鎖實跑：404 → curl exit=0，body `Entry not found`（純文字，§2「HTML」措辭勘正）
  → 假成功徵狀鏈屬實，F12 域本單不治正確。
- T1 真碼提取無硬編碼確認；advisory §3.1 註與 `<6` 硬閘矛盾→v1.1 文字勘正。

## 裁決
三席全 ✅ 一輪過審。v1.1＝計畫書文字升版＋cargo 側補釘，已審函數體零改動。

## 驗證命令＋實際輸出（首相自跑，commit 前）
```
$ cd src-tauri && cargo test --lib piper_url_tests
test result: ok. 13 passed; 0 failed; 14 filtered out

$ node tools/verify-f9-piper-url.mjs
ALL PASS — 18 passed, 0 failed        （連跑 ×2＋三委員各獨立重跑全綠）

$ npm run build
✓ built in 748ms
```
既存環境缺陷（非 F9，勿計）：`cargo test --lib` 全量時 sim_tests 3 紅——
load_req 讀 /tmp/sim-req-*.json 手工 fixture 缺失（:1801/:1818/:1835）。

## 未完成事項
- `?download=true`／大寫網域 → F12 順治（F9-fix-plan §6 登記）。
- D5 本佇列跳過 → D5-SR1 登 scope-requests 待總統裁示。
