# Subagent log — D17（2026-08-28, PM3）

## 任務目標
D17：backup_db（lib.rs 修法前 :1291-1303）unix 秒命名 teno-<ts>.db＋fs::copy
覆寫語意 → 同秒第二次備份靜默截斷覆寫同一檔（按兩次備份按鈕列表只多一條，
快照幽靈消失零回聲）。

## 修法（v1.1，lib.rs）
- 新純函式 `unique_backup_dest`：O_EXCL `create_new(true)`＋`.mode(0o600)`
  （R1#1-M-2，F11 write_private 同課）；AlreadyExists→`saturating_add(1_000)`
  （+1µs）換名重試，上限 10000（病態連撞响亮 Err 不死迴圈）；建立即獨佔零
  TOCTOU。
- `backup_db`：命名 as_secs→`as_nanos().min(u64::MAX as u128)`（nanos＝
  list/prune「nanos→secs」启发式原生格式，解析端零改動；鉗位＝R1#1-M-1 防
  2554 年 u128 截斷 wrap→1970 排位 prune 首端新檔先刪）；fs::copy→io::copy
  到已獨佔檔柄；複製失敗 remove 半成品。
- cargo 測 `backup_naming_tests::d17_same_ts_never_overwrites`：同 ts 三連呼
  名稱互異＋首檔 SNAP0 零覆寫＋nanos 解析契約釘＋手刻同名檔零碰＋0600 權限斷言。

## 委員（R1，3 席 sequential leaf，全 ✅）
### #1 修法正確性＋邊界攻擊 ✅（M-1/M-2 採納）
- 行號/启发式/解析端零改屬實；重跑 27/27＋cargo 1/1；自建 8 攻擊向量全過
  （u64::MAX 連撞 Err@12ms 終止、10000 上限實觸發、mode-000 檔 EEXIST 走換名、
  EACCES 立 Err 不進換名）
- M-1：計畫 §5「2554 溢出 parse 跳過」不實——`as u64` 截斷 wrap 仍是數字→
  1970 排位→prune 首端新備份先刪。採納 .min 鉗位＋§5 勘誤
- M-2：fs::copy 保源權限位元→create_new 預設 0644 世界可讀（備份含全量复习
  史）＝隱私回歸。採納 .mode(0o600)＋cargo 權限斷言
### #2 Android/跨平台＋回归母 ✅（Info×2 無阻斷）
- aarch64/x86_64-linux-android 純 std 隔離 crate check 雙綠；整包掛 NDK clang
  缺席（ring/oboe build script＝環境限制非代碼，老實登記）
- tauri-2.11.3 源碼鏈：app_config_dir→PathPlugin.kt dataDir=/data/user/0/
  com.teno.app 內部儲存沙箱（非 FUSE）→0600 生效；FUSE 假設情境與舊碼持平非回歸
- 前端 4 呼叫點回傳全丟棄零假設；列表用 b.timestamp 非檔名（19 位透明）；
  還原鏈消毒鏡像安全；混存 prune 鏡像正確；ts=0/時鐘回撥三情境安全
### #3 變異牙檢 ✅（11 變體 10 撓 1 逃逸→逃逸已補牙）
- M-c create 覆寫復活 9 腿紅／M-e token 假在位行為戳破／M-j2 恆真鉗位結構紅／
  M-h 內聯死碼恰 T4a 紅／M-i 浮動 pin 恰 T3a 紅
- F-1（採納）：無上限 loop{} 全綠逃逸＝腳本無終止性探針→補 T1-6/7 飽和連撞
  timeout 探針（驗 M-f 形態非零退出即紅）
- F-2（採納）：T4f 註解 token 騙過→鏈式緊釘 `.create_new(true).mode(0o600)`
- F-3（採納）：harness panic 混 stdout SyntaxError 崩腳本→T1-z 輸出乾淨釘

## 裁決與處置
- R1 三席 ✅，v1.1 全吸收（吸收面＝字串釘/探針/權限/鉗位，無語意返修，
  #1/#3 原案預授「採納後不重送」）
- 驗證 v1.1：tools/verify-d17-backup-collision.mjs **32/32 ALL PASS**
  （T0 cargo×3・T1 真碼提取向量機×11 含飽和終止探針・T2 雙端解析契約×5・
  T3 負控制 pin 0096e88 靜態 hash 徵狀重現＋判別性×7・T4 結構釘×7）
  ＋cargo d17_same_ts_never_overwrites
- 回归：d16 32/32（同檔鄰域）d19 32/32 d7 18/18 container 3/3 vite 823ms 全綠
- commit：lib.rs＋verify 腳本＋計畫書＋本 log（cli.mjs SR-C4 hunk、
  scope-requests.md 共享檔零夾帶）

## 範圍外（計畫書 §6）
- D5 WAL checkpoint（同函式不同病灶，前端正解 D5-SR1 已登案待裁示）
- CLI cmdBackups 同分鐘碰撞（不同域不同格式）
- prune 換算启发式 5138 秒名遠端脆弱（與鉗位同類登案）
- io::copy 無 sync_all 掉電耐久性（pre-existing 同衰，nit 另案）
- OpenOptionsExt 無 cfg gate（Windows 不在產品矩陣，備註）

## 教訓
- 「舊碼行為等價面」要逐位元組查：fs::copy 保權限位元是隱性契約，改
  create_new 就悄悄放寬 0644——#1 的 M-2 示範了權限也是回歸面。
- 溢出測「值去哪」不是測「會不會炸」：u128→u64 截斷 wrap 出的仍是合法數字，
  parse 跳過的想當然被鏡像實證推翻（§5 勘誤課＝宣稱鏈要跑過才寫）。
