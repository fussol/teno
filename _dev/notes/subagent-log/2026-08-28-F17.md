# F17 審查紀錄 — findVoice main-thread 讀 tts.voices（ANR）2026-08-28

## 任務目標
修復 TtsPlugin.kt findVoice() 在 main thread（Tauri @Command 派發緒）直讀 `tts.voices`
之 ANR 風險；修法 stale-while-revalidate 語音索引；全程四輪審查（R1×3、R2×3、R3×3、R4×1，共 10 人次）。

## 各輪裁決
- **R1（v1 計畫＋腳本 v1）3 席**：#1 ✅（5 minor：ttsRef 捕獲/@Volatile destroyed/單欄發佈/失敗語意/snapshot 命名）｜#2 ✅（3 minor：T7 補強/TTL 更名/import）｜#3 ❌（腳本 6 突破向量 A1/A2/A3/A4/A5/A7）→ 升 v1.1＋腳本 v2。
- **R2（v1.1＋腳本 v2）3 席**：#1 ✅（2 minor：findVoice 讀取側 latch/註解語氣）｜#3 ✅ 有條件（最有價值：miss 不觸發刷新 → 新下載語音最長 60s 不可見；root cause 沿 Tauri 框架源碼全鏈實錘 @Command 必在 main thread）｜#2 ❌（新向量 A3b 方法參照/B6 標識符子串劫持/A4 條件化 finally/A2b decoy start/A5c raw string）。
- **憲法⑩裁決（總統）**：同類缺陷（文字閘 vs 文本攻擊）連續第二輪 → 停止逐條補丁，結構重做：單趟狀態機剝離器（等長空白化）＋token 級掃描＋Thread{}.start() 鏈式綁定白名單＋T3 釋放錨收緊；**威脅模型定文**（防無意回歸＋偷工假修法，不抵禦蓄意對抗偽裝——圖靈完備語言上文字掃描不可全抓為數學事實）。
- **R3（v1.2 碼＋腳本 v3）3 席**：#1 ✅｜#3 ✅（獨立判定 miss 觸發＝對基線嚴格改善；連跑確定性零狀態洩漏）｜#2 ❌ 但自判非軍備競賽：模型內 3 條錨點缺口（N4 val 遮蔽/N5 error reason/N5c polling done reason）＋1 誤殺（N1c .start() 銜接窗 20 字元過窄），開出精確處方並預驗 v31.mjs，「吸收升 v4 即可予 ✅」。
- **R4（複核席 1 人，代碼面 R3 已兩席終審✅ 且零改動）**：✅。三補釘轉紅驗證（自構變體）、N1c 轉綠、v3 已焊六向量回歸 6/6 零翻綠、fixed 8/8、bug 態 1/7 零漂移、連跑確定性、精確計數雙邊實證（4≠3 也紅）。

## 最終驗證命令＋實際輸出（本席 PM5 親跑）
```
$ node tools/verify-f17-findvoice.mjs /tmp/f17v2/fixed.kt   # 定稿態
8/8 ALL PASS（含 T8 負控制：revert 後 T1/T2/T4 精準紅）
$ node tools/verify-f17-findvoice.mjs                        # bug 態基線
1/7 HAS FAILURES（T1-T6 紅、T7 綠、T8 SKIP）
$ 攻擊自測七連紅：atk-n4/n5/n5c→T7、atk-b6/a3b/a2b→T2、atk-a4→T3（7/8 各紅）
$ 合法變體 leg-n1c（}.start() 間插行註解）：8/8 綠（誤殺已除）
$ ./gradlew :app:compileArmDebugKotlin --offline   # 基線（改動前）
BUILD SUCCESSFUL in 10s
（POST 編譯見 commit 3367f12 後本檔實施段驗證輸出）
```

## 實施中誠實登記
- v4 首版禁遮蔽檢查誤用全檔 clean → 誤殺欄位宣告 `private var stopRequested`，fixed 態 7/8 當輪抓出，縮限 speakFn.body 修復。
- gradle 任務名 `:app:compileDebugKotlin` 在 Tauri 多 flavor 下歧義（不存在），正解 `:app:compileArmDebugKotlin`。
- 任務書檔案路徑勘誤：`src-tauri/src/TtsPlugin.kt` → 實際 `src-tauri/gen/android/app/src/main/java/com/teno/app/TtsPlugin.kt`。

## 未完成事項／後續
- 真機冒煙（選 voice→speak→切 voice→殺引擎）＝使用者責任，非靜態閘職責。
- R4 minor：x1 跨行 val 遮蔽（威脅模型外）、x2 `this.stopRequested` 窄錨刻意紅（教義一致）。
- R3#3 環境發現（非本軌）：c1-undo-modes/a10-due-anchor 紅於 node v26 移除 mock.module——tools 域另案。
- R3#1 minor-1：Thread.start() 拋 OOM(Error) 單飛卡死——記錄不修（降級態仍非阻塞，與基線持平，修反破 T3 錨）。
