# D10-SR1/SR2 — CLI 雙鏡像 find_db_file 盲取修復（subagent-log）

- 日期：2026-08-30
- 首相：SR2（CLI/Drive/Rust 域）
- 計畫書：`_dev/notes/D10-SR-fix-plan.md`（v1→v5）

## 任務目標
同步 Rust 主修 `pick_latest_file` 語意到 `tools/cli.mjs` 與 `_dev/cli/cli.mjs` 兩處 CLI 鏡像（同 bug 跨鏡像併單 commit），消除 `fields=files(id)`＋`list.files?.[0]?.id` 對同名多檔盲取首筆的缺陷（upload 覆寫舊檔/download 抓錯版之資料分岔）。

## Bug 實錘
- `tools/cli.mjs:3385-3391`、`_dev/cli/cli.mjs:3045-3051`（任務書 3283/3046 漂移，已勘正）。
- Rust 語意參考源：`src-tauri/src/drive_sync.rs:262-294`（pick_latest_file 純函式）。

## 方案演化（歷經 5 版，憲法⑩ 重估替代結構）
- v1：純靜態掃描器法。❌ 委員0：scanCli 只剝 block comment，行註解 decoy（攻6）可四斷言全綠突破。
- v2：採納六點掃描器修法＋typeof mtime。❌ 委員0：縫1 `!f.id` 對 null TypeError crash（劣於舊版＝半套 as_str）；縫2 解構盲取改樣本遷就掃描器未補掃描器且未登記。
- v3：縫1 null guard＋typeof id；縫2 行首錨＋先剝 block＋結構化 URL＋解構釘＋DESTRUCT 牙。❌ 委員0：掃描器仍有 A2/A8/A12（多行模板字串偽行首錨／字串偽 block 剝除／組合）——純靜態字樣掃描本質防不了語意假修法。
- v4：憲法⑩ 停止補丁，改**行為級**替代結構——提取純函式 `pickLatestDriveFile`＋`new Function` 真碼向量對拍（對齊 Rust T3 真碼微編譯）。❌ 委員1：阻斷一（bestLoop 斷言滯後於 hunk 結構→POST 必紅）＋重大二（行為級只覆蓋 tools、_dev 零覆蓋）＋NC 缺 L7。
- v5：採納委員1 四修（pickCall 取代 bestLoop、TH 雙鏡像＋TH-sync 一致＋orphan 紅、NC 補 L7、數字實跑化）。**主席 ✅ 放行**（委員0/2 HTTP 429 工具失敗不計席）。

## 修法落地（v5 最終）
兩鏡像同款：新增 local arrow 純函式 `pickLatestDriveFile`（modifiedTime RFC3339 字典序 max、嚴格大於平手留首見、全缺 mtime 退 `${fileId ?? firstWithId}`、null/缺 id/非字串 `continue`、空/非陣列 null），fetch 加 `orderBy=${encodeURIComponent("modifiedTime desc")}`＋`fields=files(id,modifiedTime)`，`fileId = pickLatestDriveFile(list?.files)`。fileId=null 時 upload/download 保守行為同舊。

## 驗證命令＋實際輸出
- `node tools/verify-d10.mjs` **POST 態：72 PASS / 0 FAIL 全綠**（Rust T1-T5 全綠；T-CLI 兩鏡像 pickCall/orderBy/fieldsFull/blindFirst 5 斷言綠＋三樣本牙；TH 行為級對兩鏡像各 12 向量＋TH-sync＋TH-NC/TH-NC-year 負控制紅）。
- `node --check tools/cli.mjs` OK；`node --check _dev/cli/cli.mjs` OK。
- `npx vite build` ✓ built 941ms（chunk>500kB 既有警告僅）。
- 版本 `./tools/version.sh patch` 5.8.8→**5.8.9**，源三檔一致，verify-version-sync PASS。

## 裁決
✅ 修復完成，行為級替代結構健全（語意假修法族封死，純靜態掃描器本質限制以行為級治本）。

## 未完成事項
- scope-requests.md 的 D10-SR1/SR2 行仍「待裁示」，波尾翻狀態（共享檔不 add）。
- §6 行為級追蹤項：已由本顆落地（純函式 pickLatestDriveFile 即為治本），尚有「其他語意假修法創意變體」持續風險，已以行為級向量基本涵蓋。