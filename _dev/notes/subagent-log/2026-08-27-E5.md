# E5 審查紀錄 — 2026-08-27（PM2 波次）

## Bug
`tools/cli.mjs` cmdRate/cmdSim：`Math.random()` fuzz（app 是確定性 seed）＋不套
greaterThanLast load-balancing＋FSRS 構造走 `makeSession → new FSRS()` 預設權重
（＝audit 假 mismatch 真兇：寫入用預設權重、replay 用 settings 權重，必然漂移）。

## 循環
計畫書 `_dev/notes/E5-fix-plan.md`：v1.0（前次首相 session 擬＋code 凍結在工作區，
本 session 補程序）→ v1.1（R1 修訂）→ v1.2（R2 修訂，僅驗證腳本）。
Commit `972382e`（4 檔：cli.mjs、verify-e4 strip 適配、verify-e5 新建、計畫書）。

## R1 — 3 委員（delegate leaf，唯讀）
- **#1 修法正確性 ✅**：fsrsCtx 與 store.rateCard:650-655/711-716 六點逐字同構
  （weights 剝殼/retention clamp/maxIvl/steps fallback/fuzz seed=複習前 reps/
  futureCounts Map 型別三處）；新卡 stability 1→0 與 audit base 2.5→0 均為
  fsrs.js:301 nth-忽略路徑的功能 NOP（零回歸論證成立）。
  附 MED：verify-e5 T6 flaky（連跑 4 敗 2）；LOW：T7 固定檔名落 repo、cmdSim reps 恆 0。
- **#2 消費者完整性 ✅**：makeSession 消費者窮舉＝cmdSim/cmdStray/cmdFix（唯 sim 涉
  FSRS 已收斂；stray/fix 直接 SQL）；cmdStudy 係寫路徑但已自讀 settings 權重＋seeded
  fuzz（缺口屬 E6/E7 正確）；audit 非 flip log 不 replay；base 2.5→0 系修復（fsrs.js:119
  init 分支）；附 LOW：LEARN_STEPS 頂層死碼、cmdAudit anki/F 未用變數。
- **#3 測試區分力 ❌**：T6 flaky（29 跑 7 敗 ~24%）＋無牙（cmdSim fuzz 剝回
  Math.random 雙副本比較 0/6 仍綠；真變因 cli.mjs:1324 牆鐘）。其餘 13 斷言有牙。

## v1.1（code 改）
cmdSim `--now` SandboxDate 完整時鐘沙箱＋digest sha256 錨點＋reps 推進；死碼清理；
verify T6 重寫（×3 逐字一致＋digest＋負控制）；T7 buggy 副本改 tmp bugsub/＋src symlink
（`../src` 相對 import 深度＝v1.0 落 repo 真因）。連跑 12/12、e4 24/24、回歸全綠。

## R2 — #3 複審（單席，C6 先例）❌（有條件）
- flaky 根治 ✅（0/15）、沙箱/digest/T7/e4/無污染 ✅。
- ❌ 殘留：「digest 必漂移」宣稱不實——T6 測資純新卡在 ratings 序列下走不到
  REVIEW（fuzz 不進 interval）；負控制走非 json 管線洩 CMD 牆鐘行，g1≠g2 靠時間戳
  巧合轉紅。委員自證「預置 REVIEW 卡則 buggy digest 真漂移」（4 種落點）。

## v1.2（僅驗證腳本，code 零改動）
採委員親測處方：T6 測資加 4 張預置 REVIEW 卡（state=2 當日到期、stability/reps 各异）
＋負控制改走同一 `cli(db, ARGS, buggyCli)` --json 管線。斷言 17→18。連跑 15 次 0 失敗。
憲法⑩自查：連兩輪同類缺陷，但 v1.2＝委員本人實證處方（證據先行），非盲補丁，記錄於 §11。

## R3 — #3 複審（單席）✅ 「E5 放行」
- 連跑 8 次全 18/18；v1.2 僅動 T6 核證（cli.mjs 與 v1.1 逐字相同）。
- 雙向變異體獨立牙檢：A=cmdSim fuzz 剝回 Math.random → 正控制 2 斷言轉紅（16/18）；
  B=反向牙（due 加毫秒 jitter）→ 同 2 條轉紅 → 正/負控制雙向封死。
- buggy 輸出全文無牆鐘洩漏；repo 無污染；e4 24/24。

## 驗證命令（可重跑）
- `node tools/verify-e5-seeded-fuzz.mjs` → 18/18 ALL PASS（穩定性：15＋8 連跑 0 敗）
- `node tools/verify-e4-rate-log.mjs` → 24/24（strip 適配後）
- 回歸：a10/a9/c3（--experimental-test-module-mocks --test）fail 0、a3 7/7、c5 96/96、
  `node --check tools/cli.mjs`、`npm run build` 738ms。

## 範圍外登記（憲法⑥）
- cmdStudy futureCounts=null（dueDays 差 fuzz 區間，不影響 audit 五欄）→ E6/E7 域。
- replay 無法重建歷史 futureCounts（§4 論證不需要）。
- cmdAudit/cmdFsrsReport/whatif 本地 split-map steps 統一＝讀路徑重構。
- cmdSim rating 亂數保留（壓力測試設計本體；--ratings 為確定性入徑）。
- setTimeout(0) 還原 Date 於拋例外：定時器先排後拋仍會觸發（進程存活前提下無虞），
  R2 #3 (c) 註記，未列紅線。

## 未完成事項
無。E5 閉合。佇列下一顆：E6（cmdStudy 不寫 review_log）。
