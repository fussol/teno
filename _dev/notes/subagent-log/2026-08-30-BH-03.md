# subagent-log — 2026-08-30 BH-03

## 任務
deleteDeck 不清 memory 端 reviewLog/examHistory（DB 已清，UI 殘留到重載）（BUGHUNT-TODO.md#BH-03）

## 實錘
- store.js deleteDeck():1740-1771 只 filter words/cards/各 Set/buriedAt/examples，漏 reviewLog/examHistory 兩個 array。
- db.deleteWordsByDeck（db.js:366-381）已清 DB review_log(:370)+exam_history 雙世代(:372-373)→ memory/DB 分歧（整字本版）。
- BH-02 已建共用 helper `deleteWordFromMemory(id)`（:574-580）清 reviewLog/examHistory/六 Set/三 buriedAt/examples。

## 修法（共用 helper，零漂移）
deleteDeck wordIds 計算後（:1757）加：
`for (const wid of wordIds) deleteWordFromMemory(wid);`
（用 wid 避遮蔽外層 deck id，採委員 #3 建議）。既有 cards/Sets/buriedAt/examples 手寫過濾保留（與 helper 冪等重複，結果一致）。+1 行。

## 驗證
- node --check OK
- tools/verify-bh03.mjs：9 PASS / 0 FAIL（源碼釘+語意重放+負控制）
- 保留率關聯：deck A 字被剔後 computeRetention 剩他字本、不含已刪字本
- 負控制 stash：未修態 S1/S2 轉紅（有牙）；回歸 verify-bh01 11/11、verify-bh02 19/19、vite build 954ms

## 審查
3 委員 ✅✅✅（串行收斂）
- 委員1（完整源碼）：for 迴圈位置正確、共用 helper 無重定義、與既有過濾冪等不衝突、負控制咬得住 ✅
- 委員2（共用一致性）：deleteWord:1712 與 deleteDeck:1757 呼叫同一 helper 零漂移、wordIds 貫穿不受污染、空 Set 邊界 ✅
- 委員3（負面 edge）：DB 順序正確、誤刪面與既有邏輯對稱、冪等無害、O(W×N) 低頻可接受；建議 for 變數改名 wid 避遮蔽（已採納）✅

## 版本
v5.8.15（四檔一致，commit 前驗證）

## Commit
3480625 (v5.8.15)