# PM-OCR2 · T5 稽核報告（獨立第二軌 PM-OCR2-T5，2026-08-28）

## 背景：T5 雙派碰撞
- 調度器重複派工：`pocr2c`（20:27，T4→T5→T6）與本軌 `pocr2t5`（20:38，僅 T5）。
- pocr2c 先動工（20:41:51 寫 store.js），並覆寫本軌已寫好的同名驗證腳本（20:45:09）。
- 裁決（總統未在时限內裁示）：不硬搶 commit（違憲⑨）、不 kill 他軌；commit 職權讓渡
  pocr2c → **2dae766 `feat(ocr): importOcrText`**；本軌轉獨立稽核。詳見 ocr2-checkpoint.md。

## commit 審查（靜態）
- 2dae766 僅 add 3 檔（store.js +16 / verify-ocr-import.mjs +208 / subagent-log）——邊界合規，無夾帶他軌髒檔 ✅
- store.js diff 對計畫 §6.2b：照錄＋`String(w)` 正規化防呆強化（偏離計畫但無害，屬防禦性）✅
- 消費者對齊：tools.js:1105 `s.actions.importOcrText(picked)` 讀 `.added/.skipped` — 形態吻合 ✅

## 原驗證缺口（本稽核補測）
1. **事務失敗 added=0**：任務單 POST 明列項，原腳本零 `failInsert` 用例 → 本軌 G4 補測 ✅
2. **負控制「不過濾」態**：任務單 PRE 列「函式不存在/不過濾」兩態，原 NC 僅前者 → 本軌 N1 補測 ✅

## 四層內測門（本軌獨立重跑）
1. 靜態：`node --check` store.js＋audit 腳本 ✅；vite build 768ms ✅
2. 雙態：
   - 原腳本 HEAD 復跑 **16/16 ALL PASS**
   - 本軌 audit `_dev/notes/verify-ocr-t5-audit.mjs` **18/18 ALL PASS**：
     - P1 PRE 獨立重現＝`git show HEAD~1:src/lib/store.js` 真快照匯入 → 函式不存在 → 呼叫 TypeError 紅（非自製剝除檔，鑑識等級）
     - G1 垃圾過濾（123/3d/單字母/標點/#/超長32 全擋；don't/well-known 放行＋DB 實查）
     - G2 大小寫去重 added=1 skipped=3；G3 既有詞 skipped；G4 事務失敗 added=0＋DB 零新行；G5 回傳 key 集合＝importWords 形態
     - N1 剝除白名單 filter → added=3 垃圾落盤（敏感）；N2 NC 反換釘不誤傷
   - 證據 /tmp/ocr-t5-audit.log
3. Browser 內測（dev:5199 活體，共享 store 經 `/src/main.js` 模組快取取得）：
   - 學習主流程冒煙：注入 3 字 3 卡 → 點「學習」→ 翻卡模式 → 顯示答案 → Good 評分
     → **reps 0→1、state 0→1、due=當日、lastReview ✅、reviewLog=1**（importWords 觸碰零回歸）
   - OCR 活體路徑：`importOcrText(['Audit','123','AUDIT'])` → 過濾後 'audit' 抵達 importWords、垃圾零洩漏；
     無 DB 邊界錯誤與 importWords 直調**逐字元相同**（零新增失敗模式；無 DB 態 state 污染＝T1 已登記既有基線）
   - 工具頁 render：#ocrCaptureBtn/#ocrEngineSelect/#ocrConfirmBtn 全在位
   - console：js_errors=0、total_errors=0
4. 證據：本檔＋audit 腳本＋/tmp/ocr-t5-audit.log

## 裁定
- T5 實裝**品質合格**：功能正確、計畫合規、驗證鏈完整（補測後）。commit 2dae766 維持不动。
- 流程缺陷登記：調度雙派同檔子任務（教訓已寫 checkpoint）；原腳本漏 2 測試用例（已由本軌補足，若總統要求可併入 tools/ 腳本升版）。
- 後續 T6 屬 pocr2c 軌，本軌不越權。
