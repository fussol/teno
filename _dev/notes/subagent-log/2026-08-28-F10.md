# Subagent log — F10（2026-08-28, PM3）

## 任務目標
F10：import_piper_model_dialog Android FilePath::Url(content://) as_path()=None 必敗。
修法＝鏡同檔 import_db_dialog 慣例（cfg match + copy_uri_to_cache + .onnx 守門）。

## 委員（R1，1 席，leaf delegate_task）
裁決：❌（阻斷 1）

### 委員實際執行與結果（自報）
- (a) `node tools/verify-f10-import-piper-android.mjs` → 23 PASS / 0 FAIL ✅
- (b) `cd src-tauri && cargo test --lib piper` → 16 passed ✅
- (c) 負控制：`git show HEAD:src-tauri/src/lib.rs` 舊函式 `as_path().ok_or("無效路徑")` 在位、零 cfg；
      tauri-plugin-fs-2.5.1 file_path.rs `Self::Url(_) => None` 源碼屬實 ✅
- (d) 消費者：settings.js:741→api.js:63 唯一鏈；桌面 as_path→into_path 等價且更穩 ✅
- (e) diff 純度 48+/5- 僅 import_piper_model_dialog＋import_piper_path_tests ✅
- (f) 邊界攻擊 → **阻斷鑿出**：TtsPlugin.kt:272
      `val outFile = java.io.File(activity.cacheDir, "import.db")` 硬編碼落地名
      → piper 鏈 `.onnx` 守門必然拒絕 import.db → Android SAF 仍 100% 失敗

### 首相覆核
`grep copyUriToCache TtsPlugin.kt` 親驗 :272 原文屬實（非委員幻讀）。
連帶發現 import.db 固定名與 db 匯入共用 cacheDir（交錯匯入互覆寫，序貫無恙）。

## 裁決與處置
- bug 真身橫跨 lib.rs＋TtsPlugin.kt 兩檔；後者非 PM3 白名單（gen/android）
- 鐵律⑦：登 F10-SR1（scope-requests.md）→ 跳過改派，**本單不 commit**
- lib.rs 改裝回滚；資產封存：
  - _dev/notes/F10-pending.patch（lib.rs 改裝 76 行 diff，R1 已核 diff 純度）
  - _dev/notes/F10-pending-verify.mjs（曾 23/23 綠；復用時 T1 補釘 Kotlin 落地名非硬編碼）
- 首相文書不實兩處勘誤：§5 Kotlin 兜底敘述（他庫腦補）、§3 async 化敘述（HEAD 本 async）

## 未完成事項
- F10 端到端修復（待總統批 F10-SR1 後兩檔一單：Kotlin DISPLAY_NAME 派生檔名＋兜底
  sanitize，lib.rs 併 pending patch；Android SAF 真機流程仍需用戶終端驗證）

## 教訓（記 skill）
「鏡像倉內既有委派模式」驗證面只覆蓋本檔；委派鏈下游（Kotlin/native）的檔名/語意合約
必須在送審前實讀下游源碼，否則結構釘＋交叉編譯全綠照樣交付必敗路徑。
