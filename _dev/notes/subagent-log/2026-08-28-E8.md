# E8 委派紀錄（2026-08-28，首相2／PM2）

## 任務目標
修復 `tools/cli.mjs` `cmdSelfTest()` 第 3 段死引用：import
`../src/lib/sim-engine.js`（不存在，實際在 `src/lib/deprecated/`）→
selftest 永遠 `❌ 模擬引擎 Cannot find module`（實測 11 通過/1 失敗）。
修法＝整段退役刪除（非改路徑；方案 A 因政策隔離＋零防卫价值＋E16 衝突被否決），
核准退役註解文字由審查契約 pin 死。

## 委員裁決
### R1（3 名，Hermes delegate leaf，全程唯讀）
- **#1 修法正確性 ✅**：獨立查證五項全過——全庫 sim-engine 消费者穷举（app 端
  零消费者）、deprecated 檔頭政策註解實錘（「禁止接回」）、git 漂移史
  （0b43e48 初始引用 → c461727 搬入 deprecated 未同步 selftest）、負控制實跑、
  否決方案 A 三理由成立＋加證（A 會拖入 sim-behavior.js 隔離碼非「一行修」）。
  LOW：計數 12→11 非 v1.0 誤記 9（已採納）。
- **#2 消費者穷举 ✅**：selftest 輸出/「N 通過 / M 失敗」計數/「[TEST]」標記
  零機械消費者（src、Discord bot、cron 穷举）；段落無共用變數交錯；無孤立
  import；T4 walk 排除 deprecated 目錄與 E16 白名單等價；負控制真實性 /tmp
  修法後狀態模擬 18/18 實錘、真檔 md5 前後一致零污染。
- **#3 牙檢 ❌（兩缺陷）**：
  1. T2e/f/g 無 `✅ ` 前綴 → 變異 c2（誤刪容器段代碼行）ReferenceError 訊息
     拼接湊出 pin 子串**假綠**（實測實錘）。
  2. 註解 token 衝突：退役註解自然寫法（提 sim-engine 檔名）觸 T1a/T4a **假紅**
     （fix-natural 變異實測）→ 採方案 (ii) pin 死核准註解文字。
  其餘變異有牙：mut-a 方案 A 七發斃命、mut-b 不留言／mut-d marker 改字均
  fail-safe 連坐紅、T3 重組無假綠路徑。

### R2（#3 複審）✅ 放行
- 五項複審全過：腳本 diff 僅三處前綴；fix-clean 18/18×2；變異 c2 → T2f 轉紅
  實錘（v1.0 對照組假綠親眼復現）；fix-natural 紅＝契約機械強制；基準未修態
  負控制群 6/6 綠。

## 驗證命令＋實際輸出
```
$ node --check tools/cli.mjs            → CHECK-OK
$ node tools/verify-e8-selftest.mjs     → 18 PASS / 0 FAIL — ALL PASS（×3 連跑穩定）
$ TENO_DB=<tmp副本> node tools/cli.mjs selftest
  → ═══ 結果: 11 通過 / 0 失敗 ═══（修前：11 通過 / 1 失敗）
$ node tools/verify-e4-rate-log.mjs     → 24/24 ALL PASS
$ node tools/verify-e5-seeded-fuzz.mjs  → 18/18 ALL PASS
$ node tools/verify-e6-study-log.mjs    → 22/22 ALL PASS
$ node tools/verify-e7-mode-cards.mjs   → 20/20 ALL PASS
```
（vite build 結果補記於 commit log）

## 未完成／範圍外
- E10（selftest 容器檢查 `|| true` 恆真）：佇列下一顆獨立單。
- E16 孤兒刪除本體＋`deprecated/sim-behavior.js` 連帶孤兒（不在 PM2 白名單，
  E16 時視情況登 scope-requests）。
- `_dev/cli/cli.mjs:2104` 同款死引用：舊版禁碰。
- 工作區 SR-C4 hunk：非 E8 產物，commit 前反剝、事後還原（E6/E7 既定程序）。
