# D19 執行紀錄（2026-08-28，首相2／PM2）

## 任務目標
封閉 CLI `import-db` 零驗證入口（Bug D19）：垃圾/截斷/損壞容器直接覆寫主 DB。
計畫書 `_dev/notes/D19-fix-plan.md`（v1.0 → R1 兩 ❌ → v1.1 → R2 全 ✅）。

## 修法（tools/cli.mjs cmdImportDb，單檔 guard 入口）
- `unpackContainer` 之後、`backupDb()` 之前加 SQLite magic 守門：
  `isSqlite = Buffer.isBuffer(b) && b.length >= 100 && magic(16B) === 'SQLite format 3\0'`
- teno 段不符 → `❌ 拒絕匯入`（凍結字面量）＋ log('ERROR') ＋ exitCode 1，
  拒絕路徑零副作用（不備份、不 rmWal、不覆寫、零 audit）。
- **v1.1 關鍵修正（R1）**：log 條 `logBytes?.length &&`（空 Buffer truthy 誤拒
  l2=0 正品實測）＋落地條 `if (logBytes?.length)` 順手殲滅同根既存蟲
  （`export-db --no-log` 產物 import 把 app-log.db 覆寫成 0 字節，向 Rust
  `write_db_container` `if !log.is_empty()` lib.rs:576 對齊）。
- 連帶調整 `tools/verify-d7-restore-wal.mjs`：T4 容器 log 段 'LOGDATA'→
  `Buffer.alloc(0)`（現行 magic 守門正確拒絕非 SQLite log 段屬預期；該釘目標
  是 rmWal 覆寫路徑，log 段語意由 verify-d19 全家覆蓋）。

## 審查
- R1（v1.0，3 委員）：#1 ✅有條件／#2 ❌阻斷級（空 log 段誤拒正品＋清零蟲）／
  #3 ❌兩洞（mutB >=16 存活、mutE 寬鬆匹配）→ v1.1 全採納。
- R2（v1.1 複審，R1 ❌ 兩席）：**#2 ✅／#3 ✅ 全席過審**。
  - #2：變異牙檢 mut-gate→T9a 紅、mut-land→8192B app-log 清零實測＋T9b 紅；
    T9 測資與 packContainer(includeLog=false) 逐字節 equals（514B）；
    T7b 真機 raw 檔在場實跑。
  - #3：四變異體全殲（mutB→T10 雙紅、mutDel→T3-T6 十六釘紅、
    mutC→T8d 紅＋區段限定比對修 R1 殘留越函式誤匹、mutLit→凍結字面量釘紅）；
    T8 ORIGINAL_BLOCK 822B 與 git show HEAD BYTE-IDENTICAL 獨立比證；
    判針純度合規（拒絕斷言只用 `❌ 拒絕匯入`＋stdout＋exit 1）。

## 驗證（實際執行輸出）
- `node tools/verify-d19-import-magic.mjs` → **32 PASS / 0 FAIL — ALL PASS**
  （送審前＋SR-C4 反剝後各一跑皆綠；負控制 T8 舊版反換垃圾直入精準重現、
  T8d 守門順序釘、T9 l2=0 正品、T10 32B/99B 截斷偽檔釘）。
- 回歸（反剝後）：d7 18/18、d6 14/14、e4 24/24、e5 18/18、e8 18/18、
  c3 ALL PASS（需 `--experimental-test-module-mocks`，Node 26）、c5 96/96、
  `node --check tools/cli.mjs` OK、vite build 680ms。全部 exit 0。

## commit 程序
SR-C4 makeSession hunk commit 前反剝（`/tmp/d19work-sr4-hunk.txt` 存檔）、
commit 僅含 tools/cli.mjs（D19 段）＋verify-d19＋verify-d7 調整＋計畫書＋本紀錄；
scope-requests.md 工作區改动屬 PM1（G8/G26 裁示），未入本 commit；事後 hunk 原樣還原。

## 範圍外（計畫書 §6 逐名）
unpackContainer truncated fallback 本體、import 來源檔備份、
cmdRestore/cmdBackups/cmdDrive magic 面、CLI 全域 exit code 系統化、
目標 DB 已損壞時頂層 crash 救援路徑（另單）、l2 欄截斷靜默丟 log（另單）、
100B magic＋垃圾偽檔 page-size sanity（非本單）。

## 未完成事項
無。本顆閉合。
