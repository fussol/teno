# 2026-08-28 F11 審查紀錄（PM4）

## 任務目標
殲滅 drive_sync.rs OAuth client secret 硬編碼（洩漏進源碼 repo＋每個編譯產物）＋
tokens/creds 明文 0644（同機任何使用者可讀 refresh_token＝Drive 帳號長期存取權）。
計畫書 `_dev/notes/F11-fix-plan.md`（v1→v1.1 送審前定稿→v1.2 R1 吸收）。

## 審查歷程
### R1（3 委員，Hermes delegate leaf，全程唯讀）
- **#1 Rust/安全：✅ 有條件**——降級鏈閉環核對（load_creds→creds_valid→drive_status→ensure_token→drive_oauth）、寫點穷举完備（憑證寫盤唯 :58/:71 兩處）、include_str! 自掃合法性確認。附必修：(a) snippet 缺 PermissionsExt import（照抄 E0599）(b) 測試名與 verify 錨點不一致 (c) include_str! needle 自咬陷阱（拆串构造）(d) T4b 產物正則實測命中 0 (e) **L1 洩漏衛生：pm4.runner.log 含 secret 完整值，commit 波必 scrub 或不入**；建議 fd 級 set_permissions 加固。
- **#2 消費者/影響面：✅**——DEFAULT_* 全庫消費者穷举（Rust 唯 load_creds fallback；cli.mjs:3233-3260 鏡像自帶「未設定」分支從不吃 Rust 預設；cron/bot 零）；fresh install 引導鏈實證閉環（settings.js:366-378 輸入框＋:883-888 存回）；本機實測 drive_creds/drive_tokens 現行 0644 屬實（bug 真）。次要：S1 R1 風險宣稱不完整（需 re-login 非僅貼憑證＋受眾須自建憑證）；S2 CLI 鏡像 writeFileSync 不收緊登範圍外；nit N4 更換憑證 UI 缺。
- **#3 驗證鑑識：❌ 4 阻斷**——**B1** const+`unwrap_or`=E0658（rustc 1.96 實測，計畫書代碼編不過）；**B2** write_private import 缺 PermissionsExt（E0599 實測）；**B3** verify T6 harness 缺 MetadataExt（NC 腿非無牙是編炸）；**B4** T4 產物正則 `/^teno/` 對真實 `libteno-*` 命中 0→T4b 假紅＋T4c 空集假綠。次要：S1 測試名、S2 掃描器正向釘缺（File::create 繞道盲）、S3 拆串迴避＝過度防衛不加碼只登記；屬實面：PRE 15/15 重跑屬實、pin 9e3116b 逐字節核對有效、T4 rmeta 嵌入前提實測成立、T6 NC umask 行為前提實測成立。

### 首相 R1 吸收（v1.2）
B1 match 形（Some/None，const-eval 合法）；B2/B3 import 修正＋採納 #1 fd 級先 chmod 後寫；B4 正則 `^(lib)?teno`＋非空守衛；S1 測試名 f11_* 統一；S2 正向釘 saveCallsPrivate（T1g/T5g）；S1(#2) R1 風險誠實化；S2(#2)/N4/nit1 登範圍外；L1 pm4 兩 log 就地 scrub（`[REDACTED-F11-SCRUB]`）＋R5 commit 波紀律（spot.log 為 runner 即時轉錄本會回血→commit 前終掃＋兩 log 不入庫）。PRE 復跑 17/17。

### R2（#3 原席復審）：✅
B1-B4/S1-S3 逐項閉合實測（match 形雙鏈版實編＋嵌入 strings 命中；§2 代碼逐字餵提取器＋harness rustc 實編 MICRO_OK；NC panic mode=644 有牙；正則命中 15 對照 0）。新攻擊：option_env None 臂空降級正確、fd 版 symlink 追隨＝與舊碼 fs::write 同（非回歸，前提屬帳號淪陷，資訊性）、提取器對 v1.2 形態穩定。追盯項 N1：spot.log 殘片 commit 波前終掃。PRE 17/17 復核。

## 動工（drive_sync.rs 單檔）
1. :43-44 硬編碼常數→`match option_env!("TENO_DRIVE_CLIENT_ID/SECRET") { Some(s)=>s, None=>"" }`
2. 新增 `write_private`（cfg(unix)：OpenOptions mode(0o600) 開→fd set_permissions(0600)→write_all→flush；Windows 降級 fs::write）
3. save_creds/save_tokens 改走 write_private
4. mod tests 新增 f11_no_hardcoded_oauth_secret（include_str! 自掃＋needle 拆串防自咬）＋f11_write_private_0600

動工中誠實登記：測試初版 `.mode()` 缺 MetadataExt import→cargo E0599 一發自修（同 B3 教訓親踩）。

## 驗證（tools/verify-f11.mjs，實跑）
- PRE 態 17/17×2（含並行委員）＋POST 態 29/29：T1 靜態×7・T2 cargo 測試名×2・T3 零 env 可編譯・T4 注入假憑證真嵌入 rmeta＋touch 重編殲滅×3・T5 負控制 pin 9e3116b 精準紅集×7・T6 write_private 微編譯行為＋NC 0644 復現×5・T7 空值語意釘×4。
- 離線紀律：全程零 Google API 觸碰（純靜態/編譯/微編譯斷言）。

## 回歸
cargo test drive_sync 4→6 全綠；全量 cargo test 34 passed＋sim_tests 3 failed＝既有環境缺測資（/tmp/sim-req-*.json NotFound，lib.rs:1826 load_req，F1 commit 同紀錄先例，非本檔引入）；vite build 794ms ✓；verify-d19 32/32・verify-d7 18/18・verify-e4 24/24。

## 範圍外/連帶登記
- F11-SR1（build.rs rerun-if-env-changed）登 scope-requests.md（白名單外）。
- 憑證輪換（git 歷史仍含舊值，Console 撤銷為唯一根治）→ 交付通知必列。
- CLI 鏡像不收緊、更換憑證 UI、secret 輸入框明文、re-login 錯誤引導、save 靜默吞失敗——全登計畫書 §範圍外。
- 既有使用者一次性行為變更：Drive 變「未設定」需貼憑證＋重新登入（R1 已誠實化）。

## 未完成事項
無（本顆閉環）。F11-SR1 屬總統裁示項；D11/D10 為佇列後續。
