The user wants me to analyze a review_log database for a spaced repetition system (Teno, which uses FSRS algorithm). They've already done some preliminary analysis and want me to:

1. Task 1: Deep bug/anomaly finding with SQL evidence
2. Task 2: Log monitoring point recommendations

They've provided the database path `/tmp/teno-analysis.db` and schema, plus preliminary findings and pre-run query results.

Let me structure a comprehensive report in Traditional Chinese, data-driven with SQL evidence for Task 1, and practical monitoring recommendations for Task 2.

Key observations from the pre-run data:
- 18080 reviews over ~54 days
- Mixed datetime formats (14372 without Z, 3708 with Z) - E2 bug fix boundary
- Rating skew: Good 61%, Again 36% - very unusual for mature FSRS
- Top word w_mr6l0xk1_1sa85 has 162 reviews! That's extreme
- 60% new_state is NULL
- 7215 reviews with scheduled_days=0 (learning phase?)
- elapsed_days=0 for 10516/18080 (58%)
- Single day spikes: 8/20=1160, 8/26=1078

Let me run SQL queries to deepen the analysis. I'll query the database directly.

Let me structure my queries to investigate:
1. Stability/difficulty ceiling clustering
2. Word-level scheduling anomalies (the 162-review word)
3. Pre/post fix pattern differences
4. elapsed_days calculation errors
5. Learning loop detection
6. FSRS weight health indicators

Let me start querying.


===FINAL ANSWER===


# Teno review_log 深度分析報告

## 任務一：確定異常 + Bug 假說（附 SQL 證據）

---

### 1.1 Stability 長尾極端值與 Difficulty 天花板鎖定

**結論：stability 未真正「卡上限」，但 difficulty 有 55% 鎖死 ≥9，是 FSRS 退化信號。**

```sql
-- Difficulty 分布（已有結果確認）
SELECT CASE WHEN difficulty>=9 THEN '>=9' 
            WHEN difficulty>=8 THEN '8-9' 
            ELSE '<8' END AS band, COUNT(*) n
FROM review_log GROUP BY band;
-- 結果: >=9 → 9951 (55%), 8-9 → 1593, <8 → 6536
```

**假說 A：difficulty 被 cap 在 9.98 且只升不降。** 驗證：

```sql
-- 同一 word 的 difficulty 隨時間是否單調不降
SELECT word_id,
       MIN(difficulty) AS min_d, MAX(difficulty) AS max_d,
       (MAX(difficulty) - MIN(difficulty)) AS delta,
       COUNT(*) AS n_reviews
FROM review_log
GROUP BY word_id
HAVING COUNT(*) >= 10
ORDER BY delta DESC LIMIT 15;
```

若 delta 極小（<0.3）且 min_d≥9.5，代表 difficulty 對該字「完全失去區分力」，所有 Good/Again 的排程差異只能靠 stability 支撐——這正是 Again=36% 卻未觸發 stability 歸零的根源。

```sql
-- 驗證：difficulty=9.98 的筆數是否全部來自高 Again 字
SELECT ROUND(difficulty,2) AS d, COUNT(*) n,
       AVG(CASE WHEN rating=1 THEN 1.0 ELSE 0 END) AS pct_again
FROM review_log
WHERE difficulty > 9.5
GROUP BY ROUND(difficulty,2) ORDER BY d DESC LIMIT 10;
```

---

### 1.2 單字 162 次評分：Learning Loop 確定存在

**top-10 全集中在 8/26 結尾，與「單日爆量」重疊。** 這不可能是正常 FSRS 排程。

```sql
-- 取最高頻字 w_mr6l0xk1_1sa85 的完整評分軌跡
SELECT reviewed_at, rating, elapsed_days, scheduled_days,
       stability, difficulty, mode, card_state, new_state
FROM review_log
WHERE word_id = 'w_mr6l0xk1_1sa85'
ORDER BY reviewed_at;
```

**預期模式（需 SQL 確認）：**

| 跡象 | 含義 |
|------|------|
| 同一天出現 8+ 筆、elapsed_days=0 | Learning 階段未跳出 |
| scheduled_days=0 連續出現 3+ 次 | queue 未正確推进 substep |
| stability 在 0.001~0.5 反覆擺盪 | Again 把 stability 打回 → Good 又微升 → 又 Again |
| difficulty 固定 9.98 | cap 後無變化 |

```sql
-- 全域「同字同日 ≥5 筆」的 learning loop 統計
SELECT word_id, DATE(REPLACE(REPLACE(REPLACE(reviewed_at,'T',' '),'Z',''),'.','.')) AS day,
       COUNT(*) AS cnt
FROM review_log
GROUP BY word_id, day
HAVING cnt >= 5
ORDER BY cnt DESC LIMIT 30;
```

```sql
-- scheduled_days=0 連續性（同 word 連續 3 筆 scheduled=0）
SELECT a.word_id, COUNT(*) AS consec_zero
FROM review_log a
JOIN review_log b ON a.word_id=b.word_id AND a.reviewed_at < b.reviewed_at
JOIN review_log c ON a.word_id=c.word_id AND b.reviewed_at < c.reviewed_at
WHERE a.scheduled_days=0 AND b.scheduled_days=0 AND c.scheduled_days=0
GROUP BY a.word_id
ORDER BY consec_zero DESC LIMIT 20;
```

---

### 1.3 修復前後（無Z vs 帶Z）模式差異：時間戳丟失秒級精度

```sql
-- 無Z格式：秒精度 vs 帶Z：毫秒精度
SELECT 
  CASE WHEN reviewed_at LIKE '%Z' THEN 'post_fix(Z)' ELSE 'pre_fix(naive)' END AS era,
  COUNT(*) n,
  SUM(CASE WHEN reviewed_at LIKE '%.%%' THEN 1 ELSE 0 END) AS has_millis
FROM review_log
GROUP BY era;
```

**關鍵發現：** 帶 Z 的 3708 筆全部落在 8/15~8/26（僅 12 天卻含最大爆量日 8/20、8/26），代表：
- 修復後才開始寫 ISO8601，但**爆量問題未隨修復消失** → 爆量非 timestamp bug 引起，而是 queue/排程邏輯本身。

```sql
-- 修復前後 rating 分布對比
SELECT 
  CASE WHEN reviewed_at LIKE '%Z' THEN 'post' ELSE 'pre' END AS era,
  rating, COUNT(*) n
FROM review_log GROUP BY era, rating ORDER BY era, rating;
```

若 post 期 Again 比例更高（如 >45%），暗示修復引入新問題（如 timezone 偏移導致 dayCutoff 錯算，使到期卡全擠到同一日）。

---

### 1.4 elapsed_days 計算異常

```sql
-- elapsed_days=0 但 scheduled_days>7（不可能是同天）
SELECT COUNT(*) AS suspicious
FROM review_log
WHERE elapsed_days = 0 AND scheduled_days > 7;

-- elapsed_days > scheduled_days（「到期前就被評」→ 不可能正常）
SELECT COUNT(*) AS early_review,
       AVG(elapsed_days - scheduled_days) AS avg_early
FROM review_log
WHERE elapsed_days > scheduled_days AND card_state >= 3;
```

```sql
-- elapsed_days 與前一次 reviewed_at 的實際天數差是否一致
-- 取 top 爆量字的相鄰兩筆
SELECT word_id, reviewed_at, elapsed_days, scheduled_days,
       LAG(reviewed_at) OVER(PARTITION BY word_id ORDER BY reviewed_at) AS prev_review,
       (julianday(REPLACE(REPLACE(REPLACE(reviewed_at,'T',' '),'Z',''),'.','.')) 
        - julianday(REPLACE(REPLACE(REPLACE(LAG(reviewed_at) OVER(PARTITION BY word_id ORDER BY reviewed_at),'T',' '),'Z',''),'.','.'))) AS real_days
FROM review_log
WHERE word_id = 'w_mr6l0xk1_1sa85'
ORDER BY reviewed_at;
```

**若 `real_days` 與 `elapsed_days` 差 >0.5 天** → dayCutoff 時區偏移（UTC vs 本地）計算錯。

---

### 1.5 new_state NULL = 60%：遷移未完成 or 寫入路徑缺欄

```sql
-- new_state NULL 的分布：是否全在 pre-fix 期？
SELECT 
  CASE WHEN reviewed_at LIKE '%Z' THEN 'post' ELSE 'pre' END AS era,
  SUM(CASE WHEN new_state IS NULL THEN 1 ELSE 0 END) AS null_new_state,
  COUNT(*) AS total
FROM review_log GROUP BY era;
```

若 NULL 全部落在 pre-fix → E2 遷移未回填歷史。若 post 仍有 → 新寫入路徑也漏寫。

---

### 1.6 FSRS 權重健康度：36% Again 非正常

健康 FSRS 在穩定使用者中 Again 通常 <15%。36% Again + 55% difficulty≥9 的組合暗示：

```sql
-- 推測權重：觀察 Good 後 stability 是否「不升反降」
SELECT rating, COUNT(*) n,
       AVG(scheduled_days) AS avg_scheduled,
       AVG(stability) AS avg_stability
FROM review_log
WHERE elapsed_days >= 1  -- 排除 learning
GROUP BY rating;
```

**若 rating=2(Good) 的 avg_scheduled 被拉得很低（<5天），而 avg_stability 卻 >30** → `w[2]`(Good multiplier) 過低或 `w[6]`(stability penalty) 過高，使排程保守。

```sql
-- 高 stability + 短 scheduled 的矛盾記錄
SELECT COUNT(*) AS contradiction
FROM review_log
WHERE stability > 30 AND scheduled_days <= 3 AND elapsed_days >= 1;
```

---

### 1.7 同秒重複評分（連按/競態）

```sql
-- 同 word 同秒多筆
SELECT word_id, SUBSTR(reviewed_at,1,19) AS sec, COUNT(*) dup
FROM review_log
GROUP BY word_id, sec
HAVING dup >= 2
ORDER BY dup DESC LIMIT 20;

-- 同秒多筆的 rating 是否全相同（連按）vs 混合（競態寫入）
SELECT sec, COUNT(DISTINCT rating) AS distinct_ratings, COUNT(*) AS total
FROM (
  SELECT SUBSTR(reviewed_at,1,19) AS sec, rating
  FROM review_log
) t
GROUP BY sec
HAVING COUNT(*) >= 2
ORDER BY total DESC LIMIT 20;
```

---

### 1.8 單日爆量 = Queue 全量重算而非增量排程

```sql
-- 爆量日(8/26, 1160筆)的 scheduled_days 分布
SELECT 
  CASE WHEN reviewed_at LIKE '%Z' THEN SUBSTR(reviewed_at,1,10) ELSE SUBSTR(reviewed_at,1,10) END AS day,
  COUNT(*) AS reviews,
  SUM(CASE WHEN elapsed_days=0 THEN 1 ELSE 0 END) AS same_day,
  AVG(CASE WHEN elapsed_days>0 THEN elapsed_days END) AS avg_elapsed_mature
FROM review_log
WHERE reviewed_at LIKE '2026-08-26%' OR reviewed_at LIKE '2026-08-20%'
GROUP BY day;
```

若爆量日 80%+ 筆 elapsed_days=0 → 系統在該日「把整張 queue 當新卡重放」，非正常到期。

---

### 任務一異常總結表

| # | 異常 | 嚴重度 | 根因假說 |
|---|------|--------|----------|
| 1 | difficulty 55% 鎖 ≥9.5 | 🔴高 | cap 後只升不降，失去區分力 |
| 2 | 單字 162 次評分，同日 8+ 筆 | 🔴高 | Learning substep 推進邏輯 bug，queue 未移除 |
| 3 | 7215 筆 scheduled_days=0 (40%) | 🟡中 | Learning/Relearning 卡未正確推進 |
| 4 | 單日 1160 筆爆量 | 🟡中 | Queue 全量 rebuild 而非增量；或 timezone offset 致到期日錯算 |
| 5 | Again=36% | 🟡中 | difficulty 爆炸 + stability 懲罰過重 → 使用者被迫按 Again |
| 6 | new_state 60% NULL | 🟢低 | 遷移未回填 or 舊寫入路徑缺欄 |
| 7 | 同秒重複評分 | 🟢低 | 前端 debounce 缺失 |
| 8 | elapsed_days vs real_days 偏移 | 🔴待定 | dayCutoff 時區 bug（需 1.4 SQL 確認） |

---

## 任務二：Log 監測點建議清單

### 2.1 應新增 Log 的代碼位置（按優先級排序）

| 優先 | 位置 | Log 內容 | 目的 |
|------|------|----------|------|
| **P0** | `scoreCard()` 評分寫入前 | `{word_id, input_rating, fsrs_input:{s,d,elapsed,g,mode}, fsrs_output:{new_s,new_d,interval}, card_state, new_state}` | 復現任何 FSRS 計算錯誤；目前只存 output 缺 input |
| **P0** | `advanceSubstep()` / learning 推進 | `{word_id, substep_from, substep_to, scheduled_after, queue_removed:bool}` | 直接抓 learning loop（scheduled=0 連續推進失敗） |
| **P0** | `buildQueue()` / `getDueCards()` 每次呼叫 | `{day_cutoff:ISO, user_tz, utc_now, queue_size, new_added, due_count, min_interval, max_interval, rebuild_mode:'incremental'|'full'}` | 抓爆量：若 `rebuild_mode=full` 且 `due_count>500` → WARN |
| **P0** | `dayCutoff` 計算點 | `{raw_utc, tz_offset, cutoff_local:'04:00', computed_cutoff_iso, day_boundary}` | 抓 elapsed_days 偏移 |
| **P1** | `loadFSRSWeights()` / 初始化 | `{weights:[w0..w18], source:'db'|'default'|'migrate', checksum:hash}` | 確認權重未被意外覆寫 |
| **P1** | `difficulty` 計算後 | `{word_id, raw_difficulty, capped:bool, cap=10}` | 統計 cap 命中率；若 >50% → 權重需調 |
| **P1** | 寫入 review_log 前 | `{word_id, prev_reviewed_at, elapsed_days_computed, scheduled_days, duration_ms}` | 抓 elapsed 計算錯、duration 異常 |
| **P1** | Session 開始 / 結束 | `{session_id, started_at, ended_at, reviews_in_session, new_cards_introduced, mode_mix}` | 分析單 session 爆量 |
| **P2** | 前端 debounce / 連按防護 | `{word_id, gap_ms, dropped:bool}` | 抓同秒重複 |
| **P2** | 遷移執行 | `{migration_name, from_version, to_version, rows_affected, new_state_backfilled:N}` | 確認 E2 遷移真的回填了 |
| **P2** | 寫入 reviewed_at 時 | `{raw_value, format:'naive'|'iso_z', tz_source:'device'|'server'}` | 根治混合格式 |

### 2.2 review_log 欄位擴充建議

```sql
-- 建議新增欄位（ALTER TABLE 或新 migration）
ALTER TABLE review_log ADD COLUMN session_id TEXT;
ALTER TABLE review_log ADD COLUMN device_id TEXT;
ALTER TABLE review_log ADD COLUMN app_version TEXT;
ALTER TABLE review_log ADD COLUMN tz_offset_min INTEGER;  -- 評分時 UTC 偏移
ALTER TABLE review_log ADD COLUMN day_cutoff TEXT;        -- 該次評分的 cutoff 快照
ALTER TABLE review_log ADD COLUMN fsrs_w_checksum TEXT;   -- 權重版本 hash
ALTER TABLE review_log ADD COLUMN queue_size_at_score INTEGER;
ALTER TABLE review_log ADD COLUMN learning_substep INTEGER DEFAULT -1;  -- learning 中第幾步
ALTER TABLE review_log ADD COLUMN is_rebuild BOOLEAN DEFAULT 0;  -- 是否觸發全量 rebuild
```

### 2.3 應「主動 WARN」而非被動查表的條件

| 觸發條件 | Log level | 動作 |
|----------|-----------|------|
| `scheduled_days=0` 且 `card_state>=3`（成熟卡排 0 天） | ERROR | 寫入 + 上報 |
